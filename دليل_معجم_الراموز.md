# 🔍 دليل معجم الراموز - Arramooz Dictionary Guide

**التاريخ**: 2025-12-05  
**الحالة**: دليل شامل

---

## 🎉 الاكتشاف المهم!

تم اكتشاف **معجم الراموز** في المشروع!

### 📊 الإحصائيات:
- **40,850 كلمة عربية** 🎯
- أسماء وأفعال
- جذور وأوزان
- معاني وتعريفات

---

## 📁 الملفات الموجودة

### في `/vocabulary/`:

1. **`arramoozDictionaryAdapter.ts`** - المحول الأصلي (TypeScript)
2. **`foundationVocabulary.ts`** - القاموس الأساسي
3. **`completeVocabulary.ts`** - القاموس الكامل
4. **`foundationVocabularyComplete.ts`** - القاموس الأساسي الكامل
5. **`foundationVocabularyExtended.ts`** - القاموس الأساسي الموسع
6. **`unifiedLexiconSystem.ts`** - نظام المعجم الموحد

---

## ✅ ما تم إنجازه

### تحويل إلى Python:

تم إنشاء **`bayan/bayan/arramooz_adapter.py`**

**الميزات**:
- ✅ الوصول إلى 40,850 كلمة
- ✅ البحث عن كلمات
- ✅ البحث بالجذر
- ✅ استخراج معلومات صرفية
- ✅ ذاكرة مؤقتة للأداء

---

## 🚀 الاستخدام

### 1. الاستخدام الأساسي:

```python
from bayan.bayan.arramooz_adapter import ArramoozAdapter

# تحميل قاعدة البيانات
adapter = ArramoozAdapter()
adapter.load_database()

# البحث عن كلمة
word = adapter.search_word("مدرسة")
if word:
    print(f"الكلمة: {word.arabic}")
    print(f"الجذر: {word.root_word}")
    print(f"المعنى: {word.core_meaning}")

# البحث بالجذر
words = adapter.search_by_root("درس")
for w in words:
    print(f"- {w.arabic}")

# الإحصائيات
stats = adapter.get_statistics()
print(f"المجموع: {stats['total']} كلمة")

# إغلاق
adapter.close()
```

### 2. استخدام Context Manager:

```python
with ArramoozAdapter() as adapter:
    word = adapter.search_word("كتاب")
    print(word.arabic)
    # يُغلق تلقائياً
```

---

## 💡 دمج مع محرك الاستنباط

### الفكرة:
استخدام معجم الراموز لتوسيع قاعدة المعرفة تلقائياً!

### مثال:

```python
from bayan.bayan.istinbat_engine import IstinbatEngine
from bayan.bayan.linguistic_equation import KnowledgeBase
from bayan.bayan.arramooz_adapter import ArramoozAdapter

# تحميل معجم الراموز
arramooz = ArramoozAdapter()
arramooz.load_database()

# إنشاء قاعدة معرفة موسعة
kb = KnowledgeBase()

# البحث عن جميع الأفعال من جذر معين
verbs = arramooz.search_by_root("كتب")

for verb in verbs:
    if verb.word_type.value == "فعل":
        # إضافة الفعل تلقائياً
        kb.add_custom_event(
            event=verb.arabic,
            subject_changes={"تعب": +0.2},
            object_changes={"موجود": True}
        )
        print(f"✅ تم إضافة: {verb.arabic}")

# الآن يمكن استخدام هذه الأفعال!
engine = IstinbatEngine()
result = engine.process("محمد يكتب رسالة")
```

---

## 🎯 حالات الاستخدام

### 1. توسيع قاموس الأفعال:

```python
# البحث عن جميع أفعال الحركة
movement_roots = ["ذهب", "جاء", "ركض", "قفز", "سبح"]

for root in movement_roots:
    verbs = arramooz.search_by_root(root)
    for verb in verbs:
        # إضافة للقاموس
        pass
```

### 2. استخراج الجذور:

```python
# معرفة جذر أي كلمة
word = arramooz.search_word("مدرسة")
print(f"جذر 'مدرسة' هو: {word.root_word}")  # درس
```

### 3. البحث عن كلمات مرتبطة:

```python
# جميع الكلمات من نفس الجذر
words = arramooz.search_by_root("علم")
for w in words:
    print(f"- {w.arabic}: {w.core_meaning}")
```

---

## ⚠️ ملاحظات مهمة

### موقع قاعدة البيانات:

القاعدة موجودة في:
```
src/baserah/lexicon/databases/arramooz_dictionary.db
```

إذا لم تكن موجودة، ستحتاج إلى:
1. تحميلها من المصدر الأصلي
2. أو تحديد المسار الصحيح

### الأداء:

- ✅ ذاكرة مؤقتة للكلمات المبحوثة
- ✅ ذاكرة مؤقتة للجذور
- ✅ استعلامات SQL محسّنة

---

## 🔧 التطوير المستقبلي

### المقترحات:

1. **دمج كامل مع IstinbatEngine**
   - إضافة جميع الأفعال تلقائياً
   - استخراج النتائج من المعاني

2. **واجهة بحث متقدمة**
   - البحث بالوزن
   - البحث بالفئة
   - البحث بالمعنى

3. **تصدير إلى JSON**
   - حفظ القاموس كـ JSON
   - سهولة الاستخدام في تطبيقات أخرى

4. **دمج مع CAMeL Tools**
   - استخدام معجم الراموز كمصدر إضافي
   - تحسين دقة التحليل الصرفي

---

## 📊 مقارنة المصادر

| المصدر | عدد الكلمات | الميزات | الاستخدام |
|--------|-------------|---------|-----------|
| **معجم الراموز** | 40,850 | جذور، أوزان، معاني | ✅ متاح الآن |
| **CAMeL Tools** | ~100,000 | تحليل صرفي متقدم | ✅ مدمج |
| **قاعدة المعرفة** | 7 أفعال | نتائج سببية | ✅ يعمل |

### الدمج المثالي:

```
معجم الراموز (40K كلمة)
    ↓
CAMeL Tools (تحليل صرفي)
    ↓
قاعدة المعرفة (نتائج سببية)
    ↓
محرك الاستنباط (استنتاج)
```

---

## ✅ الخلاصة

### ما لديك الآن:

- ✅ معجم الراموز (40,850 كلمة)
- ✅ محول Python جاهز
- ✅ إمكانية الدمج مع محرك الاستنباط
- ✅ أمثلة استخدام واضحة

### الخطوة التالية:

**دمج معجم الراموز مع محرك الاستنباط!**

هذا سيعطيك:
- 🎯 قاعدة معرفة ضخمة
- 🚀 دقة أعلى بكثير
- 💪 تغطية شاملة للغة العربية

---

**آخر تحديث**: 2025-12-05  
**الحالة**: جاهز للاستخدام  
**المؤلف**: فريق تطوير بيان
