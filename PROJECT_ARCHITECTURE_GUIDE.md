# دليل البنية المعمارية الشامل | Comprehensive Architecture Guide
## مشروع لغة البيان - Bayan Programming Language

**التاريخ**: 2025-11-27  
**الإصدار**: 1.5.0+  
**المطور**: باسل يحيى عبدالله  

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الهيكلية العامة](#الهيكلية-العامة)
3. [المكونات الأساسية](#المكونات-الأساسية)
4. [سير تدفق الأوامر](#سير-تدفق-الأوامر)
5. [الأنظمة المتقدمة](#الأنظمة-المتقدمة)
6. [نظام الملفات](#نظام-الملفات)
7. [دليل التطوير](#دليل-التطوير)

---

## 🎯 نظرة عامة

### ما هي لغة البيان؟

**لغة البيان** هي أول لغة برمجة هجينة حقيقية في العالم تجمع ثلاثة أنماط برمجية:

1. **البرمجة الإجرائية** (Imperative) - متغيرات، حلقات، شروط
2. **البرمجة الكائنية** (OOP) - أصناف، وراثة، تعدد أشكال
3. **البرمجة المنطقية** (Logic) - حقائق، قواعد، استعلامات

### الميزات الفريدة

- ✅ دعم كامل للغة العربية (كلمات مفتاحية + معرّفات)
- ✅ نظام الدماغ المزدوج (Dual Brain Architecture)
- ✅ 7+ أنظمة ذكاء اصطناعي متقدمة
- ✅ محرر ويب متكامل (Web IDE)
- ✅ نظام رسوم شامل (SVG + Canvas)
- ✅ 621 اختبار شامل
- ✅ 148 مثال عملي

---

## 🏗️ الهيكلية العامة

```
bayan_python_ide144/
│
├── 📁 bayan/                    # ⭐ المفسر الرئيسي
│   ├── bayan/                   # الكود المصدري (23 ملف Python)
│   │   ├── lexer.py            # المحلل اللغوي
│   │   ├── parser.py           # المحلل النحوي (144K سطر)
│   │   ├── traditional_interpreter.py  # المفسر التقليدي (190K سطر)
│   │   ├── hybrid_interpreter.py       # المفسر الهجين
│   │   ├── logical_engine.py          # المحرك المنطقي
│   │   ├── dual_brain.py              # نظام الدماغ المزدوج
│   │   ├── entity_engine.py           # محرك الكيانات
│   │   ├── gse.py, gse_fitting.py     # محرك GSE
│   │   ├── mother_equation.py         # نظام المعادلة الأم
│   │   ├── linguistic_equation.py     # المعادلات اللغوية
│   │   ├── expert_explorer.py         # نظام الخبير-المستكشف
│   │   ├── compiler_interface.py      # واجهة المترجم
│   │   ├── builtins.py                # الدوال المدمجة
│   │   ├── import_system.py           # نظام الاستيراد
│   │   └── visualization.py           # التصور البصري
│   ├── main.py                  # نقطة الدخول الرئيسية
│   └── libraries/               # مكتبات إضافية
│
├── 📁 ai/                       # ⭐ مكتبات الذكاء الاصطناعي (21 ملف)
│   ├── ml.bayan                # خوارزميات التعلم الآلي (104K سطر)
│   ├── nlp.bayan               # معالجة اللغة الطبيعية (68K سطر)
│   ├── data.bayan              # هياكل البيانات (26K سطر)
│   ├── lexicon.bayan           # المعجم اللغوي
│   ├── morphology.bayan        # علم الصرف
│   ├── conceptual_blueprints.bayan    # المخططات المفاهيمية
│   ├── conceptual_circuits.bayan     # الدوائر المفاهيمية
│   ├── conceptual_programs.bayan     # البرامج المفاهيمية
│   ├── conceptual_orchestrator.bayan # منسق المفاهيم
│   ├── conceptual_surface_realizer.bayan  # محقق السطح
│   └── ...
│
├── 📁 nlp_bayan/                # ⭐ نظام NLP متقدم (81 ملف)
│   ├── core/                    # النواة الأساسية
│   │   ├── integrated_kb.bayan # قاعدة المعرفة المتكاملة
│   │   ├── simple_parser.bayan # محلل بسيط
│   │   └── simple_inference.bayan  # محرك الاستدلال
│   ├── examples/                # أمثلة NLP
│   └── ...
│
├── 📁 examples/                 # ⭐ أمثلة (148 ملف!)
│   ├── hello.by                # مرحباً بالعالم
│   ├── family.by               # شجرة العائلة (منطق)
│   ├── calculator.by           # آلة حاسبة (OOP)
│   ├── dual_brain_demo.py      # عرض الدماغ المزدوج
│   ├── gse_adaptive_demo.py    # عرض GSE التكيفي
│   ├── conceptual_*.bayan      # أمثلة مفاهيمية (20+ ملف)
│   ├── ar_*.md                 # أمثلة عربية
│   ├── svg_*.md                # أمثلة رسوم SVG
│   └── ...
│
├── 📁 tests/                    # ⭐ اختبارات (621 اختبار)
│   ├── test_*.py               # اختبارات شاملة
│   └── ...
│
├── 📁 web_ide/                  # ⭐ محرر الويب
│   ├── app.py                  # تطبيق Flask
│   ├── templates/              # قوالب HTML
│   │   ├── ide.html           # واجهة المحرر
│   │   └── ...
│   └── user_scripts/           # سكريبتات المستخدم
│
├── 📁 docs/                     # ⭐ توثيق شامل (119 ملف)
│   ├── تعليمية/                # دروس تعليمية
│   │   ├── ar/                # 21 ملف عربي
│   │   └── en/                # 21 ملف إنجليزي
│   ├── DEVELOPER_GUIDE.md      # دليل المطورين
│   ├── ARCHITECTURE.md         # البنية المعمارية
│   ├── LLM_SYSTEM_PROMPT.txt   # Prompt للنماذج اللغوية
│   └── ...
│
├── 📁 gfx/                      # نظام الرسوم
│   ├── svg.bayan               # رسوم SVG
│   ├── waves.bayan             # موجات
│   └── img.bayan               # صور نقطية
│
├── 📄 README.md                 # الدليل الرئيسي (808 سطر)
├── 📄 README_AR.md              # الدليل العربي
├── 📄 CHANGELOG.md              # سجل التغييرات
├── 📄 QUICKSTART.md             # البدء السريع
├── 📄 START_HERE.md             # ابدأ من هنا
└── 📄 NEXT_MODEL_BRIEFING.md    # موجز للنماذج اللغوية
```

---

## 🔧 المكونات الأساسية

### 1. المحلل اللغوي (Lexer)

**الملف**: `bayan/bayan/lexer.py`

**الوظيفة**: تحويل النص المصدري إلى رموز (Tokens)

**العملية**:
```
كود مصدري → Lexer → قائمة رموز (Tokens)
```

**مثال**:
```bayan
x = 10 + 5
```
↓
```python
[
    Token(IDENTIFIER, 'x'),
    Token(ASSIGN, '='),
    Token(NUMBER, 10),
    Token(PLUS, '+'),
    Token(NUMBER, 5)
]
```

**الميزات**:
- دعم الكلمات المفتاحية العربية والإنجليزية
- معالجة النصوص العربية بشكل صحيح
- التعامل مع التعليقات (#)
- معالجة السلاسل النصية (strings)

---

### 2. المحلل النحوي (Parser)

**الملف**: `bayan/bayan/parser.py` (144,651 سطر!)

**الوظيفة**: تحويل الرموز إلى شجرة بناء جملة مجردة (AST)

**العملية**:
```
قائمة رموز → Parser → شجرة AST
```

**مثال**:
```bayan
if (x > 5) {
    print("كبير")
}
```
↓
```python
IfNode(
    condition=BinaryOpNode(left=x, op='>', right=5),
    then_block=[PrintNode("كبير")],
    else_block=None
)
```

**الميزات**:
- تحليل البرمجة الإجرائية (if, for, while)
- تحليل البرمجة الكائنية (class, def)
- تحليل البرمجة المنطقية (facts, rules, queries)
- معالجة الأخطاء النحوية

---

### 3. المفسر (Interpreter)

يوجد نوعان من المفسرات:

#### أ) المفسر التقليدي
**الملف**: `bayan/bayan/traditional_interpreter.py` (190,091 سطر!)

**الوظيفة**: تنفيذ الكود الإجرائي والكائني

**العملية**:
```
شجرة AST → Traditional Interpreter → نتائج
```

#### ب) المفسر الهجين
**الملف**: `bayan/bayan/hybrid_interpreter.py`

**الوظيفة**: تنفيذ الكود الهجين (إجرائي + كائني + منطقي)

**العملية**:
```
شجرة AST → Hybrid Interpreter → نتائج
              ↓
         Logical Engine (للمنطق)
```

---

### 4. المحرك المنطقي (Logical Engine)

**الملف**: `bayan/bayan/logical_engine.py`

**الوظيفة**: تنفيذ البرمجة المنطقية (Prolog-style)

**المكونات**:
- **قاعدة المعرفة** (Knowledge Base): تخزين الحقائق والقواعد
- **محرك التوحيد** (Unification Engine): مطابقة الأنماط
- **محرك التراجع** (Backtracking Engine): البحث عن الحلول

**مثال**:
```bayan
hybrid {
    # حقائق
    parent("أحمد", "محمد").
    parent("أحمد", "فاطمة").
    
    # قواعد
    sibling(?X, ?Y) :- parent(?P, ?X), parent(?P, ?Y), ?X != ?Y.
    
    # استعلام
    results = query sibling("محمد", ?S)?
}
```

**العملية**:
1. تخزين الحقائق في قاعدة المعرفة
2. تخزين القواعد
3. عند الاستعلام:
   - البحث عن مطابقات
   - تطبيق التوحيد
   - التراجع للبحث عن جميع الحلول

---

## 🔄 سير تدفق الأوامر

### المسار الكامل لتنفيذ كود بيان

```
┌─────────────────────────────────────────────────────────────┐
│ 1. ملف الكود المصدري (.by أو .bayan)                       │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. main.py - نقطة الدخول                                   │
│    - قراءة الملف                                           │
│    - تحديد نوع الكود (hybrid أو تقليدي)                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Lexer (lexer.py)                                        │
│    - تحليل النص إلى رموز (Tokens)                         │
│    - معالجة الكلمات المفتاحية العربية/الإنجليزية          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Parser (parser.py)                                      │
│    - بناء شجرة AST                                         │
│    - التحقق من الصحة النحوية                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
                    ┌─────┴─────┐
                    │           │
         ┌──────────▼──┐    ┌───▼──────────┐
         │ كود تقليدي  │    │ كود هجين     │
         └──────────┬──┘    └───┬──────────┘
                    │           │
    ┌───────────────▼───┐   ┌───▼──────────────────┐
    │ Traditional       │   │ Hybrid Interpreter   │
    │ Interpreter       │   │ + Logical Engine     │
    └───────────────┬───┘   └───┬──────────────────┘
                    │           │
                    └─────┬─────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. التنفيذ                                                  │
│    - تنفيذ العمليات الإجرائية                              │
│    - إنشاء الكائنات (OOP)                                  │
│    - تنفيذ الاستعلامات المنطقية                            │
│    - استدعاء الدوال المدمجة (builtins.py)                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. النتائج                                                  │
│    - طباعة المخرجات (print)                                │
│    - إرجاع القيم                                           │
│    - رسوم (SVG/Canvas)                                     │
└─────────────────────────────────────────────────────────────┘
```

### مثال تفصيلي

**الكود**:
```bayan
hybrid {
    x = 10
    print(x)
    
    parent("أحمد", "محمد").
    query parent(?P, "محمد")?
}
```

**التدفق**:

1. **Lexer**:
   ```python
   [HYBRID, LBRACE, IDENTIFIER('x'), ASSIGN, NUMBER(10), ...]
   ```

2. **Parser**:
   ```python
   HybridNode(
       statements=[
           AssignNode(var='x', value=10),
           PrintNode(var='x'),
           FactNode(predicate='parent', args=['أحمد', 'محمد']),
           QueryNode(...)
       ]
   )
   ```

3. **Hybrid Interpreter**:
   - تنفيذ `x = 10` → تخزين في البيئة
   - تنفيذ `print(x)` → طباعة 10
   - تنفيذ `parent(...)` → إضافة إلى قاعدة المعرفة
   - تنفيذ `query` → البحث في قاعدة المعرفة

4. **النتيجة**:
   ```
   10
   [{'?P': 'أحمد'}]
   ```

---

## 🧠 الأنظمة المتقدمة

### 1. نظام الدماغ المزدوج (Dual Brain)

**الملف**: `bayan/bayan/dual_brain.py`

**المكونات**:
- **الدماغ الأيسر** (`left_brain.py`): التحليل المنطقي
- **الدماغ الأيمن** (`right_brain.py`): التحليل الرياضي
- **طبقة التكامل** (`integration_layer.py`): التحقق المتبادل

**سير العمل**:
```
إدخال → الدماغ الأيسر (منطقي) → نتائج منطقية
      ↓
      → الدماغ الأيمن (رياضي) → نتائج رياضية
      ↓
      → طبقة التكامل → التحقق المتبادل → نتيجة نهائية
```

---

### 2. محرك GSE (Generalized Shape Equation)

**الملفات**: 
- `bayan/bayan/gse.py`
- `bayan/bayan/gse_fitting.py`
- `bayan/bayan/gse_visualization.py`

**الوظيفة**: تعلم تكيفي لتقريب الدوال من البيانات

**المعادلة**:
```
f(x) = Σ [Aᵢ / (1 + exp(-Bᵢ(x - Cᵢ)))]
```

**الاستخدام**:
```bayan
hybrid {
    x_data = [0, 1, 2, 3, 4, 5]
    y_data = [0, 1, 4, 9, 16, 25]
    
    model = learn("square", x_data, y_data)
    prediction = infer("square", 2.5)
}
```

---

### 3. نظام المعادلة الأم (Mother Equation)

**الملف**: `bayan/bayan/mother_equation.py`

**التمثيل**:
```
Object = (id, Φ, Ψ(t), Γ)
```
- **Φ**: خصائص ثابتة (عمر، مهنة، ...)
- **Ψ(t)**: حالات ديناميكية (سعادة، طاقة، ...) - قيم 0..1
- **Γ**: معادلات الشكل (GSE)

**الاستخدام**:
```bayan
hybrid {
    person = MotherEquation("P001", "سارة")
    person.add_property("العمر", 25, PropertyDomain("بيولوجي"))
    person.add_state("السعادة", 0.7)
}
```

---

### 4. المعادلات اللغوية (Linguistic Equations)

**الملف**: `bayan/bayan/linguistic_equation.py`

**الفلسفة**: الفكرة = (أشياء + حدث + نتيجة)

**التحويل**:
```
"محمد أكل تفاحة"
↓
محمد(فاعل) + تفاحة(مفعول_به) + أكل = 
  [محمد: جوع↓, طاقة↑] + [تفاحة: موجود=False]
```

---

### 5. محرك الكيانات (Entity Engine)

**الملف**: `bayan/bayan/entity_engine.py`

**الوظيفة**: نمذجة الكيانات الديناميكية مع حالات وأفعال

**الاستخدام**:
```bayan
hybrid {
    entity Ahmed { "states": {"hunger": 0.6} }
    entity John { "actions": {
        "feed": {"effects": [{"on": "hunger", "formula": "max(value - 0.4, 0)"}]}
    }}
    apply John.feed(Ahmed, action_value=1.0)
}
```

---

### 6. نظام الخبير-المستكشف (Expert-Explorer)

**الملف**: `bayan/bayan/expert_explorer.py`

**المكونات**:
- **الخبير**: معرفة مخزنة مع ثقة
- **المستكشف**: استكشاف خيارات جديدة

**الاستخدام**:
```bayan
hybrid {
    brain = BrainSystem(expert_weight=0.7, explorer_weight=0.3)
    brain.expert.add_knowledge("كيف أتعلم Python؟", "ابدأ بالأساسيات", 0.95)
    decision = brain.decide("كيف أتعلم البرمجة؟")
}
```

---

### 7. نظام اللغة التوليدية (GLM)

**الملفات**:
- `ai/lexicon.bayan` - المعجم
- `ai/morphology.bayan` - علم الصرف
- `ai/conceptual_surface_realizer.bayan` - المحقق

**المسار**:
```
مفهوم مجرد → Lexicon → كلمات
            ↓
         Morphology → تصريف
            ↓
    Surface Realizer → جملة متماسكة
```

---

## 📂 نظام الملفات

### أنواع الملفات

#### 1. ملفات البيان (.by, .bayan)
- `.by` - سكريبتات قابلة للتشغيل
- `.bayan` - مكتبات/وحدات

#### 2. ملفات Python (.py)
- الكود المصدري للمفسر
- الاختبارات
- الأدوات المساعدة

#### 3. ملفات Markdown (.md)
- التوثيق
- الأمثلة (مع كود بيان مضمن)
- الأدلة

---

### المجلدات الرئيسية

| المجلد | الوظيفة | عدد الملفات |
|--------|---------|-------------|
| `bayan/bayan/` | الكود المصدري للمفسر | 23 ملف |
| `ai/` | مكتبات الذكاء الاصطناعي | 21 ملف |
| `nlp_bayan/` | نظام NLP متقدم | 81 ملف |
| `examples/` | أمثلة عملية | 148 ملف |
| `tests/` | اختبارات | 162 ملف |
| `docs/` | توثيق | 119 ملف |
| `web_ide/` | محرر الويب | 12 ملف |

---

## 🛠️ دليل التطوير

### تشغيل كود بيان

#### الطريقة 1: من سطر الأوامر
```bash
python -m bayan examples/hello.by
```

#### الطريقة 2: الوضع التفاعلي
```bash
python -m bayan
>>> x = 10
>>> print(x)
10
```

#### الطريقة 3: محرر الويب
```bash
python web_ide/app.py
# افتح: http://127.0.0.1:5001/ide
```

---

### تشغيل الاختبارات

```bash
# جميع الاختبارات
python -m pytest tests/ -v

# اختبار محدد
python -m pytest tests/test_lexer.py -v

# مع تفاصيل
python -m pytest tests/ -v -s
```

---

### إضافة ميزة جديدة

#### 1. تعديل Lexer (إذا لزم الأمر)
```python
# bayan/bayan/lexer.py
KEYWORDS = {
    'new_keyword': 'NEW_KEYWORD',
    'كلمة_جديدة': 'NEW_KEYWORD',
}
```

#### 2. تعديل Parser
```python
# bayan/bayan/parser.py
def parse_new_feature(self):
    # منطق التحليل
    pass
```

#### 3. تعديل Interpreter
```python
# bayan/bayan/traditional_interpreter.py
def visit_NewFeatureNode(self, node):
    # منطق التنفيذ
    pass
```

#### 4. إضافة اختبارات
```python
# tests/test_new_feature.py
def test_new_feature():
    code = "new_keyword { ... }"
    result = run_bayan_code(code)
    assert result == expected
```

---

### إضافة دالة مدمجة

```python
# bayan/bayan/builtins.py

def bayan_new_function(arg1, arg2):
    """وصف الدالة"""
    # منطق الدالة
    return result

# إضافة إلى القاموس
BUILTIN_FUNCTIONS = {
    'new_function': bayan_new_function,
    'دالة_جديدة': bayan_new_function,  # النسخة العربية
}
```

---

## 📊 الإحصائيات التفصيلية

### الكود المصدري

| الملف | الأسطر | الوظيفة |
|-------|--------|---------|
| `parser.py` | 144,651 | المحلل النحوي |
| `traditional_interpreter.py` | 190,091 | المفسر التقليدي |
| `ast_nodes.py` | 55,395 | عقد AST |
| `hybrid_interpreter.py` | 43,948 | المفسر الهجين |
| `logical_engine.py` | 41,303 | المحرك المنطقي |
| `builtins.py` | 33,849 | الدوال المدمجة |
| `entity_engine.py` | 34,054 | محرك الكيانات |
| `lexer.py` | 30,177 | المحلل اللغوي |
| `visualization.py` | 28,445 | التصور البصري |

### مكتبات الذكاء الاصطناعي

| الملف | الأسطر | الوظيفة |
|-------|--------|---------|
| `ml.bayan` | 104,130 | التعلم الآلي |
| `nlp.bayan` | 68,623 | معالجة اللغة |
| `conceptual_programs.bayan` | 29,362 | البرامج المفاهيمية |
| `data.bayan` | 26,598 | هياكل البيانات |
| `conceptual_circuits.bayan` | 24,223 | الدوائر المفاهيمية |

### التوثيق

- **إجمالي ملفات التوثيق**: 119 ملف
- **الدروس التعليمية**: 42 ملف (21 عربي + 21 إنجليزي)
- **إجمالي أسطر التوثيق**: 9,318+ سطر

---

## 🎓 أمثلة الاستخدام

### مثال 1: برنامج بسيط

```bayan
hybrid {
    x = 10
    y = 20
    sum = x + y
    print("المجموع: " + str(sum))
}
```

### مثال 2: برمجة كائنية

```bayan
hybrid {
    class Person: {
        def __init__(self, name, age): {
            self.name = name
            self.age = age
        }
        
        def greet(self): {
            print("مرحباً، أنا " + self.name)
        }
    }
    
    person = Person("أحمد", 25)
    person.greet()
}
```

### مثال 3: برمجة منطقية

```bayan
hybrid {
    parent("أحمد", "محمد").
    parent("أحمد", "فاطمة").
    parent("علي", "سارة").
    
    sibling(?X, ?Y) :- parent(?P, ?X), parent(?P, ?Y), ?X != ?Y.
    
    results = query sibling("محمد", ?S)?
    
    for result in (results) {
        print("الأخ/الأخت: " + result["?S"])
    }
}
```

### مثال 4: هجين كامل

```bayan
hybrid {
    # OOP
    class Student: {
        def __init__(self, name, grade): {
            self.name = name
            self.grade = grade
            assertz(student(name, grade))
        }
    }
    
    # Imperative
    students = [
        Student("أحمد", 85),
        Student("فاطمة", 95),
        Student("علي", 92)
    ]
    
    # Logic
    results = query student(?N, ?G), ?G >= 90?
    
    print("الطلاب المتفوقون:")
    for result in (results) {
        print("  - " + result["?N"] + ": " + str(result["?G"]))
    }
}
```

---

## 🔗 روابط مفيدة

### التوثيق الأساسي
- [README.md](README.md) - الدليل الرئيسي
- [QUICKSTART.md](QUICKSTART.md) - البدء السريع
- [START_HERE.md](START_HERE.md) - ابدأ من هنا

### للمطورين
- [docs/DEVELOPER_GUIDE.md](docs/DEVELOPER_GUIDE.md) - دليل المطورين
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) - البنية المعمارية
- [FILE_INDEX.md](FILE_INDEX.md) - فهرس الملفات

### للنماذج اللغوية
- [docs/AI_MODEL_LINKS.md](docs/AI_MODEL_LINKS.md) - روابط مباشرة
- [docs/LLM_SYSTEM_PROMPT.txt](docs/LLM_SYSTEM_PROMPT.txt) - System Prompt
- [NEXT_MODEL_BRIEFING.md](NEXT_MODEL_BRIEFING.md) - موجز تنفيذي

---

## 📝 ملاحظات مهمة

### المتطلبات
- Python 3.8+
- pytest (للاختبارات)
- Pillow < 12.0 (للرسوم النقطية)

### قواعد البرمجة في بيان
- ✅ استخدم `{}` للكتل
- ✅ استخدم `:` بعد `def` و `class`
- ❌ لا تستخدم `;` في نهاية الأسطر
- ❌ لا تستخدم `**` (استخدم `pow()`)
- ❌ لا تستخدم list comprehensions

### الأولويات الحالية
- 🔴 إصلاح الاختبارات الفاشلة (الهدف: 95%+)
- ⚠️ تحسين توليد النص الطبيعي
- 📝 توسيع الأمثلة والتوثيق

---

## 🏆 الخلاصة

**لغة البيان** هي مشروع ضخم ومتقدم يقدم:

✅ **لغة برمجة هجينة فريدة** (3 أنماط في واحد)  
✅ **دعم كامل للعربية** (كلمات مفتاحية + معرّفات)  
✅ **7+ أنظمة ذكاء اصطناعي** متقدمة  
✅ **621 اختبار** شامل  
✅ **148 مثال** عملي  
✅ **9,318+ سطر** توثيق تعليمي  
✅ **محرر ويب** متكامل  
✅ **نظام رسوم** شامل  

**المشروع جاهز للاستخدام في التعليم والبحث!** 🚀

---

**تم إعداد هذا الدليل بتاريخ**: 2025-11-27  
**المطور**: باسل يحيى عبدالله  
**بمساعدة**: نماذج الذكاء الاصطناعي اللغوية
