# 🤖 كيف تشغل نماذج الذكاء الاصطناعي في بيان؟

## 🚀 البدء السريع (دقيقة واحدة)

### الطريقة الأسهل - الوضع المستقل (لا يحتاج إعداد):

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide1
python3 تجربة_الذكاء_الاصطناعي.py
```

**أو:**

```bash
python3 examples/neuro_symbolic_demo.py
```

---

## 📊 الأوضاع الثلاثة المتاحة

| الوضع | الحالة | المتطلبات |
|------|--------|-----------|
| 🟢 **المستقل** | ✅ يعمل الآن | لا شيء - جاهز! |
| 🟡 **المحلي (Ollama)** | ⚙️ يحتاج إعداد | Ollama + نموذج |
| 🔵 **السحابي (Gemini)** | ⚙️ يحتاج إعداد | مفتاح API |

---

## 🟢 الوضع 1: المستقل (يعمل الآن!)

### ✅ المميزات:
- **جاهز مباشرة** - لا يحتاج أي إعداد
- **خصوصية كاملة** - كل شيء على جهازك
- **سريع جداً** - لا انتظار

### 🎯 كيف تستخدمه:

#### الطريقة 1: السكريبت التفاعلي
```bash
python3 تجربة_الذكاء_الاصطناعي.py
```

#### الطريقة 2: كود Python مباشر
```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# إنشاء النظام
loop = NeuroSymbolicLoop(mode="standalone")

# معالجة نص عربي
result = loop.process("أحمد يدرس البرمجة", language="arabic")

# عرض النتيجة
print(result['realization'])
```

#### الطريقة 3: المثال الرسمي
```bash
python3 examples/neuro_symbolic_demo.py
```

---

## 🟡 الوضع 2: المحلي (Ollama) - للقوة الإضافية

### 📦 الإعداد (5 دقائق):

```bash
# 1. ثبّت Ollama
curl -fsSL https://ollama.com/install.sh | sh

# 2. شغّل الخادم (في نافذة منفصلة)
ollama serve

# 3. حمّل نموذج
ollama pull qwen2.5-coder:7b

# 4. ثبّت المكتبة
pip install ollama
```

### 🎯 كيف تستخدمه:

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# إنشاء النظام في الوضع المحلي
loop = NeuroSymbolicLoop(mode="local")

# معالجة نص
result = loop.process("الطالب المجتهد ينجح", language="arabic")
print(result['realization'])
```

---

## 🔵 الوضع 3: السحابي (Gemini) - للقوة القصوى

### 📦 الإعداد (3 دقائق):

```bash
# 1. ثبّت المكتبة
pip install google-generativeai

# 2. احصل على مفتاح API من:
# https://makersuite.google.com/app/apikey

# 3. عيّن المفتاح
export GEMINI_API_KEY="AIzaSy..."
```

### 🎯 كيف تستخدمه:

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# إنشاء النظام في الوضع السحابي
loop = NeuroSymbolicLoop(mode="cloud")

# معالجة نص
result = loop.process("محمد يأكل تفاحة", language="arabic")
print(result['realization'])
```

---

## 📚 أمثلة عملية

### مثال 1: معالجة نص واحد

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

loop = NeuroSymbolicLoop(mode="standalone")
result = loop.process("أحمد ضرب الكرة", language="arabic")

print("النص الأصلي:", result['input'])
print("الذرات:", result['dream'])
print("النتيجة:", result['realization'])
```

### مثال 2: معالجة نصوص متعددة

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

loop = NeuroSymbolicLoop(mode="standalone")

texts = [
    "محمد يأكل تفاحة",
    "الطالب يدرس الرياضيات",
    "السيارة تسير بسرعة"
]

for text in texts:
    result = loop.process(text, language="arabic")
    print(f"{text} → {result['realization']}")
```

### مثال 3: استخدام LLM Gateway مباشرة

```python
from bayan.ai.llm_gateway import LLMGateway

gateway = LLMGateway(mode="standalone")

# توليد نص
response = gateway.generate("اشرح لي البرمجة")
print(response)
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Ollama not running"
**الحل:**
```bash
ollama serve
```

### المشكلة: "GEMINI_API_KEY not found"
**الحل:**
```bash
export GEMINI_API_KEY="AIzaSy..."
```

### المشكلة: "google-generativeai not installed"
**الحل:**
```bash
pip install google-generativeai
```

---

## 📖 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `تجربة_الذكاء_الاصطناعي.py` | سكريبت تجريبي تفاعلي |
| `دليل_تشغيل_الذكاء_الاصطناعي.md` | دليل شامل مفصل |
| `examples/neuro_symbolic_demo.py` | المثال الرسمي |
| `bayan/ai/llm_gateway.py` | كود واجهة النماذج |
| `bayan/ai/neuro_symbolic_loop.py` | كود حلقة الدمج |

---

## 🎯 أي وضع تختار؟

### اختر **المستقل** إذا:
- ✅ تريد البدء فوراً (لا إعداد)
- ✅ تريد خصوصية كاملة
- ✅ تعمل على بيانات حساسة

### اختر **المحلي** إذا:
- ✅ تريد قوة أكبر
- ✅ لديك GPU قوي
- ✅ تريد نماذج مفتوحة المصدر

### اختر **السحابي** إذا:
- ✅ تريد أفضل أداء
- ✅ تعمل على نصوص معقدة
- ✅ لديك اتصال إنترنت جيد

---

## ✅ قائمة التحقق السريعة

- [ ] جربت الوضع المستقل: `python3 تجربة_الذكاء_الاصطناعي.py`
- [ ] قرأت الدليل الشامل: `دليل_تشغيل_الذكاء_الاصطناعي.md`
- [ ] جربت المثال الرسمي: `python3 examples/neuro_symbolic_demo.py`
- [ ] فهمت الفرق بين الأوضاع الثلاثة

---

## 🚀 ابدأ الآن!

```bash
# الطريقة الأسرع:
python3 تجربة_الذكاء_الاصطناعي.py
```

**بالتوفيق! 🎉**

---

**آخر تحديث**: 2025-12-05  
**المؤلف**: مشروع لغة بيان
