# 🚀 دليل تشغيل نظام بصيرة الثوري

## 📋 المتطلبات الأساسية

### 1. تثبيت المكتبات المطلوبة
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide145/new_baserah_ai-main
pip install -r requirements.txt
```

أو تثبيت يدوي:
```bash
pip install numpy matplotlib requests
```

---

## 🎮 طرق التشغيل

### 1. **الواجهة الرئيسية** - `basera_main.py` ⭐

**الطريقة الأساسية للتشغيل**:
```bash
python3 basera_main.py
```

**الميزات**:
- ✅ واجهة تفاعلية في سطر الأوامر
- ✅ يدمج جميع مكونات النظام
- ✅ اختبار شامل للنظام

**الأوامر المتاحة**:
```
🧬 بصيرة> help          # عرض المساعدة
🧬 بصيرة> status        # حالة النظام
🧬 بصيرة> test          # اختبار شامل
🧬 بصيرة> search <query> # البحث في المعرفة
🧬 بصيرة> exit          # الخروج
```

---

### 2. **الاختبار السريع** - `tools/quick_test.py` 🧪

**التشغيل**:
```bash
python3 tools/quick_test.py
```

**الميزات**:
- ✅ اختبار سريع لجميع المكونات
- ✅ تقرير مفصل عن حالة النظام
- ✅ مفيد للتحقق من أن كل شيء يعمل

---

### 3. **نظام تغذية المعرفة** - `knowledge/knowledge_feeding_interface.py` 📚

#### واجهة سطر الأوامر (CLI):
```bash
python3 knowledge/knowledge_feeding_interface.py --interface cli
```

**الأوامر**:
```
📚> file <path> [category]     # معالجة ملف واحد
📚> dir <path> [category]      # معالجة مجلد
📚> stats                      # عرض الإحصائيات
📚> history                    # عرض التاريخ
📚> help                       # المساعدة
📚> exit                       # الخروج
```

#### واجهة Gradio (ويب):
```bash
python3 knowledge/knowledge_feeding_interface.py --interface gradio
```

**الميزات**:
- 🌐 واجهة ويب تفاعلية
- 📁 رفع الملفات بسهولة
- 📊 عرض الإحصائيات
- 🎯 تصنيف المعرفة

**الوصول**: سيفتح على `http://localhost:7862`

---

### 4. **النسخ المتقدمة** - مجلد `variants/` 🔬

#### أ. النسخة المتقدمة:
```bash
python3 variants/basera_advanced.py
```

#### ب. النسخة الإبداعية:
```bash
python3 variants/basera_creative_advanced.py
```

#### ج. النسخة العلمية:
```bash
python3 variants/basera_scientific_advanced.py
```

#### د. النسخة التطبيقية:
```bash
python3 variants/basera_applications_advanced.py
```

#### هـ. النسخة النهائية:
```bash
python3 variants/basera_ultimate.py
```

---

### 5. **الأنظمة المتخصصة** - مجلد `systems/` ⚙️

```bash
# نظام توليد الصور
python3 systems/نظام_توليد_الصور_الثوري.py

# نظام توليد الإجابات
python3 systems/نظام_توليد_الإجابات_الذكية.py

# نظام التعلم المستمر
python3 systems/نظام_التعلم_المستمر_الثوري.py

# نظام التقييم الذاتي
python3 systems/نظام_التقييم_الذاتي_الثوري.py
```

---

## 🎯 الواجهات المتاحة

### 1. **واجهة سطر الأوامر (CLI)** 💻
- الأكثر استخداماً
- سريعة ومباشرة
- لا تحتاج تبعيات إضافية

### 2. **واجهة Gradio (ويب)** 🌐
- واجهة رسومية جميلة
- سهلة الاستخدام
- تحتاج تثبيت: `pip install gradio`

### 3. **واجهة برمجية (API)** 🔌
يمكنك استيراد المكونات واستخدامها في كودك:

```python
# استخدام نظام المعرفة
from knowledge.revolutionary_knowledge_system import RevolutionaryKnowledgeSystem

system = RevolutionaryKnowledgeSystem()
results = system.search_unified_knowledge("الذكاء الاصطناعي")

# استخدام المعادلة الأم
from core.revolutionary_mother_equation import RevolutionaryMotherEquation

equation = RevolutionaryMotherEquation()
result = equation.apply_zero_duality_theory(data)
```

---

## 🚀 البدء السريع

### الخطوة 1: التثبيت
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide145/new_baserah_ai-main
pip install -r requirements.txt
```

### الخطوة 2: الاختبار
```bash
python3 tools/quick_test.py
```

### الخطوة 3: التشغيل
```bash
python3 basera_main.py
```

---

## 💡 أمثلة الاستخدام

### مثال 1: تشغيل النظام الرئيسي
```bash
python3 basera_main.py
```

### مثال 2: تغذية المعرفة من ملف
```bash
python3 knowledge/knowledge_feeding_interface.py --file data.json --category scientific
```

### مثال 3: تغذية المعرفة من مجلد
```bash
python3 knowledge/knowledge_feeding_interface.py --directory /path/to/knowledge --category general
```

### مثال 4: واجهة ويب
```bash
python3 knowledge/knowledge_feeding_interface.py --interface gradio
# ثم افتح المتصفح على: http://localhost:7862
```

---

## 📊 فئات المعرفة المتاحة

عند تغذية النظام بالمعرفة، يمكنك اختيار الفئة:

- `mathematical` - معرفة رياضية
- `scientific` - معرفة علمية
- `linguistic` - معرفة لغوية
- `historical` - معرفة تاريخية
- `technical` - معرفة تقنية
- `philosophical` - معرفة فلسفية
- `cultural` - معرفة ثقافية
- `general` - معرفة عامة (افتراضي)

---

## 🔧 استكشاف الأخطاء

### مشكلة: ModuleNotFoundError
```bash
# الحل: تثبيت المكتبات
pip install numpy matplotlib requests
```

### مشكلة: Gradio غير متوفر
```bash
# الحل: تثبيت Gradio (اختياري)
pip install gradio
```

### مشكلة: أخطاء الاستيراد
```bash
# تأكد من أنك في المجلد الصحيح
cd /home/al-mubtakir/Documents/bayan_python_ide145/new_baserah_ai-main
```

---

## 🎉 الخلاصة

**أسهل طريقة للبدء**:
```bash
python3 basera_main.py
```

**للاختبار**:
```bash
python3 tools/quick_test.py
```

**لواجهة ويب**:
```bash
pip install gradio
python3 knowledge/knowledge_feeding_interface.py --interface gradio
```
