# كيفية استخدام لغة البيان مع النماذج اللغوية
# How to Use Bayan Language with LLMs

<div dir="rtl">

## 📋 نظرة عامة

هذا الدليل يشرح كيفية تعليم النماذج اللغوية (مثل ChatGPT، Claude، Gemini، وغيرها) كتابة كود بلغة البيان بشكل صحيح.

**آخر تحديث**: 2025-12-03

**حالة المشروع**:
- ✅ 74/74 ملف مثال يعمل بنجاح (100%) 🎉
- ✅ 42 ملف تعليمي (21 عربي + 21 إنجليزي)
- ✅ 9,318+ سطر توثيق
- ✅ نظام Conceptual LM كامل
- ✅ نظام NLP حواري متقدم
- ✅ الشبكات السببية، نظام الكيانات، الشبكات الدلالية
- ✅ نظام سيميائية الحروف الموحد (28 حرف عربي + 26 حرف إنجليزي)
- ✅ نظام استنباط المعاني الذكي (4 محركات: شكلي، صوتي، اسمي، معجمي) 🧠
- ✅ مولد الكلمات من المعاني (معنى → كلمة)

---

## 🎯 المشكلة

عندما تطلب من نموذج لغوي كتابة كود بلغة البيان، سيجيب:
> "آسف، لا أعرف لغة البيان. هل تقصد Python أو لغة أخرى؟"

**السبب**: النماذج اللغوية لم تُدرَّب على لغة البيان لأنها لغة جديدة (2025).

---

## ✅ الحل

قمنا بإنشاء **5 ملفات مرجعية** يمكنك استخدامها لتعليم النماذج اللغوية:

### 1. **AI_MODEL_LINKS.md** 🌟 (للوصول الكامل للمشروع)
- **الاستخدام**: روابط مباشرة لجميع ملفات المشروع (632+ ملف)
- **الحجم**: منظم حسب الفئة
- **متى تستخدمه**: عندما تريد أن يطلع النموذج على المشروع بالكامل
- **الروابط**:
  - RAW: https://raw.githubusercontent.com/mubtakir/nlp_bayan/main/docs/AI_MODEL_LINKS.md
  - GitHub plain: https://github.com/mubtakir/nlp_bayan/blob/main/docs/AI_MODEL_LINKS.md?plain=1

### 2. **LLM_SYSTEM_PROMPT.txt** ⭐ (الأهم للبداية السريعة)
- **الاستخدام**: انسخ محتواه والصقه في بداية المحادثة
- **الحجم**: مختصر وشامل
- **متى تستخدمه**: دائماً! هذا هو الملف الأساسي للبداية السريعة

### 3. **LLM_QUICK_REFERENCE.md**
- **الاستخدام**: مرجع سريع للقواعد الأساسية والكلمات المفتاحية
- **الحجم**: متوسط
- **متى تستخدمه**: عندما تحتاج تذكير سريع

### 4. **LLM_REFERENCE_GUIDE.md**
- **الاستخدام**: دليل شامل مع أمثلة متقدمة
- **الحجم**: كبير (1200+ سطر)
- **متى تستخدمه**: للمشاريع المعقدة أو التعلم العميق

### 5. **HOW_TO_USE_WITH_LLMS.md** (هذا الملف)
- **الاستخدام**: دليل الاستخدام
- **متى تستخدمه**: للمرة الأولى

---

## 🚀 طريقة الاستخدام

### الطريقة 1: استخدام System Prompt (موصى بها)

#### الخطوة 1: افتح ملف `LLM_SYSTEM_PROMPT.txt`
```bash
cat docs/LLM_SYSTEM_PROMPT.txt
```

#### الخطوة 2: انسخ المحتوى كاملاً

#### الخطوة 3: الصقه في بداية محادثتك مع النموذج اللغوي

**مثال مع ChatGPT:**
```
[الصق محتوى LLM_SYSTEM_PROMPT.txt هنا]

الآن اكتب لي برنامج بلغة البيان لحساب مجموع قائمة من الأرقام.
```

**مثال مع Claude:**
```
[الصق محتوى LLM_SYSTEM_PROMPT.txt هنا]

أريد كود بلغة البيان لإنشاء صنف Student مع طرق لحساب المعدل.
```

---

### الطريقة 2: استخدام Custom Instructions (ChatGPT)

إذا كنت تستخدم ChatGPT Plus:

#### الخطوة 1: اذهب إلى Settings → Custom Instructions

#### الخطوة 2: في قسم "How would you like ChatGPT to respond?"
الصق محتوى `LLM_SYSTEM_PROMPT.txt`

#### الخطوة 3: احفظ
الآن ChatGPT سيعرف لغة البيان في جميع محادثاتك!

---

### الطريقة 3: استخدام Projects (Claude)

إذا كنت تستخدم Claude Pro:

#### الخطوة 1: أنشئ Project جديد باسم "Bayan Language"

#### الخطوة 2: أضف الملفات التالية للـ Project Knowledge:
- `LLM_SYSTEM_PROMPT.txt`
- `LLM_QUICK_REFERENCE.md`
- `LLM_REFERENCE_GUIDE.md`

#### الخطوة 3: ابدأ محادثة في هذا الـ Project
Claude سيستخدم هذه الملفات تلقائياً!

---

### الطريقة 4: استخدام مع API

إذا كنت تستخدم API (OpenAI، Anthropic، إلخ):

```python
import openai

# اقرأ System Prompt
with open('docs/LLM_SYSTEM_PROMPT.txt', 'r', encoding='utf-8') as f:
    system_prompt = f.read()

# استخدمه في المحادثة
response = openai.ChatCompletion.create(
    model="gpt-4",
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": "اكتب برنامج بلغة البيان لحساب Fibonacci"}
    ]
)

print(response.choices[0].message.content)
```

---

## 📝 أمثلة على الطلبات

### مثال 1: برنامج بسيط
**الطلب:**
```
اكتب برنامج بلغة البيان لحساب مجموع الأرقام من 1 إلى 100
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    total = 0
    for i in (range(1, 101)) {
        total = total + i
    }
    print("Sum: " + str(total))
}
```

---

### مثال 2: برمجة كائنية
**الطلب:**
```
أنشئ صنف BankAccount بلغة البيان مع طرق deposit و withdraw
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    class BankAccount: {
        def __init__(self, owner, balance): {
            self.owner = owner
            self.balance = balance
        }
        
        def deposit(self, amount): {
            self.balance = self.balance + amount
            return self.balance
        }
        
        def withdraw(self, amount): {
            if (amount > self.balance) {
                print("Insufficient funds")
                return self.balance
            }
            self.balance = self.balance - amount
            return self.balance
        }
    }
    
    account = BankAccount("أحمد", 1000)
    account.deposit(500)
    account.withdraw(200)
    print("Balance: " + str(account.balance))
}
```

---

### مثال 3: برمجة منطقية
**الطلب:**
```
اكتب نظام خبير بسيط بلغة البيان لتشخيص الأمراض بناءً على الأعراض
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    # الأعراض
    symptom("patient1", "fever").
    symptom("patient1", "cough").
    
    # قواعد التشخيص
    diagnosis(?P, "flu") :- symptom(?P, "fever"), symptom(?P, "cough").
    
    # استعلام
    results = query diagnosis("patient1", ?D)?
    
    for result in (results) {
        print("Diagnosis: " + result["?D"])
    }
}
```

---

### مثال 4: برمجة هجينة
**الطلب:**
```
اكتب برنامج بلغة البيان يجمع بين OOP والبرمجة المنطقية لإدارة الطلاب
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    class Student: {
        def __init__(self, name, grade): {
            self.name = name
            self.grade = grade
            assertz(student(name, grade))
        }
    }
    
    s1 = Student("أحمد", 85)
    s2 = Student("فاطمة", 95)
    
    results = query student(?N, ?G), ?G >= 90?
    
    for result in (results) {
        print("Excellent student: " + result["?N"])
    }
}
```

---

### مثال 5: الشبكات السببية (جديد!)
**الطلب:**
```
اكتب برنامج بلغة البيان لإنشاء شبكة سببية للصحة
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    create_network("health", "Health Network", "custom")
    add_node("health", "exercise", "behavior", "سلوك")
    add_node("health", "fitness", "state", "حالة")
    add_node("health", "happiness", "state", "حالة")

    add_causal_relation("health", "exercise", "fitness", "leads_to", "0.9")
    add_causal_relation("health", "fitness", "happiness", "causes", "0.7")

    chain = infer_causal_chain("health", "exercise", "happiness", "5")
    print("Causal chain: " + str(chain))
}
```

---

### مثال 6: نظام الكيانات الديناميكي (جديد!)
**الطلب:**
```
اكتب برنامج بلغة البيان لمحاكاة طالب يدرس
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    entity Student: {
        states: {"hunger": 0.6, "energy": 0.8, "knowledge": 0.3},
        actions: {
            "study": {
                formula: "value * (1 - states['hunger']) * states['energy']",
                effects: {"knowledge": "+0.3", "energy": "-0.2"}
            },
            "eat": {
                formula: "value",
                effects: {"hunger": "-0.5", "energy": "+0.1"}
            }
        }
    }

    ahmad = Student("Ahmad")
    perform("study", ["Ahmad.1.0"], action_value=1.0)
    perform("eat", ["Ahmad.1.0"], action_value=1.0)

    print("Final states: " + str(ahmad.states))
}
```

---

### مثال 7: نظام NLP الحواري (جديد!)
**الطلب:**
```
اكتب برنامج بلغة البيان لنظام حوار ذكي
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    add_fact("القط", "هو", "حيوان")
    add_fact("القط", "له", "ذيل")
    add_fact("cat = قط")  # مرادف ثنائي الاتجاه

    answer1 = ask("ما هو القط؟")
    print("Answer: " + answer1)

    answer2 = ask("ما الذي له ذيل؟")
    print("Answer: " + answer2)

    # استدلال مع سياسة
    answer3 = get_answer_with_policy("ما هو القط؟", "most_recent")
    print("Answer with policy: " + answer3)
}
```

---

### مثال 8: Conceptual LM (جديد!)
**الطلب:**
```
اكتب برنامج بلغة البيان لتوليد نص من آثار تصورية
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    # تشغيل برنامج معنوي
    result = run_meaning_program(
        "student_study",
        {"detail_level": "high", "focus": "causal"}
    )

    # توليد نص طبيعي
    text = realize_surface(result["trace"], "ar", "high")
    print("Generated text: " + text)
}
```

---

## 📊 إحصائيات المشروع

- **الأمثلة**: 72/72 ملف يعمل بنجاح (100%) 🎉
- **التوثيق التعليمي**: 42 ملف (21 عربي + 21 إنجليزي)
- **التوثيق الكلي**: 9,318+ سطر
- **الميزات الرئيسية**:
  - ✅ البرمجة الهجينة (Imperative + OOP + Logic)
  - ✅ الشبكات السببية
  - ✅ نظام الكيانات الديناميكي
  - ✅ الشبكات الدلالية
  - ✅ المترادفات والتشابه
  - ✅ الاستدلال الاحتمالي
  - ✅ نظام NLP الحواري
  - ✅ Conceptual LM (4 طبقات، 6 دوائر، 5 برامج)

---

## ⚠️ الأخطاء الشائعة وكيفية تصحيحها

### خطأ 1: النموذج ينسى `hybrid { }`
**المشكلة:**
```bayan
x = 10  # خطأ: لا يوجد hybrid
```

**الحل:**
ذكّر النموذج:
```
تذكر: كل كود البيان يجب أن يكون داخل hybrid { }
```

---

### خطأ 2: النموذج ينسى `:` قبل `{`
**المشكلة:**
```bayan
hybrid {
    def f(x) {  # خطأ: لا يوجد :
        return x
    }
}
```

**الحل:**
ذكّر النموذج:
```
تذكر: استخدم : قبل { في الدوال والأصناف والشروط
```

---

### خطأ 3: النموذج ينسى `.` في نهاية الحقائق
**المشكلة:**
```bayan
hybrid {
    parent("a", "b")  # خطأ: لا يوجد .
}
```

**الحل:**
ذكّر النموذج:
```
تذكر: الحقائق والقواعد المنطقية يجب أن تنتهي بـ .
```

---

### خطأ 4: النموذج يستخدم print مع معاملات متعددة
**المشكلة:**
```bayan
hybrid {
    print("Value:", x)  # خطأ
}
```

**الحل:**
ذكّر النموذج:
```
تذكر: استخدم + لدمج النصوص بدلاً من الفواصل
الصحيح: print("Value: " + str(x))
```

---

## 🎯 نصائح للحصول على أفضل النتائج

### 1. كن محدداً في طلبك
❌ **سيء**: "اكتب برنامج"
✅ **جيد**: "اكتب برنامج بلغة البيان لحساب المعدل لقائمة من الدرجات"

### 2. حدد النمط البرمجي
❌ **سيء**: "اكتب كود"
✅ **جيد**: "اكتب كود بلغة البيان باستخدام البرمجة الكائنية"

### 3. اطلب أمثلة
```
اكتب برنامج بلغة البيان لإدارة مكتبة مع أمثلة على الاستخدام
```

### 4. اطلب شرح
```
اكتب برنامج بلغة البيان واشرح كل جزء
```

### 5. صحح الأخطاء بلطف
```
الكود جيد لكن نسيت : قبل { في السطر 5. هل يمكنك تصحيحه؟
```

---

## 📊 مقارنة الملفات المرجعية

| الملف | الحجم | الاستخدام | متى تستخدمه |
|-------|-------|-----------|-------------|
| LLM_SYSTEM_PROMPT.txt | صغير | System Prompt | دائماً (الأساسي) |
| LLM_QUICK_REFERENCE.md | متوسط | مرجع سريع | للتذكير السريع |
| LLM_REFERENCE_GUIDE.md | كبير | دليل شامل | للمشاريع المعقدة |
| HOW_TO_USE_WITH_LLMS.md | متوسط | دليل الاستخدام | للمرة الأولى |

---

## 🔧 استكشاف الأخطاء

### المشكلة: النموذج لا يزال لا يفهم لغة البيان
**الحل:**
1. تأكد من نسخ `LLM_SYSTEM_PROMPT.txt` كاملاً
2. الصقه في **بداية** المحادثة
3. جرب نموذج أحدث (GPT-4، Claude 3، إلخ)

### المشكلة: النموذج يكتب كود Python بدلاً من البيان
**الحل:**
ذكّره صراحة:
```
أريد الكود بلغة البيان، ليس Python. تذكر استخدام hybrid { }
```

### المشكلة: الكود لا يعمل
**الحل:**
1. تحقق من القواعد الأساسية (hybrid, :, {}, .)
2. اطلب من النموذج مراجعة الكود
3. استخدم `LLM_REFERENCE_GUIDE.md` للأمثلة الصحيحة

---

## 🌟 أمثلة متقدمة

### مثال: طلب نظام كامل
```
اكتب نظام إدارة طلاب بلغة البيان يجمع بين:
1. البرمجة الكائنية (صنف Student)
2. البرمجة المنطقية (قواعد للتقييم)
3. معالجة البيانات (حساب الإحصائيات)

مع أمثلة على الاستخدام
```

### مثال: طلب تحسين كود
```
هذا كود بلغة البيان:
[الصق الكود هنا]

هل يمكنك:
1. تحسينه
2. إضافة معالجة للأخطاء
3. إضافة تعليقات
```

### مثال: طلب شرح
```
اشرح هذا الكود بلغة البيان سطراً بسطر:
[الصق الكود هنا]
```

---

### مثال 5: شبكة سببية (NEW! 🎯)
**الطلب:**
```
اكتب برنامج بلغة البيان يبني شبكة سببية لفهم العلاقة بين التمارين الرياضية والنظام الغذائي والصحة
```

**النتيجة المتوقعة:**
```bayan
hybrid {
    # إنشاء شبكة صحية
    create_network("health", "الصحة والعافية", "custom")

    # إضافة عقد
    add_node("health", "تمارين", "behavior", "سلوك")
    add_node("health", "نظام_غذائي_صحي", "behavior", "سلوك")
    add_node("health", "لياقة", "state", "حالة")
    add_node("health", "طاقة", "state", "حالة")
    add_node("health", "صحة_جيدة", "state", "حالة")
    add_node("health", "سعادة", "emotion", "عاطفة")

    # إضافة علاقات سببية
    add_causal_relation("health", "تمارين", "لياقة", "leads_to", "0.9")
    add_causal_relation("health", "نظام_غذائي_صحي", "طاقة", "causes", "0.85")
    add_causal_relation("health", "لياقة", "صحة_جيدة", "enhances", "0.9")
    add_causal_relation("health", "طاقة", "صحة_جيدة", "enhances", "0.8")
    add_causal_relation("health", "صحة_جيدة", "سعادة", "leads_to", "0.85")

    # استدلال سببي
    print("=== كيف تؤدي التمارين إلى السعادة؟ ===")
    infer_causal_chain("health", "تمارين", "سعادة", "5")

    # إيجاد جميع أسباب الصحة الجيدة
    print("\n=== ما الذي يؤدي إلى صحة جيدة؟ ===")
    find_all_causes("health", "صحة_جيدة", "3")
}
```

---

## 📚 موارد إضافية

### للمبتدئين:
- ابدأ بـ `LLM_SYSTEM_PROMPT.txt`
- اطلب أمثلة بسيطة
- تدرج في التعقيد

### للمتقدمين:
- استخدم `LLM_REFERENCE_GUIDE.md`
- اطلب أنظمة كاملة
- ادمج الأنماط الثلاثة

### للمطورين:
- استخدم API مع System Prompt
- أنشئ templates جاهزة
- شارك أمثلتك

---

## ✅ Checklist للتأكد من النجاح

قبل أن تطلب من النموذج كتابة كود:
- [ ] نسخت `LLM_SYSTEM_PROMPT.txt`
- [ ] لصقته في بداية المحادثة
- [ ] حددت النمط البرمجي المطلوب
- [ ] كنت محدداً في طلبك

بعد أن يكتب النموذج الكود:
- [ ] الكود داخل `hybrid { }`
- [ ] يوجد `:` قبل كل `{` في الدوال والأصناف
- [ ] الحقائق المنطقية تنتهي بـ `.`
- [ ] المتغيرات المنطقية تبدأ بـ `?`
- [ ] `print()` يستخدم `+` لدمج النصوص
- [ ] إذا استخدم الشبكات السببية: استخدم الأوامر الصحيحة (NEW! 🎯)

---

## 🎉 الخلاصة

الآن يمكنك استخدام أي نموذج لغوي لكتابة كود بلغة البيان!

**الخطوات:**
1. انسخ `LLM_SYSTEM_PROMPT.txt`
2. الصقه في بداية المحادثة
3. اطلب ما تريد
4. استمتع بالكود!

**بالتوفيق! 🚀**

</div>

