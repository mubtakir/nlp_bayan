# اختبارات للنماذج اللغوية - لغة البيان
# LLM Test Prompts - Bayan Language

<div dir="rtl">

## 🎯 الغرض

هذا الملف يحتوي على **اختبارات جاهزة** يمكنك استخدامها لاختبار قدرة النموذج اللغوي على كتابة كود بلغة البيان بشكل صحيح.

---

## 📋 كيفية الاستخدام

### الخطوة 1: أضف System Prompt
انسخ محتوى `LLM_SYSTEM_PROMPT.txt` والصقه في بداية المحادثة.

### الخطوة 2: اختر اختبار
اختر أحد الاختبارات أدناه.

### الخطوة 3: انسخ الطلب
انسخ الطلب والصقه في المحادثة.

### الخطوة 4: تحقق من النتيجة
قارن النتيجة مع الإجابة المتوقعة.

---

## 🧪 الاختبارات

### اختبار 1: Hello World (سهل جداً)
**المستوى**: مبتدئ  
**النمط**: إجرائي

**الطلب:**
```
اكتب برنامج بلغة البيان يطبع "مرحباً بالعالم"
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    print("مرحباً بالعالم")
}
```

**معايير النجاح:**
- ✅ الكود داخل `hybrid { }`
- ✅ استخدام `print()`
- ✅ النص العربي يعمل بشكل صحيح

---

### اختبار 2: حلقة For (سهل)
**المستوى**: مبتدئ  
**النمط**: إجرائي

**الطلب:**
```
اكتب برنامج بلغة البيان يطبع الأرقام من 1 إلى 10
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    for i in (range(1, 11)) {
        print(str(i))
    }
}
```

**معايير النجاح:**
- ✅ استخدام `:` قبل `{`
- ✅ استخدام `{ }` للحلقة
- ✅ استخدام `range(1, 11)`
- ✅ تحويل الرقم إلى نص بـ `str()`

---

### اختبار 3: دالة بسيطة (متوسط)
**المستوى**: متوسط  
**النمط**: إجرائي

**الطلب:**
```
اكتب دالة بلغة البيان تأخذ رقمين وترجع مجموعهما
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    def add(a, b): {
        return a + b
    }
    
    result = add(5, 3)
    print("Result: " + str(result))
}
```

**معايير النجاح:**
- ✅ استخدام `def` مع `:` قبل `{`
- ✅ استخدام `return`
- ✅ استدعاء الدالة بشكل صحيح

---

### اختبار 4: صنف بسيط (متوسط)
**المستوى**: متوسط  
**النمط**: كائني

**الطلب:**
```
اكتب صنف Person بلغة البيان مع خاصيتين (name, age) ودالة greet
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    class Person: {
        def __init__(self, name, age): {
            self.name = name
            self.age = age
        }
        
        def greet(self): {
            return "Hello, I am " + self.name
        }
    }
    
    person = Person("أحمد", 25)
    print(person.greet())
}
```

**معايير النجاح:**
- ✅ استخدام `class` مع `:` قبل `{`
- ✅ استخدام `__init__` و `self`
- ✅ إنشاء كائن بشكل صحيح

---

### اختبار 5: حقائق منطقية (متوسط)
**المستوى**: متوسط  
**النمط**: منطقي

**الطلب:**
```
اكتب برنامج بلغة البيان يحدد علاقات parent ثم يستعلم عن أطفال شخص معين
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    # حقائق
    parent("أحمد", "محمد").
    parent("أحمد", "فاطمة").
    parent("علي", "سارة").
    
    # استعلام
    results = query parent("أحمد", ?Child)?
    
    print("Children of أحمد:")
    for result in (results) {
        print(result["?Child"])
    }
}
```

**معايير النجاح:**
- ✅ الحقائق تنتهي بـ `.`
- ✅ المتغيرات المنطقية تبدأ بـ `?`
- ✅ استخدام `query` مع `?` في النهاية
- ✅ الوصول للنتائج بـ `result["?Child"]`

---

### اختبار 6: قاعدة منطقية (متقدم)
**المستوى**: متقدم  
**النمط**: منطقي

**الطلب:**
```
اكتب برنامج بلغة البيان يحدد علاقات parent ثم يضيف قاعدة grandparent ويستعلم عنها
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    # حقائق
    parent("إبراهيم", "أحمد").
    parent("أحمد", "محمد").
    
    # قاعدة
    grandparent(?GP, ?GC) :- parent(?GP, ?P), parent(?P, ?GC).
    
    # استعلام
    results = query grandparent(?GP, "محمد")?
    
    for result in (results) {
        print("Grandparent: " + result["?GP"])
    }
}
```

**معايير النجاح:**
- ✅ القاعدة تستخدم `:-`
- ✅ القاعدة تنتهي بـ `.`
- ✅ استخدام `,` للربط بين الشروط
- ✅ الاستعلام يعمل بشكل صحيح

---

### اختبار 7: *args (متقدم)
**المستوى**: متقدم  
**النمط**: إجرائي

**الطلب:**
```
اكتب دالة بلغة البيان تقبل عدد غير محدد من الأرقام وترجع مجموعها
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    def sum_all(*numbers): {
        total = 0
        for num in (numbers) {
            total = total + num
        }
        return total
    }
    
    result = sum_all(1, 2, 3, 4, 5)
    print("Sum: " + str(result))
}
```

**معايير النجاح:**
- ✅ استخدام `*numbers`
- ✅ التعامل مع `numbers` كقائمة
- ✅ الدالة تعمل مع أي عدد من المعاملات

---

### اختبار 8: **kwargs (متقدم)
**المستوى**: متقدم  
**النمط**: إجرائي

**الطلب:**
```
اكتب دالة بلغة البيان تقبل معاملات keyword وتطبعها
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    def print_info(**info): {
        for key in (info) {
            print(key + ": " + str(info[key]))
        }
    }
    
    print_info(name="أحمد", age=25, city="الرياض")
}
```

**معايير النجاح:**
- ✅ استخدام `**info`
- ✅ التعامل مع `info` كقاموس
- ✅ الدالة تعمل مع أي عدد من المعاملات

---

### اختبار 9: برمجة هجينة (متقدم جداً)
**المستوى**: متقدم جداً  
**النمط**: هجين (OOP + Logic)

**الطلب:**
```
اكتب برنامج بلغة البيان يجمع بين البرمجة الكائنية والمنطقية:
- صنف Student مع name و grade
- عند إنشاء طالب، أضفه لقاعدة المعرفة المنطقية
- استعلم عن الطلاب الذين درجاتهم >= 90
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    class Student: {
        def __init__(self, name, grade): {
            self.name = name
            self.grade = grade
            
            # إضافة لقاعدة المعرفة
            assertz(student(name, grade))
        }
    }
    
    # إنشاء طلاب
    s1 = Student("أحمد", 85)
    s2 = Student("فاطمة", 95)
    s3 = Student("علي", 92)
    
    # استعلام منطقي
    results = query student(?N, ?G), ?G >= 90?
    
    print("Excellent students:")
    for result in (results) {
        print(result["?N"])
    }
}
```

**معايير النجاح:**
- ✅ دمج OOP مع Logic بشكل صحيح
- ✅ استخدام `assertz()` داخل `__init__`
- ✅ الاستعلام يعمل بشكل صحيح
- ✅ استخدام شرط `?G >= 90` في الاستعلام

---

### اختبار 10: نظام خبير (احترافي)
**المستوى**: احترافي  
**النمط**: هجين (Imperative + Logic)

**الطلب:**
```
اكتب نظام خبير بسيط بلغة البيان لتشخيص الأمراض:
- حدد أعراض لمريضين
- أضف قواعد للتشخيص
- اكتب دالة diagnose_patient تأخذ اسم المريض وترجع التشخيص
```

**الإجابة المتوقعة:**
```bayan
hybrid {
    # أعراض المرضى
    symptom("patient1", "fever").
    symptom("patient1", "cough").
    
    symptom("patient2", "headache").
    symptom("patient2", "nausea").
    
    # قواعد التشخيص
    diagnosis(?P, "flu") :- symptom(?P, "fever"), symptom(?P, "cough").
    diagnosis(?P, "migraine") :- symptom(?P, "headache"), symptom(?P, "nausea").
    
    # دالة التشخيص
    def diagnose_patient(patient_id): {
        results = query diagnosis(patient_id, ?Disease)?
        
        if (len(results) > 0) {
            disease = results[0]["?Disease"]
            return disease
        }
        
        return "Unknown"
    }
    
    # اختبار
    diag1 = diagnose_patient("patient1")
    diag2 = diagnose_patient("patient2")
    
    print("Patient 1: " + diag1)
    print("Patient 2: " + diag2)
}
```

**معايير النجاح:**
- ✅ دمج الدوال مع البرمجة المنطقية
- ✅ قواعد التشخيص صحيحة
- ✅ الدالة تتعامل مع النتائج بشكل صحيح
- ✅ معالجة حالة عدم وجود تشخيص

---

## 📊 جدول الاختبارات

| # | الاختبار | المستوى | النمط | الميزات المختبرة |
|---|----------|---------|-------|------------------|
| 1 | Hello World | مبتدئ | إجرائي | hybrid, print |
| 2 | حلقة For | مبتدئ | إجرائي | for, range, : { } |
| 3 | دالة بسيطة | متوسط | إجرائي | def, return |
| 4 | صنف بسيط | متوسط | كائني | class, __init__, self |
| 5 | حقائق منطقية | متوسط | منطقي | facts, query, ?Variable |
| 6 | قاعدة منطقية | متقدم | منطقي | rules, :- |
| 7 | *args | متقدم | إجرائي | *args |
| 8 | **kwargs | متقدم | إجرائي | **kwargs |
| 9 | برمجة هجينة | متقدم جداً | هجين | OOP + Logic, assertz |
| 10 | نظام خبير | احترافي | هجين | Expert System |

---

## ✅ معايير النجاح العامة

عند اختبار أي نموذج لغوي، تأكد من:

### 1. القواعد الأساسية:
- [ ] الكود داخل `hybrid { }`
- [ ] استخدام `:` قبل `{` في def/class/if/for/while
- [ ] استخدام `{ }` لجميع الكتل
- [ ] الحقائق والقواعد تنتهي بـ `.`
- [ ] المتغيرات المنطقية تبدأ بـ `?`

### 2. الدوال:
- [ ] استخدام `def` بشكل صحيح
- [ ] استخدام `return` للإرجاع
- [ ] المعاملات الافتراضية تعمل
- [ ] *args و **kwargs تعمل

### 3. البرمجة الكائنية:
- [ ] استخدام `class` بشكل صحيح
- [ ] `__init__` و `self` موجودان
- [ ] الطرق تعمل بشكل صحيح
- [ ] إنشاء الكائنات يعمل

### 4. البرمجة المنطقية:
- [ ] الحقائق صحيحة
- [ ] القواعد تستخدم `:-`
- [ ] الاستعلامات تستخدم `query ... ?`
- [ ] الوصول للنتائج بـ `result["?Variable"]`

### 5. البرمجة الهجينة:
- [ ] دمج الأنماط يعمل بشكل صحيح
- [ ] `assertz()` و `retract()` تعمل
- [ ] التفاعل بين OOP و Logic صحيح

---

## 🎯 نصائح للاختبار

### 1. ابدأ بالسهل
ابدأ بالاختبار 1 وتدرج حتى الاختبار 10.

### 2. اختبر ميزة واحدة في كل مرة
لا تطلب كل الميزات دفعة واحدة.

### 3. صحح الأخطاء بلطف
إذا أخطأ النموذج، ذكّره بالقاعدة الصحيحة.

### 4. استخدم الأمثلة
إذا فشل النموذج، أعطه مثالاً من `LLM_REFERENCE_GUIDE.md`.

### 5. سجل النتائج
سجل أي نموذج نجح في أي اختبار لمقارنة الأداء.

---

## 📈 تقييم الأداء

### ممتاز (9-10 اختبارات ناجحة):
النموذج يفهم لغة البيان بشكل ممتاز ويمكن استخدامه لكتابة كود معقد.

### جيد جداً (7-8 اختبارات ناجحة):
النموذج يفهم معظم الميزات ويمكن استخدامه مع بعض التوجيه.

### جيد (5-6 اختبارات ناجحة):
النموذج يفهم الأساسيات لكن يحتاج مساعدة في الميزات المتقدمة.

### مقبول (3-4 اختبارات ناجحة):
النموذج يفهم الأساسيات البسيطة فقط.

### ضعيف (1-2 اختبارات ناجحة):
النموذج لا يفهم لغة البيان بشكل جيد. جرب:
- نموذج أحدث
- System Prompt أوضح
- أمثلة أكثر

---

## 🎉 الخلاصة

استخدم هذه الاختبارات لـ:
- ✅ التأكد من أن النموذج يفهم لغة البيان
- ✅ مقارنة أداء النماذج المختلفة
- ✅ تحديد نقاط القوة والضعف
- ✅ تحسين System Prompt

**بالتوفيق في الاختبار! 🚀**

</div>

