# دليل لغة البيان - الجزء الأول: مقدمة وتعريف

<div dir="rtl">

## 📚 جدول المحتويات

1. [ما هي لغة البيان؟](#ما-هي-لغة-البيان)
2. [المزايا الرئيسية](#المزايا-الرئيسية)
3. [الكلمات المفتاحية](#الكلمات-المفتاحية)
4. [التثبيت والتشغيل](#التثبيت-والتشغيل)
5. [طرق الاستخدام](#طرق-الاستخدام)
6. [التكامل مع بايثون (Python)](#التكامل-مع-python)
7. [أمثلة سريعة](#أمثلة-سريعة)

---

## ما هي لغة البيان؟

**لغة البيان** هي لغة برمجة عربية/ثنائية اللغة صُمِّمت لتجميع **ثلاثة أساليب برمجية** في لغة واحدة:

- أسلوب الأوامر المتتابعة (البرمجة الإجرائية)
- أسلوب الكائنات والأصناف (البرمجة الكائنية)
- أسلوب الحقائق والقواعد والاستعلامات (البرمجة المنطقية على طريقة برولوغ (Prolog))

هذه الأنماط تُستخدم عادة في لغات مختلفة؛ أمّا في البيان فيمكنك الجمع بينها في نفس الملف ونفس البرنامج.

### 🎯 الأنماط البرمجية الثلاثة (لمحة سريعة)

#### 1. **البرمجة الإجرائية** – تنفيذ أوامر متتابعة
```bayan
س = 10
ص = 20
المجموع = س + ص
اطبع(المجموع)
```

#### 2. **البرمجة الكائنية** – العمل مع كائنات (أصناف + خصائص + سلوك)
```bayan
صنف شخص: {
    دالة __init__(الذات, اسم, عمر): {
        الذات.اسم = اسم
        الذات.عمر = عمر
    }

    دالة عرف_بنفسك(الذات): {
        ارجع "مرحباً، أنا " + الذات.اسم
    }
}

شخص_١ = شخص("أحمد", 25)
اطبع(شخص_١.عرف_بنفسك())
```

#### 3. **البرمجة المنطقية** (أسلوب برولوغ (Prolog) في التعامل مع الحقائق والقواعد)
```bayan
هجين {
    # حقائق: علاقات أب/أم عربية بالكامل
    أب("أحمد", "محمد").
    أب("محمد", "علي").

    # قواعد: جد هو من يكون أبًا لأبٍ لشخصٍ ما
    جد(?جد, ?حفيد) :- أب(?جد, ?أب_وسيط), أب(?أب_وسيط, ?حفيد).

    # استعلام عربي مباشر (يعيد كل الأزواج الممكنة)
    استعلام جد(?من, ?إلى)?
}
```

---

## المزايا الرئيسية

### 🌟 1. البرمجة الهجينة الفريدة
- **الميزة الوحيدة في العالم**: لا توجد لغة أخرى تجمع الثلاثة أنماط بهذا الشكل
- يمكنك استخدام البرمجة المنطقية مع OOP في نفس الكود
- مثالية للذكاء الاصطناعي والأنظمة الخبيرة

### 🌍 2. الدعم الكامل للغة العربية
- **كلمات مفتاحية عربية وإنجليزية**
- **معرّفات عربية** (أسماء متغيرات، دوال، أصناف)
- **تعليقات عربية**
- **نصوص عربية بدون مشاكل** (اتصال الحروف، التشكيل، RTL)

### 🚀 3. سهولة التعلم
- بناء جملة واضح مع أقواس معقوفة `{}`
- مشابه في أسلوبه لبايثون (Python) وجافاسكربت (JavaScript)
- رسائل خطأ واضحة

### 🔧 4. قوية ومرنة
- **255+ اختبار ناجح** (100% success rate)
- دعم كامل لـ:
  - مولّدات (Generators) ودوال غير متزامنة (Async/Await)
  - مزخرفات (Decorators) ومديرو سياق (Context Managers)
  - معالجة الاستثناءات (Exception Handling)
  - الوراثة المتعدّدة (Multiple Inheritance)
  - قاعدة معرفة ديناميكية يمكن تعديلها في وقت التشغيل (assert/retract)
  - ميتا-مسندات (meta-predicates) مثل `findall/3`, `bagof/3`, `setof/3`

### 🤖 5. مثالية للذكاء الاصطناعي
- دوال مدمجة للـ ML: `sum`, `min`, `max`, `sorted`, `map`, `filter`
- دعم `*args` و `**kwargs` للمرونة
- البرمجة المنطقية للاستدلال
- قاعدة معرفة ديناميكية
- **محرك الشبكات السببية** (Causal Network Engine) - جديد! 🎯
  - بناء شبكات سببية في أي مجال
  - 12 نوع من العلاقات السببية
  - استدلال سببي متقدم
  - تطبيقات في العلوم، الطب، الأعمال، الفلسفة

---

## الكلمات المفتاحية

### 📝 الكلمات المفتاحية الإجرائية والكائنية

> **ملاحظة**: لغة البيان تدعم الكلمات المفتاحية بالعربية والإنجليزية معًا!
> يمكنك استخدام أي من الصيغتين حسب تفضيلك.

| الكلمة بالعربية | الكلمة بالإنجليزية | الشرح بالعربية |
|-----------------|---------------------|-----------------|
| `اطبع`          | `print`             | طباعة قيمة أو أكثر على الشاشة |
| `اذا`           | `if`                | تنفيذ جزء من الكود عند تحقّق شرط |
| `والا_اذا`      | `elif`              | شرط إضافي بعد شرط أوّل |
| `والا`          | `else`              | فرع يُنفّذ إذا لم تتحقق الشروط السابقة |
| `لكل`           | `for`               | حلقة تكرار على عناصر قائمة أو مجال |
| `في`            | `in`                | فحص الانتماء داخل قائمة أو مجموعة |
| `بينما`         | `while`             | حلقة تستمر ما دام الشرط صحيحًا |
| `دالة`          | `def`               | تعريف دالة جديدة |
| `ارجع`          | `return`            | إرجاع نتيجة من داخل الدالة |
| `صنف`           | `class`             | تعريف صنف (نوع كائني جديد) |
| `الذات`         | `self`              | المرجع إلى الكائن الحالي داخل الصنف |
| `صحيح`          | `True`              | قيمة منطقية صحيحة |
| `خطأ`           | `False`             | قيمة منطقية خاطئة |
| `لاشيء`         | `None`              | غياب القيمة / لا شيء |
| `و`             | `and`               | عامل منطقي (و) بين شرطين |
| `او`            | `or`                | عامل منطقي (أو) بين شرطين |
| `ليس`           | `not`               | نفي شرط منطقي |
| `استورد`        | `import`            | استيراد وحدة/مكتبة |
| `من`            | `from`              | استيراد رمز محدّد من مكتبة |
| `حاول`          | `try`               | تجربة تنفيذ جزء قد يسبّب خطأ |
| `اعترض`         | `except`            | التعامل مع الخطأ إذا حدث |
| `اخيرا`         | `finally`           | جزء يُنفّذ في جميع الأحوال في نهاية المحاولة |
| `ارفع`          | `raise`             | رفع استثناء يدويًا |
| `مع`            | `with`              | إدارة سياق (فتح ملف، اتصال، …) بأمان |
| `غير_متزامن`    | `async`             | تعريف دالة غير متزامنة |
| `انتظر`         | `await`             | انتظار نتيجة عملية غير متزامنة |
| `انتج`          | `yield`             | إنتاج قيم متتابعة من مولّد (generator) |
| `اكسر`          | `break`             | كسر الحلقة والخروج منها |
| `استمر`         | `continue`          | الانتقال إلى التكرار التالي في الحلقة |
| `مرر`           | `pass`              | لا تفعل شيئًا (مكان لحشو الكود لاحقًا) |
| `حذف`           | `del`               | حذف متغيّر أو عنصر من بنية بيانات |
| `عام`           | `global`            | إعلان أن المتغيّر عام (خارج الدالة) |
| `تأكيد`         | `assert`            | تأكيد أن شرطًا ما يجب أن يكون صحيحًا |
| `هو`            | `is`                | مقارنة الهوية (هل الكائنان نفس الكائن؟) |
| `لامبدا`        | `lambda`            | تعريف دالة مجهولة قصيرة داخل سطر واحد |

> لا توجد كلمة `new` في لغة البيان؛ لإنشاء كائن جديد نكتفي باستدعاء اسم الصنف كدالة، مثلًا:
> `طالب = طالب("محمد", 95)`.

### 🔍 الكلمات المفتاحية للبرمجة المنطقية

| الكلمة بالعربية | الكلمة بالإنجليزية | الوصف |
|-----------------|---------------------|-------|
| `استعلام`       | `query`             | تنفيذ استعلام منطقي |
| `حقيقة`         | `fact`              | تعريف حقيقة منطقية |
| `قاعدة`         | `rule`              | تعريف قاعدة منطقية |
| `.`             | `.`                 | نهاية حقيقة أو قاعدة |
| `:-`            | `:-`                | فاصل بين رأس القاعدة وجسمها (إذا كان) |
| `?-`            | `?-`                | بداية استعلام |
| `?متغير`        | `?Variable`         | متغير منطقي |
| `,`             | `,`                 | عامل (و) المنطقي |
| `;`             | `;`                 | عامل (أو) المنطقي |
| `ليس(هدف)`      | `not(Goal)`         | نفي الهدف |
| `findall/3`     | `findall/3`         | جمع جميع الحلول |
| `bagof/3`       | `bagof/3`           | جمع الحلول (يفشل إذا لم توجد) |
| `setof/3`       | `setof/3`           | جمع حلول فريدة ومرتبة |

### 🎲 الكلمات المفتاحية للاستدلال الاحتمالي (جديد!)

| العربية | English | الوصف |
|---------|---------|-------|
| `prob/3` | `prob/3` | حقيقة احتمالية: `prob("fact", "entity", probability)` |
| `ربما` | `maybe` | احتمال > 50% |
| `محتمل` | `likely` | احتمال > 70% |
| `غير_محتمل` | `unlikely` | احتمال < 30% |
| `ممكن` | `possible` | احتمال بين 20% و 80% |
| `مؤكد` | `certain` | احتمال > 95% |

**مثال:**
```bayan
هجين {
    # حقيقة احتمالية
    prob("أخضر", "حديقة", 0.7).

    # أدوات التشكيك
    استعلام ربما("أخضر", "حديقة")?   # نعم (70% > 50%)
    استعلام محتمل("أخضر", "حديقة")?  # نعم (70% > 70% = لا، لكن قريب)
}
```

### 🔗 الكلمات المفتاحية لنظام السببية والدلالة ⭐ جديد!

| العربية | English | الوصف |
|---------|---------|-------|
| `سبب_نتيجة` | `cause_effect` | تعريف قانون سببي مع السبب الفيزيائي/المنطقي |
| `علاقة` | `relation` | تعريف علاقة دلالية بين مفاهيم |

**الصيغة:**
```bayan
# قانون سببي: سبب_نتيجة(شرط, نتيجة, سبب, قوة)
سبب_نتيجة("رفع_شيء_لفوق", "يسقط", "جاذبية", 1.0).

# علاقة دلالية: علاقة(من, نوع_العلاقة, إلى, قوة)
علاقة("الاستحمام", "في", "حمام", 0.9).
```

**أنواع العلاقات:**
- **سببية**: `ينتج`, `يسبب`, `يؤدي_إلى`
- **مكانية**: `في`, `فيه`, `على`
- **أداة**: `ب`, `بواسطة`
- **هوية**: `هو`, `هي`
- **ملكية**: `له`, `لها`
- **غاية**: `لل`, `من_أجل`

**مثال كامل:**
```bayan
هجين {
    # قوانين سببية
    سبب_نتيجة("رفع_شيء_لفوق", "يسقط", "جاذبية", 1.0).
    سبب_نتيجة("دراسة_مجتهدة", "نجاح", "اكتساب_معرفة", 0.9).

    # علاقات دلالية
    علاقة("الاستحمام", "في", "حمام", 0.9).
    علاقة("البيت", "فيه", "حمام", 0.95).
    علاقة("النهر", "هو", "ماء", 1.0).

    # استعلامات
    استعلام سبب_نتيجة("رفع_شيء_لفوق", ?نتيجة, ?سبب, ?قوة).
    # النتيجة: ?نتيجة="يسقط", ?سبب="جاذبية", ?قوة=1.0

    استعلام علاقة("الاستحمام", ?نوع_علاقة, ?مكان, ?قوة).
    # النتيجة: ?نوع_علاقة="في", ?مكان="حمام", ?قوة=0.9
}
```

---

## التثبيت والتشغيل

### 📦 المتطلبات

- **Python 3.8+** (يُفضل Python 3.10+)
- نظام التشغيل: Linux, Windows, macOS

### 🔧 التثبيت

#### على Linux / macOS:

```bash
# 1. استنساخ المشروع
git clone https://github.com/mubtakir/nlp_bayan.git
cd bayan_python

# 2. إنشاء بيئة افتراضية (اختياري لكن مُوصى به)
python3 -m venv venv
source venv/bin/activate

# 3. تثبيت المتطلبات (إن وجدت)
pip install -r requirements.txt

# 4. اختبار التثبيت
python -m pytest tests/ -v
```

#### على Windows:

```cmd
REM 1. استنساخ المشروع
git clone https://github.com/mubtakir/nlp_bayan.git
cd bayan_python

REM 2. إنشاء بيئة افتراضية
python -m venv venv
venv\Scripts\activate

REM 3. تثبيت المتطلبات
pip install -r requirements.txt

REM 4. اختبار التثبيت
python -m pytest tests/ -v
```

### ▶️ طرق التشغيل

#### 1. تشغيل ملف `.by`:

```bash
# الطريقة الأولى: باستخدام Python
python -c "
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

with open('myfile.by', 'r', encoding='utf-8') as f:
    code = f.read()

lexer = HybridLexer(code)
tokens = lexer.tokenize()
parser = HybridParser(tokens)
ast = parser.parse()
interpreter = HybridInterpreter()
interpreter.interpret(ast)
"
```

#### 2. تشغيل كود مباشر:

```python
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

code = """
هجين {
    س = 10
    ص = 20
    اطبع(س + ص)
}
"""

lexer = HybridLexer(code)
tokens = lexer.tokenize()
parser = HybridParser(tokens)
ast = parser.parse()
interpreter = HybridInterpreter()
interpreter.interpret(ast)
```

#### 3. الوضع التفاعلي (REPL):

```python
# يمكن إنشاء REPL بسيط
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

interpreter = HybridInterpreter()

while True:
    try:
        code = input("bayan> ")
        if code.strip() in ['exit', 'quit']:
            break
        
        lexer = HybridLexer(code)
        tokens = lexer.tokenize()
        parser = HybridParser(tokens)
        ast = parser.parse()
        result = interpreter.interpret(ast)
        
        if result is not None:
            print(result)
    except Exception as e:
        print(f"Error: {e}")
```

---

## طرق الاستخدام

### 📄 1. ملفات البيان (`.by`)

احفظ الكود في ملف بامتداد `.by`:

```bayan
# myprogram.by
هجين {
    اطبع("مرحباً بك في لغة البيان")

    س = 10
    ص = 20
    اطبع(س + ص)
}
```

### 📦 2. استخراج ملف تنفيذي

يمكنك استخدام **PyInstaller** لإنشاء ملف تنفيذي:

```bash
# 1. تثبيت PyInstaller
pip install pyinstaller

# 2. إنشاء ملف Python wrapper
# bayan_runner.py
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

def main():
    if len(sys.argv) < 2:
        print("Usage: bayan_runner <file.by>")
        sys.exit(1)
    
    with open(sys.argv[1], 'r', encoding='utf-8') as f:
        code = f.read()
    
    lexer = HybridLexer(code)
    tokens = lexer.tokenize()
    parser = HybridParser(tokens)
    ast = parser.parse()
    interpreter = HybridInterpreter()
    interpreter.interpret(ast)

if __name__ == '__main__':
    main()

# 3. إنشاء الملف التنفيذي
pyinstaller --onefile bayan_runner.py

# 4. التشغيل
./dist/bayan_runner myprogram.by
```

---

## التكامل مع Python

### 🔗 1. استخدام لغة البيان من Python

```python
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

# تشغيل كود البيان
def run_bayan_code(code):
    lexer = HybridLexer(code)
    tokens = lexer.tokenize()
    parser = HybridParser(tokens)
    ast = parser.parse()
    interpreter = HybridInterpreter()
    return interpreter.interpret(ast)

# مثال
bayan_code = """
هجين {
    x = 10
    y = 20
    result = x + y
}
"""

run_bayan_code(bayan_code)
```

### 🔗 2. الوصول إلى متغيرات البيان من Python

```python
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridLexer, HybridParser, HybridInterpreter

code = """
هجين {
    الاسم = "أحمد"
    العمر = 25
    الدرجات = [85, 90, 88]
}
"""

lexer = HybridLexer(code)
tokens = lexer.tokenize()
parser = HybridParser(tokens)
ast = parser.parse()
interpreter = HybridInterpreter()
interpreter.interpret(ast)

# الوصول إلى المتغيرات
الاسم = interpreter.traditional.global_env['الاسم']
العمر = interpreter.traditional.global_env['العمر']
الدرجات = interpreter.traditional.global_env['الدرجات']

print(f"الاسم: {الاسم}, العمر: {العمر}, الدرجات: {الدرجات}")
```

### 🔗 3. استدعاء دوال Python من البيان

حالياً، يمكنك استخدام الدوال المدمجة في Python. لإضافة دوال مخصصة:

```python
import sys
sys.path.insert(0, 'bayan')
from bayan import HybridInterpreter

# إنشاء interpreter
interpreter = HybridInterpreter()

# إضافة دالة Python مخصصة
def my_custom_function(x, y):
    return x * y + 10

interpreter.traditional.global_env['custom_func'] = my_custom_function

# الآن يمكن استخدامها في كود البيان
# (يجب تشغيل الكود من خلال هذا الـ interpreter)
```

---

## أمثلة سريعة

### مثال 1: Hello World

```bayan
هجين {
    اطبع("Hello, World!")
    اطبع("مرحباً بالعالم!")
}
```

### مثال 2: حساب المجموع

```bayan
هجين {
    دالة احسب_المجموع(أ, ب): {
        ارجع أ + ب
    }

    النتيجة = احسب_المجموع(10, 20)
    اطبع(النتيجة)  # 30
}
```

### مثال 3: صنف بسيط

```bayan
هجين {
    صنف طالب: {
        دالة __init__(الذات, اسم, درجة): {
            الذات.اسم = اسم
            الذات.درجة = درجة
        }

        دالة اعرض(الذات): {
            اطبع(الذات.اسم)
            اطبع(الذات.درجة)
        }
    }

    طالب١ = طالب("محمد", 95)
    طالب١.اعرض()
}
```

### مثال 4: برمجة منطقية

```bayan
هجين {
    # حقائق
    يحب("أحمد", "برمجة").
    يحب("فاطمة", "رياضيات").
    يحب("علي", "برمجة").

    # استعلام
    النتائج = استعلام يحب(?شخص, "برمجة")?

    لكل نتيجة في النتائج: {
        اطبع(نتيجة["?شخص"])
    }
}
```

### مثال 5: محرك الشبكات السببية (جديد! 🎯)

```bayan
هجين {
    # إنشاء شبكة سببية
    create_network("my_network", "شبكة علمية", "scientific")

    # إضافة عقد
    add_node("my_network", "قوة", "concept", "مفهوم فيزيائي")
    add_node("my_network", "تسارع", "concept", "مفهوم فيزيائي")
    add_node("my_network", "حركة", "state", "حالة")

    # إضافة علاقات سببية
    add_causal_relation("my_network", "قوة", "تسارع", "causes", "0.95")
    add_causal_relation("my_network", "تسارع", "حركة", "leads_to", "0.9")

    # استدلال سببي
    infer_causal_chain("my_network", "قوة", "حركة", "3")
    # النتيجة: قوة → تسارع → حركة
}
```

---

## 📚 الخطوات التالية

الآن بعد أن تعرفت على لغة البيان، انتقل إلى:

- **[الجزء الثاني: البرمجة الإجرائية والكائنية](02_PROCEDURAL_OOP_AR.md)** - دليل شامل من المبتدئ إلى المحترف
- **[الجزء الثالث: البرمجة المنطقية](03_LOGIC_PROGRAMMING_AR.md)** - دليل البرمجة المنطقية الكامل
- إذا أردت التعمّق في **الذكاء الاصطناعي ومعالجة اللغة والنموذج اللغوي التصوّري**:
  - `ai/AI_LIBRARY_GUIDE.md` — دليل مكتبة الذكاء الاصطناعي ومعالجة اللغة `ai.nlp` و`ai.ml`.
  - `docs/CONCEPTUAL_LM_BLUEPRINT.md` — مخطط معماري عالي المستوى لطبقات النموذج اللغوي التصوّري.
  - `docs/CONCEPTUAL_LM_AI_HANDOVER.md` — دليل استلام/تسليم لتطوير طبقات الـ Conceptual LM للمطور أو النموذج التالي.
- **🆕 الطبقة الوسيطية** (`extensions/`):
  - `دليل_الطبقة_الوسيطة.md` — دليل شامل للميزات الجديدة

---

## 🆕 الطبقة الوسيطية (الإصدار 0.4.0)

تم إضافة طبقة وسيطية جديدة توفر ميزات متقدمة:

### 🌍 محول اللهجات العربية
```python
from extensions import DialectAdapter

adapter = DialectAdapter()

# تحويل من المصرية للفصحى
result = adapter.convert_to_standard("أحمد عايز ياكل تفاحة")
print(result.converted)  # "أحمد يريد ياكل تفاحة"

# اكتشاف اللهجة
dialect, confidence = adapter.detect_dialect("شلون حالك")
print(dialect)  # Dialect.GULF
```

### 📊 تصور المعادلات اللغوية
```python
from extensions import EquationVisualizer

viz = EquationVisualizer()
svg = viz.visualize_equation("أحمد", "أكل", "تفاحة")
viz.save_to_file(svg, "equation.html")
```

### 🎓 النظام التعليمي التفاعلي
```python
from extensions import BayanTutor

tutor = BayanTutor()
tutor.start_lesson("intro")
exercise = tutor.get_exercise(0)
result = tutor.check_answer(0, "فاعل + فعل → مفعول")
```

### 🤖 نظام الحوار الذكي
```python
from extensions import IntelligentDialogueSystem

system = IntelligentDialogueSystem()
response = system.chat("مرحباً، أنا محمد")
print(response)  # "مرحباً! سعيد بالتحدث معك."
```

---

## 🆘 الدعم والمساعدة

- **الوثائق**: مجلّد `docs/` داخل المشروع
- **الأمثلة**: مجلّد `examples/` الذي يحتوي على برامج جاهزة
- **الاختبارات**: مجلّد `tests/` لاختبار صحة اللغة والمكتبات
- **الطبقة الوسيطية**: مجلّد `extensions/` للميزات الجديدة

---

**لغة البيان - لغة برمجة عربية ثورية! 🚀**

</div>

