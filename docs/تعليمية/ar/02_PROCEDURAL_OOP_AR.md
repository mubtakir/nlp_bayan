# دليل لغة البيان - الجزء الثاني: البرمجة الإجرائية والكائنية

<div dir="rtl">
> ملاحظة: هذا الدليل كبير الحجم وسيُقسّم إلى عدة أجزاء (Part 1/2/3) لتسهيل التصفح. كما أضفنا ميزة "نظام الكيانات" (0..1) موثقة في docs/ENTITY_SYSTEM_GUIDE.md.


> الأجزاء: [PART1](02_PROCEDURAL_OOP_AR_PART1.md) | [PART2](02_PROCEDURAL_OOP_AR_PART2.md) | [PART3](02_PROCEDURAL_OOP_AR_PART3.md) | [PART4](02_PROCEDURAL_OOP_AR_PART4.md)



## 📚 جدول المحتويات

### القسم الأول: الأساسيات (للمبتدئين)
1. [المتغيرات وأنواع البيانات](#1-المتغيرات-وأنواع-البيانات)
2. [العمليات الحسابية والمنطقية](#2-العمليات-الحسابية-والمنطقية)
3. [النصوص (Strings)](#3-النصوص-strings)
4. [القوائم (Lists)](#4-القوائم-lists)
5. [القواميس (Dictionaries)](#5-القواميس-dictionaries)
6. [المجموعات (Sets)](#6-المجموعات-sets)

### القسم الثاني: التحكم في التدفق (متوسط)
7. [الشروط (if/elif/else)](#7-الشروط-ifelifelse)
8. [الحلقات (for/while)](#8-الحلقات-forwhile)
9. [الدوال (Functions)](#9-الدوال-functions)
10. [معالجة الاستثناءات](#10-معالجة-الاستثناءات)

### القسم الثالث: البرمجة الكائنية (متقدم)
11. [الأصناف والكائنات](#11-الأصناف-والكائنات)
12. [الوراثة](#12-الوراثة)
13. [التغليف](#13-التغليف)
14. [تعدد الأشكال](#14-تعدد-الأشكال)

### القسم الرابع: ميزات متقدمة (محترف)
15. [Decorators](#15-decorators)
16. [Generators](#16-generators)
17. [Async/Await](#17-asyncawait)
18. [Context Managers](#18-context-managers)
19. [*args و **kwargs](#19-args-و-kwargs)

---

# القسم الأول: الأساسيات

## 1. المتغيرات وأنواع البيانات

### 1.1 تعريف المتغيرات

في لغة البيان، لا تحتاج لتحديد نوع المتغير (Dynamic Typing):

```bayan
هجين {
    # أرقام صحيحة (Integers)
    العمر = 25
    السنة = 2024

    # أرقام عشرية (Floats)
    السعر = 99.99
    باي = 3.14159

    # نصوص (Strings)
    الاسم = "أحمد"
    المدينة = "الرياض"

    # قيم منطقية (Booleans)
    طالب = صحيح
    متزوج = خطأ

    # قيمة فارغة (None)
    النتيجة = لاشيء
}
```

### 1.2 أنواع البيانات الأساسية

```bayan
هجين {
    # Integer
    س = 10
    اطبع(س)  # 10

    # Float
    ص = 3.14
    اطبع(ص)  # 3.14

    # String
    نص = "مرحباً"
    اطبع(نص)  # مرحباً

    # Boolean
    علم = صحيح
    اطبع(علم)  # صحيح

    # None
    فارغ = لاشيء
    اطبع(فارغ)  # لاشيء
}
```

### 1.3 تحويل الأنواع

```bayan
هجين {
    # String to Integer
    نص = "123"
    رقم = int(نص)
    اطبع(رقم)  # 123

    # Integer to String
    عمر = 25
    نص_العمر = str(عمر)
    اطبع(نص_العمر)  # "25"

    # String to Float
    نص_السعر = "99.99"
    سعر = float(نص_السعر)
    اطبع(سعر)  # 99.99

    # Integer to Float
    س = 10
    ص = float(س)
    اطبع(ص)  # 10.0
}
```

---

## 2. العمليات الحسابية والمنطقية

### 2.1 العمليات الحسابية

```bayan
هجين {
    أ = 10
    ب = 3

    # الجمع
    المجموع = أ + ب
    اطبع(المجموع)  # 13

    # الطرح
    الفرق = أ - ب
    اطبع(الفرق)  # 7

    # الضرب
    الحاصل = أ * ب
    اطبع(الحاصل)  # 30

    # القسمة
    القسمة = أ / ب
    اطبع(القسمة)  # 3.333...

    # القسمة الصحيحة
    القسمة_الصحيحة = أ // ب
    اطبع(القسمة_الصحيحة)  # 3

    # الباقي
    الباقي = أ % ب
    اطبع(الباقي)  # 1

    # الأس
    الأس = أ ** ب
    اطبع(الأس)  # 1000
}
```

### 2.2 عمليات المقارنة

```bayan
هجين {
    س = 10
    ص = 20

    # يساوي
    النتيجة1 = (س == ص)
    اطبع(النتيجة1)  # خطأ

    # لا يساوي
    النتيجة2 = (س != ص)
    اطبع(النتيجة2)  # صحيح

    # أكبر من
    النتيجة3 = (س > ص)
    اطبع(النتيجة3)  # خطأ

    # أصغر من
    النتيجة4 = (س < ص)
    اطبع(النتيجة4)  # صحيح

    # أكبر من أو يساوي
    النتيجة5 = (س >= 10)
    اطبع(النتيجة5)  # صحيح

    # أصغر من أو يساوي
    النتيجة6 = (ص <= 20)
    اطبع(النتيجة6)  # صحيح
}
```

### 2.3 العمليات المنطقية

```bayan
هجين {
    أ = صحيح
    ب = خطأ

    # AND (و)
    النتيجة1 = أ و ب
    اطبع(النتيجة1)  # خطأ

    # OR (أو)
    النتيجة2 = أ او ب
    اطبع(النتيجة2)  # صحيح

    # NOT (ليس)
    النتيجة3 = ليس أ
    اطبع(النتيجة3)  # خطأ

    # مثال مركب
    س = 10
    ص = 20
    النتيجة4 = (س > 5) و (ص < 30)
    اطبع(النتيجة4)  # صحيح
}
```

---

## 3. النصوص (Strings)

### 3.1 إنشاء النصوص

```bayan
هجين {
    # نص بسيط
    نص1 = "مرحباً"
    نص2 = 'Hello'

    # نص متعدد الأسطر
    نص3 = """
    هذا نص
    متعدد الأسطر
    """

    # نص فارغ
    فارغ = ""
}
```

### 3.2 عمليات النصوص

```bayan
هجين {
    # دمج النصوص
    الأول = "محمد"
    الأخير = "أحمد"
    الاسم_الكامل = الأول + " " + الأخير
    اطبع(الاسم_الكامل)  # "محمد أحمد"

    # تكرار النص
    نص = "Ha" * 3
    اطبع(نص)  # "HaHaHa"

    # طول النص
    الطول = len(الاسم_الكامل)
    اطبع(الطول)  # 9

    # الوصول إلى حرف
    الحرف_الأول = الاسم_الكامل[0]
    اطبع(الحرف_الأول)  # "م"

    # قطع النص (Slicing)
    نص_فرعي = الاسم_الكامل[0:4]
    اطبع(نص_فرعي)  # "محمد"
}
```

### 3.3 دوال النصوص المدمجة

```bayan
هجين {
    نص = "Hello World"

    # تحويل لأحرف كبيرة
    نص_كبير = upper(نص)
    اطبع(نص_كبير)  # "HELLO WORLD"

    # تحويل لأحرف صغيرة
    نص_صغير = lower(نص)
    اطبع(نص_صغير)  # "hello world"

    # استبدال
    نص_جديد = replace(نص, "World", "Python")
    اطبع(نص_جديد)  # "Hello Python"

    # تقسيم النص
    كلمات = split(نص, " ")
    اطبع(كلمات)  # ["Hello", "World"]
}
```

### 3.4 Escape Sequences

```bayan
هجين {
    # سطر جديد
    نص1 = "السطر الأول\nالسطر الثاني"
    اطبع(نص1)

    # Tab
    نص2 = "العمود1\tالعمود2"
    اطبع(نص2)

    # علامة اقتباس
    نص3 = "قال: \"مرحباً\""
    اطبع(نص3)

    # Backslash
    نص4 = "المسار: C:\\Users\\Ahmad"
    اطبع(نص4)
}
```

---

## 4. القوائم (Lists)

### 4.1 إنشاء القوائم

```bayan
هجين {
    # قائمة فارغة
    قائمة_فارغة = []

    # قائمة أرقام
    أرقام = [1, 2, 3, 4, 5]

    # قائمة نصوص
    أسماء = ["أحمد", "فاطمة", "علي"]

    # قائمة مختلطة
    مختلط = [1, "نص", 3.14, صحيح]

    # قائمة متداخلة
    متداخل = [[1, 2], [3, 4], [5, 6]]
}
```

### 4.2 الوصول إلى العناصر

```bayan
هجين {
    فواكه = ["تفاح", "موز", "برتقال", "عنب"]

    # الوصول بالفهرس
    الأول = فواكه[0]
    اطبع(الأول)  # "تفاح"

    # الفهرس السالب (من النهاية)
    الأخير = فواكه[-1]
    اطبع(الأخير)  # "عنب"

    # Slicing
    مجموعة_فرعية = فواكه[1:3]
    اطبع(مجموعة_فرعية)  # ["موز", "برتقال"]

    # طول القائمة
    الطول = len(فواكه)
    اطبع(الطول)  # 4
}
```

### 4.3 تعديل القوائم

```bayan
هجين {
    أرقام = [1, 2, 3, 4, 5]

    # تغيير عنصر
    أرقام[0] = 10
    اطبع(أرقام)  # [10, 2, 3, 4, 5]

    # إضافة عنصر في النهاية
    أرقام.append(6)
    اطبع(أرقام)  # [10, 2, 3, 4, 5, 6]

    # إضافة عنصر في موضع محدد
    أرقام.insert(0, 0)
    اطبع(أرقام)  # [0, 10, 2, 3, 4, 5, 6]

    # حذف عنصر
    أرقام.remove(10)
    اطبع(أرقام)  # [0, 2, 3, 4, 5, 6]

    # حذف بالفهرس
    حذف أرقام[0]
    اطبع(أرقام)  # [2, 3, 4, 5, 6]
}
```

### 4.4 عمليات القوائم

```bayan
هجين {
    قائمة1 = [1, 2, 3]
    قائمة2 = [4, 5, 6]

    # دمج القوائم
    مدمج = قائمة1 + قائمة2
    اطبع(مدمج)  # [1, 2, 3, 4, 5, 6]

    # تكرار القائمة
    مكرر = قائمة1 * 2
    اطبع(مكرر)  # [1, 2, 3, 1, 2, 3]

    # البحث عن عنصر
    موجود = 2 في قائمة1
    اطبع(موجود)  # صحيح

    # فرز القائمة
    أرقام = [5, 2, 8, 1, 9]
    أرقام_مرتبة = sorted(أرقام)
    اطبع(أرقام_مرتبة)  # [1, 2, 5, 8, 9]

    # عكس القائمة
    قائمة_معكوسة = list(reversed(أرقام))
    اطبع(قائمة_معكوسة)  # [9, 1, 8, 2, 5]
}
```

---

## 5. القواميس (Dictionaries)

### 5.1 إنشاء القواميس

```bayan
هجين {
    # قاموس فارغ
    قاموس_فارغ = {}

    # قاموس بسيط
    شخص = {
        "الاسم": "أحمد",
        "العمر": 25,
        "المدينة": "الرياض"
    }

    # قاموس بمفاتيح عربية
    طالب = {
        "الاسم": "فاطمة",
        "العمر": 20,
        "التخصص": "علوم حاسب"
    }
}
```

### 5.2 الوصول إلى القيم

```bayan
هجين {
    شخص = {
        "الاسم": "أحمد",
        "العمر": 25,
        "المدينة": "الرياض"
    }

    # الوصول بالمفتاح
    اسم = شخص["الاسم"]
    اطبع(اسم)  # "أحمد"

    # التحقق من وجود مفتاح
    لديه_عمر = "العمر" في شخص
    اطبع(لديه_عمر)  # صحيح

    # الحصول على جميع المفاتيح
    مفاتيح = list(شخص.keys())
    اطبع(مفاتيح)  # ["الاسم", "العمر", "المدينة"]

    # الحصول على جميع القيم
    قيم = list(شخص.values())
    اطبع(قيم)  # ["أحمد", 25, "الرياض"]
}
```

### 5.3 تعديل القواميس

```bayan
هجين {
    شخص = {
        "الاسم": "أحمد",
        "العمر": 25
    }

    # إضافة/تعديل قيمة
    شخص["المدينة"] = "الرياض"
    شخص["العمر"] = 26
    اطبع(شخص)

    # حذف مفتاح
    حذف شخص["العمر"]
    اطبع(شخص)

    # مسح القاموس
    شخص.clear()
    اطبع(شخص)  # {}
}
```

---

## 6. المجموعات (Sets)

### 6.1 إنشاء المجموعات

```bayan
هجين {
    # مجموعة من الأرقام
    أرقام = {1, 2, 3, 4, 5}

    # مجموعة من النصوص
    فواكه = {"تفاح", "موز", "برتقال"}

    # ملاحظة: المجموعات لا تحتوي على عناصر مكررة
    أرقام_فريدة = {1, 2, 2, 3, 3, 3}
    اطبع(أرقام_فريدة)  # {1, 2, 3}
}
```

### 6.2 عمليات المجموعات

```bayan
هجين {
    مجموعة1 = {1, 2, 3, 4}
    مجموعة2 = {3, 4, 5, 6}

    # الاتحاد (Union)
    اتحاد = مجموعة1 | مجموعة2
    اطبع(اتحاد)  # {1, 2, 3, 4, 5, 6}

    # التقاطع (Intersection)
    تقاطع = مجموعة1 & مجموعة2
    اطبع(تقاطع)  # {3, 4}

    # الفرق (Difference)
    فرق = مجموعة1 - مجموعة2
    اطبع(فرق)  # {1, 2}

    # إضافة عنصر
    مجموعة1.add(5)
    اطبع(مجموعة1)  # {1, 2, 3, 4, 5}

    # حذف عنصر
    مجموعة1.remove(5)
    اطبع(مجموعة1)  # {1, 2, 3, 4}
}
```

---

# القسم الثاني: التحكم في التدفق

## 7. الشروط (if/elif/else)

### 7.1 الشرط البسيط

```bayan
هجين {
    العمر = 18

    اذا العمر >= 18: {
        اطبع("بالغ")
    }
}
```

### 7.2 if-else

```bayan
هجين {
    الحرارة = 30

    اذا الحرارة > 25: {
        اطبع("الجو حار")
    }
    والا: {
        اطبع("الجو معتدل")
    }
}
```

### 7.3 if-elif-else

```bayan
هجين {
    الدرجة = 85

    اذا الدرجة >= 90: {
        اطبع("ممتاز")
    }
    والا_اذا الدرجة >= 80: {
        اطبع("جيد جداً")
    }
    والا_اذا الدرجة >= 70: {
        اطبع("جيد")
    }
    والا_اذا الدرجة >= 60: {
        اطبع("مقبول")
    }
    والا: {
        اطبع("راسب")
    }
}
```

### 7.4 شروط متداخلة

```bayan
هجين {
    العمر = 20
    لديه_رخصة = صحيح

    اذا العمر >= 18: {
        اذا لديه_رخصة: {
            اطبع("يمكنك القيادة")
        }
        والا: {
            اطبع("تحتاج رخصة قيادة")
        }
    }
    والا: {
        اطبع("أنت صغير السن")
    }
}
```

---

## 8. الحلقات (for/while)

### 8.1 حلقة for

```bayan
هجين {
    # التكرار على قائمة
    فواكه = ["تفاح", "موز", "برتقال"]

    لكل فاكهة في فواكه: {
        اطبع(فاكهة)
    }

    # التكرار على range
    لكل ع في range(5): {
        اطبع(ع)  # 0, 1, 2, 3, 4
    }

    # range مع بداية ونهاية
    لكل ع في range(1, 6): {
        اطبع(ع)  # 1, 2, 3, 4, 5
    }

    # range مع خطوة
    لكل ع في range(0, 10, 2): {
        اطبع(ع)  # 0, 2, 4, 6, 8
    }
}
```

### 8.2 حلقة while

```bayan
هجين {
    # حلقة while بسيطة
    العداد = 0

    بينما العداد < 5: {
        اطبع(العداد)
        العداد = العداد + 1
    }

    # حلقة while مع شرط
    رقم = 1

    بينما رقم <= 10: {
        اذا رقم % 2 == 0: {
            اطبع(رقم)
        }
        رقم = رقم + 1
    }
}
```

### 8.3 break و continue

```bayan
هجين {
    # break - إيقاف الحلقة
    لكل ع في range(10): {
        اذا ع == 5: {
            اكسر
        }
        اطبع(ع)  # 0, 1, 2, 3, 4
    }

    # continue - تخطي التكرار الحالي
    لكل ع في range(5): {
        اذا ع == 2: {
            استمر
        }
        اطبع(ع)  # 0, 1, 3, 4
    }
}
```

### 8.4 enumerate و zip

```bayan
هجين {
    # enumerate - الحصول على الفهرس والقيمة
    فواكه = ["تفاح", "موز", "برتقال"]

    لكل فهرس_قيمة في enumerate(فواكه): {
        فهرس = فهرس_قيمة[0]
        قيمة = فهرس_قيمة[1]
        اطبع(فهرس)
        اطبع(قيمة)
    }

    # zip - دمج قائمتين
    أسماء = ["أحمد", "فاطمة", "علي"]
    أعمار = [25, 22, 30]

    لكل زوج في zip(أسماء, أعمار): {
        اسم = زوج[0]
        عمر = زوج[1]
        اطبع(اسم)
        اطبع(عمر)
    }
}
```

---

## 9. الدوال (Functions)

### 9.1 تعريف دالة بسيطة

```bayan
هجين {
    دالة تحية(): {
        اطبع("مرحباً!")
    }

    تحية()  # استدعاء الدالة
}
```

### 9.2 دالة بمعاملات

```bayan
هجين {
    دالة تحية_شخص(اسم): {
        اطبع("مرحباً " + اسم)
    }

    تحية_شخص("أحمد")  # "مرحباً أحمد"
    تحية_شخص("فاطمة")  # "مرحباً فاطمة"
}
```

### 9.3 دالة بقيمة إرجاع

```bayan
هجين {
    دالة جمع(أ, ب): {
        ارجع أ + ب
    }

    النتيجة = جمع(10, 20)
    اطبع(النتيجة)  # 30
}
```

### 9.4 معاملات افتراضية

```bayan
هجين {
    دالة تحية(اسم, تحية="مرحباً"): {
        ارجع تحية + " " + اسم
    }

    رسالة1 = تحية("أحمد")
    اطبع(رسالة1)  # "مرحباً أحمد"

    رسالة2 = تحية("فاطمة", "أهلاً")
    اطبع(رسالة2)  # "أهلاً فاطمة"
}
```

### 9.5 دوال متعددة القيم

```bayan
هجين {
    دالة احصل_على_الحد_الأدنى_والأقصى(أرقام): {
        الحد_الأدنى = min(أرقام)
        الحد_الأقصى = max(أرقام)
        ارجع [الحد_الأدنى, الحد_الأقصى]
    }

    النتيجة = احصل_على_الحد_الأدنى_والأقصى([5, 2, 8, 1, 9])
    اطبع(النتيجة[0])  # 1 (minimum)
    اطبع(النتيجة[1])  # 9 (maximum)
}
```

### 9.6 دوال متداخلة

```bayan
هجين {
    دالة دالة_خارجية(س): {
        دالة دالة_داخلية(ص): {
            ارجع س + ص
        }

        ارجع دالة_داخلية(10)
    }

    النتيجة = دالة_خارجية(5)
    اطبع(النتيجة)  # 15
}
```

---

## 10. معالجة الاستثناءات

### 10.1 try-except الأساسي

```bayan
هجين {
    حاول: {
        س = 10 / 0
    }
    اعترض ZeroDivisionError: {
        اطبع("خطأ: القسمة على صفر!")
    }
}
```

### 10.2 معالجة استثناءات متعددة

```bayan
هجين {
    حاول: {
        رقم = int("abc")
    }
    اعترض ValueError: {
        اطبع("خطأ: قيمة غير صحيحة")
    }
    اعترض TypeError: {
        اطبع("خطأ: نوع غير صحيح")
    }
}
```

### 10.3 try-except-finally

```bayan
هجين {
    حاول: {
        ملف = open("data.txt", "r")
        محتوى = ملف.read()
    }
    اعترض FileNotFoundError: {
        اطبع("الملف غير موجود")
    }
    اخيرا: {
        اطبع("تم الانتهاء من المحاولة")
    }
}
```

### 10.4 رفع استثناء

```bayan
هجين {
    دالة تحقق_من_العمر(عمر): {
        اذا عمر < 0: {
            ارفع ValueError("العمر لا يمكن أن يكون سالباً")
        }
        ارجع عمر
    }

    حاول: {
        تحقق_من_العمر(-5)
    }
    اعترض ValueError: {
        اطبع("خطأ في العمر")
    }
}
```

---

# القسم الثالث: البرمجة الكائنية

## 11. الأصناف والكائنات

### 11.1 تعريف صنف بسيط

```bayan
هجين {
    صنف شخص: {
        دالة __init__(الذات, اسم, عمر): {
            الذات.اسم = اسم
            الذات.عمر = عمر
        }

        دالة عرض(الذات): {
            اطبع(الذات.اسم)
            اطبع(الذات.عمر)
        }
    }

    # إنشاء كائن
    شخص1 = شخص("أحمد", 25)
    شخص1.عرض()
}
```

### 11.2 الخصائص والطرق

```bayan
هجين {
    صنف طالب: {
        دالة __init__(الذات, اسم, درجة): {
            الذات.اسم = اسم
            الذات.درجة = درجة
            الذات.مواد = []
        }

        دالة اضف_مادة(الذات, مادة): {
            الذات.مواد.append(مادة)
        }

        دالة احصل_على_المعلومات(الذات): {
            معلومات = الذات.اسم + " - الدرجة: " + str(الذات.درجة)
            ارجع معلومات
        }
    }

    طالب = طالب("فاطمة", 95)
    طالب.اضف_مادة("رياضيات")
    طالب.اضف_مادة("فيزياء")

    اطبع(طالب.احصل_على_المعلومات())
    اطبع(طالب.مواد)
}
```

### 11.3 صنف مع عدة طرق

```bayan
هجين {
    صنف حساب_بنكي: {
        دالة __init__(الذات, المالك, الرصيد): {
            الذات.المالك = المالك
            الذات.الرصيد = الرصيد
        }

        دالة ايداع(الذات, المبلغ): {
            الذات.الرصيد = الذات.الرصيد + المبلغ
            ارجع الذات.الرصيد
        }

        دالة سحب(الذات, المبلغ): {
            اذا المبلغ > الذات.الرصيد: {
                اطبع("رصيد غير كافٍ")
                ارجع الذات.الرصيد
            }
            الذات.الرصيد = الذات.الرصيد - المبلغ
            ارجع الذات.الرصيد
        }

        دالة احصل_على_الرصيد(الذات): {
            ارجع الذات.الرصيد
        }
    }

    حساب = حساب_بنكي("أحمد", 1000)
    حساب.ايداع(500)
    اطبع(حساب.احصل_على_الرصيد())  # 1500

    حساب.سحب(200)
    اطبع(حساب.احصل_على_الرصيد())  # 1300
}
```

---

## 12. الوراثة

### 12.1 وراثة بسيطة

```bayan
هجين {
    # الصنف الأب
    صنف حيوان: {
        دالة __init__(الذات, اسم): {
            الذات.اسم = اسم
        }

        دالة تكلم(الذات): {
            اطبع("الحيوان يصدر صوتاً")
        }
    }

    # الصنف الابن
    صنف كلب: {
        دالة __init__(الذات, اسم, سلالة): {
            الذات.اسم = اسم
            الذات.سلالة = سلالة
        }

        دالة تكلم(الذات): {
            اطبع("الكلب ينبح")
        }
    }

    كلب = كلب("ريكس", "جيرمن")
    كلب.تكلم()  # "الكلب ينبح"
}
```

### 12.2 استخدام super()

```bayan
هجين {
    صنف مركبة: {
        دالة __init__(الذات, العلامة, الطراز): {
            الذات.العلامة = العلامة
            الذات.الطراز = الطراز
        }

        دالة معلومات(الذات): {
            ارجع الذات.العلامة + " " + الذات.الطراز
        }
    }

    صنف سيارة: {
        دالة __init__(الذات, العلامة, الطراز, الأبواب): {
            الذات.العلامة = العلامة
            الذات.الطراز = الطراز
            الذات.الأبواب = الأبواب
        }

        دالة معلومات(الذات): {
            معلومات_أساسية = الذات.العلامة + " " + الذات.الطراز
            ارجع معلومات_أساسية + " - الأبواب: " + str(الذات.الأبواب)
        }
    }

    سيارة = سيارة("تويوتا", "كامري", 4)
    اطبع(سيارة.معلومات())
}
```

### 12.3 الوراثة المتعددة

```bayan
هجين {
    صنف قابل_للطيران: {
        دالة طر(الذات): {
            اطبع("يطير")
        }
    }

    صنف قابل_للسباحة: {
        دالة اسبح(الذات): {
            اطبع("يسبح")
        }
    }

    صنف بطة: {
        دالة __init__(الذات, اسم): {
            الذات.اسم = اسم
        }

        دالة طر(الذات): {
            اطبع("البطة تطير")
        }

        دالة اسبح(الذات): {
            اطبع("البطة تسبح")
        }
    }

    بطة = بطة("دونالد")
    بطة.طر()
    بطة.اسبح()
}
```

---

## 13. التغليف

### 13.1 خصائص خاصة (Private)

```bayan
هجين {
    صنف حساب_بنكي: {
        دالة __init__(الذات, رصيد): {
            الذات.__رصيد = رصيد  # خاصية خاصة
        }

        دالة احصل_على_الرصيد(الذات): {
            ارجع الذات.__رصيد
        }

        دالة عين_الرصيد(الذات, مبلغ): {
            اذا مبلغ >= 0: {
                الذات.__رصيد = مبلغ
            }
        }
    }

    حساب = حساب_بنكي(1000)
    اطبع(حساب.احصل_على_الرصيد())  # 1000

    حساب.عين_الرصيد(2000)
    اطبع(حساب.احصل_على_الرصيد())  # 2000
}
```

---

## 14. تعدد الأشكال

### 14.1 Polymorphism الأساسي

```bayan
هجين {
    صنف شكل: {
        دالة مساحة(الذات): {
            ارجع 0
        }
    }

    صنف مستطيل: {
        دالة __init__(الذات, العرض, الارتفاع): {
            الذات.العرض = العرض
            الذات.الارتفاع = الارتفاع
        }

        دالة مساحة(الذات): {
            ارجع الذات.العرض * الذات.الارتفاع
        }
    }

    صنف دائرة: {
        دالة __init__(الذات, نصف_القطر): {
            الذات.نصف_القطر = نصف_القطر
        }

        دالة مساحة(الذات): {
            ارجع 3.14 * الذات.نصف_القطر * الذات.نصف_القطر
        }
    }

    مستطيل = مستطيل(5, 10)
    اطبع(مستطيل.مساحة())  # 50

    دائرة = دائرة(7)
    اطبع(دائرة.مساحة())  # 153.86
}
```

---

# القسم الرابع: ميزات متقدمة

## 15. Decorators

### 15.1 Decorator بسيط

```bayan
هجين {
    دالة مزخرف_خاص(دالة): {
        دالة غلاف(): {
            اطبع("قبل الدالة")
            دالة()
            اطبع("بعد الدالة")
        }
        ارجع غلاف
    }

    @مزخرف_خاص
    دالة قل_مرحبا(): {
        اطبع("مرحباً!")
    }

    قل_مرحبا()
}
```

### 15.2 Decorator بمعاملات

```bayan
هجين {
    دالة كرر(مرات): {
        دالة مزخرف(دالة): {
            دالة غلاف(): {
                لكل ع في range(مرات): {
                    دالة()
                }
            }
            ارجع غلاف
        }
        ارجع مزخرف
    }

    @كرر(3)
    دالة تحية(): {
        اطبع("مرحباً")
    }

    تحية()  # يطبع "مرحباً" 3 مرات
}
```

---

## 16. Generators

### 16.1 Generator بسيط

```bayan
هجين {
    دالة عد_حتى(ن): {
        ع = 1
        بينما ع <= ن: {
            انتج ع
            ع = ع + 1
        }
    }

    لكل رقم في عد_حتى(5): {
        اطبع(رقم)  # 1, 2, 3, 4, 5
    }
}
```

### 16.2 Generator للأعداد الزوجية

```bayan
هجين {
    دالة أرقام_زوجية(الحد_الأقصى): {
        ن = 0
        بينما ن <= الحد_الأقصى: {
            انتج ن
            ن = ن + 2
        }
    }

    لكل رقم في أرقام_زوجية(10): {
        اطبع(رقم)  # 0, 2, 4, 6, 8, 10
    }
}
```

---

## 17. Async/Await

### 17.1 دالة async بسيطة

```bayan
هجين {
    غير_متزامن دالة جلب_البيانات(): {
        اطبع("جاري جلب البيانات...")
        ارجع "البيانات"
    }

    غير_متزامن دالة رئيسية(): {
        النتيجة = انتظر جلب_البيانات()
        اطبع(النتيجة)
    }
}
```

---

## 18. Context Managers

### 18.1 استخدام with

```bayan
هجين {
    صنف مدير_الملفات: {
        دالة __init__(الذات, اسم_الملف): {
            الذات.اسم_الملف = اسم_الملف
        }

        دالة __enter__(الذات): {
            اطبع("فتح الملف")
            ارجع الذات
        }

        دالة __exit__(الذات): {
            اطبع("إغلاق الملف")
        }
    }

    مع مدير_الملفات("data.txt"): {
        اطبع("العمل مع الملف")
    }
}
```

---

## 19. *args و **kwargs

### 19.1 استخدام *args

```bayan
هجين {
    دالة مجموع_الكل(*أرقام): {
        المجموع = 0
        لكل رقم في أرقام: {
            المجموع = المجموع + رقم
        }
        ارجع المجموع
    }

    النتيجة1 = مجموع_الكل(1, 2, 3)
    اطبع(النتيجة1)  # 6

    النتيجة2 = مجموع_الكل(1, 2, 3, 4, 5)
    اطبع(النتيجة2)  # 15
}
```

### 19.2 استخدام **kwargs

```bayan
هجين {
    دالة اطبع_المعلومات(**معلومات): {
        لكل مفتاح في معلومات: {
            اطبع(مفتاح)
            اطبع(معلومات[مفتاح])
        }
    }

    اطبع_المعلومات(الاسم="أحمد", العمر=25, المدينة="الرياض")
}
```

### 19.3 استخدام كليهما

```bayan
هجين {
    دالة انشئ_ملف_شخصي(اسم, *هوايات, **تفاصيل): {
        اطبع(اسم)

        لكل هواية في هوايات: {
            اطبع(هواية)
        }

        لكل مفتاح في تفاصيل: {
            اطبع(مفتاح)
            اطبع(تفاصيل[مفتاح])
        }
    }

    انشئ_ملف_شخصي("أحمد", "قراءة", "برمجة", العمر=25, المدينة="الرياض")
}
```

---

## 🎓 خاتمة

الآن أصبحت تعرف جميع أساسيات البرمجة الإجرائية والكائنية في لغة البيان!

### 📚 الخطوات التالية:
- **[الجزء الثالث: البرمجة المنطقية](03_LOGIC_PROGRAMMING_AR.md)** - تعلم البرمجة المنطقية من الصفر

### 💡 نصائح للممارسة:
1. اكتب أمثلة بنفسك
2. جرب تعديل الأمثلة الموجودة
3. ابدأ بمشاريع صغيرة
4. راجع الوثائق عند الحاجة

**بالتوفيق في رحلتك البرمجية! 🚀**

</div>

