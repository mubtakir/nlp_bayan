# المعاملات الافتراضية والمسماة في لغة بيان
# Default and Named Parameters in Bayan Language

## 📋 نظرة عامة

هذا الدليل يشرح كيفية إضافة دعم **المعاملات الافتراضية** و**المعاملات المسماة** إلى لغة البيان.

---

## 1. المعاملات الافتراضية (Default Parameters)

### الحالة الحالية:
```bayan
# الطريقة الحالية - جميع المعاملات مطلوبة
def greet(name, greeting):
{
    print(greeting + " " + name)
}

greet("أحمد", "مرحبا")  # يجب تمرير كلا المعاملين
```

### الحالة المطلوبة:
```bayan
# مع المعاملات الافتراضية
def greet(name, greeting="مرحبا"):
{
    print(greeting + " " + name)
}

greet("أحمد")                    # مرحبا أحمد
greet("فاطمة", "السلام عليكم")  # السلام عليكم فاطمة
```

### الفوائد:
- مرونة أكبر في استدعاء الدوال
- كود أقل تكراراً
- واجهات برمجية أفضل

---

## 2. المعاملات المسماة (Named Parameters)

### الحالة الحالية:
```bayan
# الطريقة الحالية - ترتيب المعاملات مهم
def create_user(name, age, email):
{
    return { "name": name, "age": age, "email": email }
}

user = create_user("علي", 25, "ali@example.com")
```

### الحالة المطلوبة:
```bayan
# مع المعاملات المسماة
def create_user(name, age, email):
{
    return { "name": name, "age": age, "email": email }
}

# يمكن تمرير المعاملات بأي ترتيب
user1 = create_user(name="علي", age=25, email="ali@example.com")
user2 = create_user(email="ali@example.com", name="علي", age=25)
user3 = create_user("علي", email="ali@example.com", age=25)
```

### الفوائد:
- وضوح أكبر في الكود
- سهولة الصيانة
- تقليل الأخطاء

---

## 3. الدمج بين المعاملات الافتراضية والمسماة

```bayan
def create_user(name, age=18, email="unknown@example.com", active=True):
{
    return {
        "name": name,
        "age": age,
        "email": email,
        "active": active
    }
}

# استدعاءات مختلفة
user1 = create_user("علي")
user2 = create_user("فاطمة", 25)
user3 = create_user("محمد", email="m@example.com")
user4 = create_user(name="سارة", age=30, active=False)
```

---

## 4. خطة التطبيق

### المرحلة 1: تحديث المحلل (Parser)
- تعديل `parse_parameter_list()` لدعم القيم الافتراضية
- تعديل `parse_argument_list()` لدعم المعاملات المسماة

### المرحلة 2: تحديث AST
- إضافة `default_value` إلى `Parameter`
- إضافة `NamedArgument` للمعاملات المسماة

### المرحلة 3: تحديث المترجم
- تحديث `visit_function_call()` لمعالجة المعاملات المسماة
- تحديث ربط المعاملات مع القيم الافتراضية

### المرحلة 4: الاختبار والتوثيق
- كتابة اختبارات شاملة
- تحديث الوثائق

---

## 5. أمثلة الاستخدام

### مثال 1: دالة بسيطة مع قيم افتراضية
```bayan
def power(base, exponent=2):
{
    result = 1
    for i in (range(exponent)) {
        result = result * base
    }
    return result
}

print(power(3))      # 9
print(power(3, 3))   # 27
```

### مثال 2: دالة مع معاملات مسماة
```bayan
def rectangle_area(width, height):
{
    return width * height
}

area1 = rectangle_area(5, 10)
area2 = rectangle_area(width=5, height=10)
area3 = rectangle_area(height=10, width=5)
```

### مثال 3: دمج الميزتين
```bayan
def format_text(text, prefix=">>", suffix="<<", uppercase=False):
{
    if (uppercase) {
        text = text.upper()
    }
    return prefix + " " + text + " " + suffix
}

print(format_text("مرحبا"))
print(format_text("مرحبا", prefix=">>>"))
print(format_text("مرحبا", uppercase=True))
print(format_text("مرحبا", suffix="", prefix=""))
```

---

## 6. ملاحظات مهمة

- المعاملات الافتراضية يجب أن تأتي بعد المعاملات المطلوبة
- المعاملات المسماة يمكن أن تأتي بأي ترتيب
- يمكن خلط المعاملات الموضعية والمسماة
- المعاملات الموضعية يجب أن تأتي قبل المسماة

---

## 7. الحالة الحالية

**الحالة:** ⏳ قيد التطوير  
**الأولوية:** عالية جداً  
**التأثير:** تحسين كبير في سهولة الاستخدام

