# 🧠 الدليل الشامل للنموذج المعرفي في لغة البيان (Unified Cognitive Model)

هذا الدليل يجمع كافة المفاهيم المتقدمة لبناء "عقل" النظام: الشبكات السببية والدلالية، المفاهيم (CSKG)، الأفكار (Ideas)، والديناميكية.

---

## 📑 الفهرس

1.  **المفاهيم الأساسية:** الشبكات السببية vs الدلالية (الخريطة والمحرك).
2.  **لغة التمثيل:** نظام CSKG والثلاثيات الدلالية ("البيت فيه حمام").
3.  **وحدة التفكير:** الفكرة (Idea: شيء + حدث + نتيجة).
4.  **الحياة والزمن:** الكيانات الديناميكية والمنطق التفاعلي.

---

## 1️⃣ المفاهيم الأساسية: الخريطة (الدلالية) والمحرك (السببي)

### 🗺️ الشبكات الدلالية (Semantic Networks) - "الخريطة"
تصف **"ماهية الأشياء"** والبنية الثابتة للمعرفة.
*   **الوظيفة:** التعريف والتصنيف.
*   **السؤال:** ما هذا الشيء؟ (تعريف جامد).
*   **العلاقات:** `is_a` (نوع من), `part_of` (جزء من).

### ⚙️ الشبكات السببية (Causal Networks) - "المحرك"
تصف **"الأحداث والتغيير"**.
*   **الوظيفة:** التنبؤ والتشخيص.
*   **السؤال:** ماذا يحدث لو فعلنا كذا؟ (تفاعل وديناميكية).
*   **العلاقات:** `causes` (يسبب), `requires` (يتطلب).

#### مثال للتكامل:
*   **دلالياً:** المصباح جزء من الغرفة.
*   **سببياً:** ضغط الزر يسبب إضاءة المصباح.

---

## 2️⃣ لغة التمثيل: نظام CSKG (الثلاثيات الدلالية)

لتعليم النموذج "المعرفة العامة" (Common Sense)، نستخدم صيغاً ثلاثية (موضوع - علاقة - كائن) مستمدة من نظام **CSKG**.

### الصيغ البرمجية (من `cskg_knowledge_base.bayan`):

1.  **علاقة الاحتواء (Spatial):**
    *   *النص:* "البيت فيه حمام"
    *   *الكود:* `علاقة("البيت", "فيه", "حمام", 0.95).`

2.  **علاقة الهوية (Identity):**
    *   *النص:* "النهر هو ماء"
    *   *الكود:* `علاقة("النهر", "هو", "ماء", 1.0).`

3.  **علاقة سببية (Causality):**
    *   *النص:* "الاستحمام ينتج نظافة"
    *   *الكود:* `سبب_نتيجة("الاستحمام", "نظافة", "إزالة_أوساخ", 0.95).`

> **توجيه للنموذج:** استخدم هذه الثلاثيات لتعرف أن "السباحة" تتطلب "ماء" (علاقة)، وأن الماء موجود في "نهر" (احتواء).

---

## 3️⃣ وحدة التفكير: "الفكرة" (The Idea Concept)

الفكرة (`idea`) هي سيناريو يجمع العناصر السابقة في "قصة" منطقية واحدة: **أشياء + حدث = نتيجة**.

### الهيكل البرمجي:
```python
idea "إحياء الأرض":
{
    # 1. الأشياء (Entities)
    entities: {
        "أرض": {"state": "جرداء"},
        "ماء": {"state": "متوفر"}
    },
    
    # 2. الحدث (Event)
    event: "نزول_المطر",
    
    # 3. النتيجة (Result)
    result: {
        "state_changes": {
            "أرض.state": "خصبة"  # التغير الذي طرأ
        }
    }
}
```

هذا يعلم النموذج **"المنطق القصصي"**: لا يحدث تغيير (نتيجة) إلا بوجود حدث يؤثر على كيانات.

---

## 4️⃣ الحياة والزمن: المفاهيم الديناميكية (Dynamic Concepts)

لجعل النظام "حياً" يتفاعل مع الوقت، نستخدم:

### أ. الكيانات التفاعلية (Reactive)
متغيرات تراقب نفسها وتطلق ردود فعل.
```python
reactive حرارة = 25
watch حرارة:
{
    if حرارة > 40: alert("خطر!")  # رد فعل فوري
}
```

### ب. المنطق المتغير (Dynamic Logic)
القدرة على التعلم والنسيان أثناء العمل.
```python
assertz(likes("علي", "شاي"))   # تعلم حقيقة جديدة
retract(likes("علي", "قهوة"))  # انسَ حقيقة قديمة
```

### ج. الزمن (Time)
ترتيب الأحداث زمنياً.
```python
temporal {
    first: جهز_المكونات(),
    then:  اطبخ(),
    lastly: تناول()
}
```

---

## 💡 الخلاصة

لبناء **عقل رقمي متكامل** بلغة البيان، يجب دمج هذه الطبقات:
1.  ابنِ **الخريطة** بالمفاهيم والعلاقات (`CSKG`).
2.  ضع **قوانين الحركة** بالشبكات السببية.
3.  غلفها في **أفكار** (`idea`) ليفهم النموذج السيناريوهات.
4.  اجعلها **حية** بالديناميكية والزمن.

---
*هذا الدليل يلخص الملفات السابقة: `CAUSAL_VS_SEMANTIC`, `CSKG_RELATIONS`, `IDEA_CONCEPT`, `DYNAMIC_CONCEPTS`.*
