# 📚 التوثيق الشامل لمشروع لغة البيان
# Comprehensive Bayan Project Documentation

**التاريخ**: 2025-12-07  
**الإصدار**: 2.0+  
**المطور**: باسل يحيى عبدالله

---

## 📋 جدول المحتويات

1. [نظرة عامة](#1-نظرة-عامة)
2. [الأنماط البرمجية](#2-الأنماط-البرمجية)
3. [هيكل المشروع](#3-هيكل-المشروع)
4. [سير تدفق الأوامر](#4-سير-تدفق-الأوامر)
5. [المكونات الأساسية](#5-المكونات-الأساسية)
6. [أنظمة الذكاء الاصطناعي](#6-أنظمة-الذكاء-الاصطناعي)
7. [نظام الملفات](#7-نظام-الملفات)
8. [التشغيل والاختبار](#8-التشغيل-والاختبار)
9. [أمثلة عملية](#9-أمثلة-عملية)
10. [الإحصائيات](#10-الإحصائيات)

---

## 1. نظرة عامة

### ما هي لغة البيان؟

**لغة البيان (Bayan)** هي أول لغة برمجة هجينة حقيقية في العالم تجمع بين:

- ✅ **ثلاثة أنماط برمجية**: إجرائية + كائنية + منطقية
- ✅ **دعم كامل ثنائي اللغة**: عربي + إنجليزي
- ✅ **10+ أنظمة ذكاء اصطناعي** متقدمة
- ✅ **ذكاء اصطناعي قابل للتفسير**: كل قرار له تبرير
- ✅ **نظام رسوم متكامل**: SVG + Canvas + Waves
- ✅ **محرر ويب متكامل** مع معاينة مباشرة

### الإنجازات الرئيسية

| المقياس | القيمة |
|---------|--------|
| الاختبارات الناجحة | 622 |
| الأمثلة العملية | 199 ملف |
| التحديات البرمجية | 11 |
| ملفات التوثيق | 146+ |
| أنظمة AI | 10+ |

---

## 2. الأنماط البرمجية

### 2.1 البرمجة الإجرائية (Imperative)

```bayan
hybrid {
    x = 10
    y = 20
    
    if (x < y) {
        print("x أصغر من y")
    }
    
    for i in range(5) {
        print(i)
    }
}
```

**الكلمات المفتاحية**:
| العربية | الإنجليزية |
|---------|------------|
| إذا | if |
| وإلا | else |
| طالما | while |
| لكل | for |

### 2.2 البرمجة الكائنية (OOP)

```bayan
hybrid {
    class Person: {
        def __init__(self, name, age): {
            self.name = name
            self.age = age
        }
        
        def greet(self): {
            print("مرحباً، أنا " + self.name)
        }
    }
    
    person = Person("أحمد", 25)
    person.greet()
}
```

### 2.3 البرمجة المنطقية (Logic)

```bayan
hybrid {
    # حقائق
    parent("أحمد", "محمد").
    parent("أحمد", "فاطمة").
    
    # قواعد
    sibling(?X, ?Y) :- parent(?P, ?X), parent(?P, ?Y), ?X != ?Y.
    
    # استعلام
    results = query sibling("محمد", ?S)?
}
```

---

## 3. هيكل المشروع

```
bayan_python_ide22/
│
├── 📁 bayan/                    ⭐ المفسر الرئيسي
│   ├── __init__.py
│   ├── main.py                  نقطة الدخول
│   └── bayan/                   51 ملف Python
│       ├── lexer.py             المحلل اللغوي
│       ├── parser.py            المحلل النحوي (205K سطر)
│       ├── traditional_interpreter.py  المفسر (267K سطر)
│       ├── hybrid_interpreter.py       المفسر الهجين
│       ├── logical_engine.py           المحرك المنطقي
│       ├── dual_brain.py               الدماغ المزدوج
│       ├── left_brain.py               الدماغ الأيسر
│       ├── right_brain.py              الدماغ الأيمن
│       ├── entity_engine.py            محرك الكيانات
│       ├── generative_model.py         نموذج GLM
│       ├── word_energy_matrix.py       مصفوفة الطاقة
│       ├── linguistic_equation.py      المعادلات اللغوية
│       ├── mother_equation.py          المعادلة الأم
│       ├── gse.py                      معادلة الشكل العام
│       ├── expert_explorer.py          الخبير-المستكشف
│       ├── istinbat_engine.py          محرك الاستنباط
│       ├── builtins.py                 الدوال المدمجة
│       ├── visualization.py            التصور البصري
│       └── ...
│
├── 📁 ai/                       ⭐ مكتبات AI (23 ملف)
│   ├── ml.bayan                 التعلم الآلي (104K سطر)
│   ├── nlp.bayan                معالجة اللغة (68K سطر)
│   ├── data.bayan               هياكل البيانات
│   ├── lexicon.bayan            المعجم
│   ├── morphology.bayan         علم الصرف
│   ├── conceptual_programs.bayan
│   ├── conceptual_circuits.bayan
│   ├── conceptual_orchestrator.bayan
│   └── conceptual_surface_realizer.bayan
│
├── 📁 nlp_bayan/                ⭐ نظام NLP (81 ملف)
│   ├── core/                    23 ملف أساسي
│   │   ├── integrated_kb.bayan
│   │   ├── thinking_core.bayan
│   │   └── semantic_model.bayan
│   ├── dialogue/
│   ├── reasoning/
│   └── examples/
│
├── 📁 examples/                 ⭐ أمثلة (199 ملف)
│
├── 📁 extensions/               ⭐ الطبقة الوسيطية (9 ملفات)
│   ├── ai_code_assistant.py
│   ├── dialect_adapter.py
│   ├── bayan_tutor.py
│   ├── dialogue_system.py
│   └── equation_visualizer.py
│
├── 📁 tests/                    ⭐ اختبارات (164 ملف)
│
├── 📁 web_ide/                  ⭐ محرر الويب
│   ├── app.py
│   └── templates/
│
├── 📁 docs/                     ⭐ توثيق (146 ملف)
│
└── 📁 gfx/                      نظام الرسوم
    ├── svg.bayan
    ├── waves.bayan
    └── img.bayan
```

---

## 4. سير تدفق الأوامر

### المسار الكامل لتنفيذ كود بيان

```
┌─────────────────────────────────────────────────────┐
│          1. ملف الكود المصدري (.by/.bayan)         │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│              2. main.py (نقطة الدخول)               │
│    - قراءة الملف                                    │
│    - تحديد نوع الكود (hybrid أو تقليدي)            │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│               3. Lexer (lexer.py)                   │
│    - تحليل النص إلى رموز (Tokens)                  │
│    - معالجة الكلمات العربية/الإنجليزية             │
│                                                     │
│    مثال: "x = 10" → [ID('x'), ASSIGN, NUM(10)]     │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│               4. Parser (parser.py)                 │
│    - بناء شجرة AST                                  │
│    - التحقق من الصحة النحوية                       │
│                                                     │
│    مثال: AssignNode(var='x', value=10)             │
└──────────────────────┬──────────────────────────────┘
                       ↓
              ┌────────┴────────┐
              ↓                 ↓
   ┌──────────────────┐  ┌──────────────────┐
   │   كود تقليدي    │  │    كود هجين     │
   └────────┬─────────┘  └────────┬─────────┘
            ↓                     ↓
   ┌──────────────────┐  ┌──────────────────┐
   │   Traditional    │  │     Hybrid       │
   │   Interpreter    │  │   Interpreter    │
   └────────┬─────────┘  └────────┬─────────┘
            │                     │
            │              ┌──────┴──────┐
            │              ↓             ↓
            │     ┌─────────────┐ ┌─────────────┐
            │     │   Logical   │ │    Other    │
            │     │   Engine    │ │   Systems   │
            │     └─────────────┘ └─────────────┘
            │              │             │
            └──────────────┴─────────────┘
                           ↓
┌─────────────────────────────────────────────────────┐
│                   5. النتائج                        │
│    - طباعة المخرجات                                 │
│    - رسوم (SVG/Canvas)                             │
│    - إرجاع القيم                                   │
└─────────────────────────────────────────────────────┘
```

### مثال تفصيلي

**الكود**:
```bayan
hybrid {
    x = 10
    parent("أحمد", "محمد").
    query parent(?P, "محمد")?
}
```

**التدفق**:

1. **Lexer** → `[HYBRID, LBRACE, ID(x), ASSIGN, NUM(10), ...]`

2. **Parser** → 
```
HybridNode(
    statements=[
        AssignNode(var='x', value=10),
        FactNode(pred='parent', args=['أحمد', 'محمد']),
        QueryNode(...)
    ]
)
```

3. **Hybrid Interpreter**:
   - `x = 10` → تخزين في البيئة
   - `parent(...)` → إضافة لقاعدة المعرفة
   - `query` → بحث في قاعدة المعرفة

4. **النتيجة**: `[{'?P': 'أحمد'}]`

---

## 5. المكونات الأساسية

### 5.1 المحلل اللغوي (Lexer)
**الملف**: `bayan/bayan/lexer.py`

يحول النص إلى رموز (Tokens) مع دعم:
- الكلمات المفتاحية العربية والإنجليزية
- المعرّفات العربية
- التعليقات (#)
- السلاسل النصية

### 5.2 المحلل النحوي (Parser)
**الملف**: `bayan/bayan/parser.py` (205,195 سطر)

يبني شجرة AST ويتعامل مع:
- البرمجة الإجرائية (if, for, while)
- البرمجة الكائنية (class, def)
- البرمجة المنطقية (facts, rules, queries)

### 5.3 المفسر الهجين (Hybrid Interpreter)
**الملف**: `bayan/bayan/hybrid_interpreter.py`

ينفذ الكود الهجين ويتكامل مع:
- المحرك المنطقي
- محرك الكيانات
- أنظمة AI المتقدمة

### 5.4 المحرك المنطقي (Logical Engine)
**الملف**: `bayan/bayan/logical_engine.py`

يوفر:
- قاعدة المعرفة (Knowledge Base)
- محرك التوحيد (Unification)
- محرك التراجع (Backtracking)

---

## 6. أنظمة الذكاء الاصطناعي

### 6.1 نظام الدماغ المزدوج (Dual Brain)
**الملفات**: `dual_brain.py`, `left_brain.py`, `right_brain.py`

```
إدخال → الدماغ الأيسر (منطقي) ─┐
      ↓                        ├→ طبقة التكامل → نتيجة
      → الدماغ الأيمن (رياضي) ─┘
```

### 6.2 نموذج اللغة التوليدي (GLM)
**الملفات**: `generative_model.py`, `word_energy_matrix.py`

```
معنى → اختيار حروف → بناء جذر → تطبيق وزن → كلمة
```

- قاعدة بيانات 28 حرف عربي مع دلالاتها
- 9 طرق دلالية للتحليل
- تكامل مع الصرف العربي

### 6.3 محرك الكيانات (Entity Engine)
**الملف**: `entity_engine.py`

نمذجة كيانات ديناميكية مع:
- حالات ضبابية (0..1)
- خصائص ثابتة
- أفعال وتأثيرات

### 6.4 المعادلات اللغوية
**الملف**: `linguistic_equation.py`

```
"محمد أكل تفاحة"
    ↓
محمد(فاعل) + تفاحة(مفعول) + أكل = 
    [محمد: جوع↓, طاقة↑] + [تفاحة: موجود=False]
```

### 6.5 أنظمة أخرى
- **المعادلة الأم**: `mother_equation.py`
- **GSE**: `gse.py`, `gse_fitting.py`
- **الخبير-المستكشف**: `expert_explorer.py`
- **محرك الاستنباط**: `istinbat_engine.py`
- **الشبكات السببية**: `causal_semantic_network.py`

---

## 7. نظام الملفات

### امتدادات الملفات

| الامتداد | الوصف |
|----------|-------|
| `.by` | سكريبتات قابلة للتشغيل |
| `.bayan` | مكتبات ووحدات |
| `.py` | كود Python |
| `.md` | توثيق Markdown |

### المجلدات الرئيسية

| المجلد | الوظيفة | الملفات |
|--------|---------|---------|
| `bayan/bayan/` | المفسر | 51 |
| `ai/` | مكتبات AI | 23 |
| `nlp_bayan/` | NLP | 81 |
| `examples/` | أمثلة | 199 |
| `tests/` | اختبارات | 164 |
| `docs/` | توثيق | 146 |

---

## 8. التشغيل والاختبار

### تشغيل ملف بيان
```bash
python -m bayan examples/hello.by
```

### الوضع التفاعلي
```bash
python -m bayan
>>> x = 10
>>> print(x)
10
```

### محرر الويب
```bash
python web_ide/app.py
# افتح: http://127.0.0.1:5001/ide
```

### تشغيل الاختبارات
```bash
python -m pytest tests/ -v
```

---

## 9. أمثلة عملية

### مثال هجين كامل

```bayan
hybrid {
    # OOP
    class Student: {
        def __init__(self, name, grade): {
            self.name = name
            self.grade = grade
            assertz(student(name, grade))
        }
    }
    
    # Imperative
    students = [
        Student("أحمد", 85),
        Student("فاطمة", 95)
    ]
    
    # Logic
    excellent(?N) :- student(?N, ?G), ?G >= 90.
    results = query excellent(?Name)?
    
    for r in (results) {
        print("متفوق: " + r["?Name"])
    }
}
```

---

## 10. الإحصائيات

### الكود المصدري

| الملف | الأسطر |
|-------|--------|
| parser.py | 205,195 |
| traditional_interpreter.py | 267,447 |
| ml.bayan | 104,130 |
| nlp.bayan | 68,623 |
| hybrid_interpreter.py | 48,828 |
| logical_engine.py | 46,336 |

### التحديات البرمجية

| التحدي | الصعوبة |
|--------|---------|
| Ultimate Challenge | خبير |
| Credit Analysis | متقدم |
| Smart City | متقدم |
| Movie Recommendation | متوسط |

---

## 📚 مصادر إضافية

- [README.md](README.md) - الدليل الرئيسي
- [PROJECT_ARCHITECTURE_GUIDE.md](PROJECT_ARCHITECTURE_GUIDE.md) - دليل المعمارية
- [BAYAN_FOR_AI_MODELS.md](BAYAN_FOR_AI_MODELS.md) - دليل النماذج اللغوية
- [QUICKSTART.md](QUICKSTART.md) - البدء السريع

---

**تم إنشاء هذا التوثيق بتاريخ**: 2025-12-07
