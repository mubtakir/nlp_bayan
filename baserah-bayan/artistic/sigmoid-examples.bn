/**
 * ═══════════════════════════════════════════════════════════════════════
 * أمثلة محرك الرسم بالسيغمويد - Sigmoid Drawing Examples
 * ═══════════════════════════════════════════════════════════════════════
 * 
 * أمثلة عملية لاستخدام النظام الثوري للرسم بالسيغمويد
 * Practical examples using the revolutionary sigmoid drawing system
 * 
 * @author Baserah AI System
 * @version 1.0.0
 * @date 2025-10-26
 * ═══════════════════════════════════════════════════════════════════════
 */

import { SigmoidShapeEquation } from "./sigmoid-shape-equation.bn";
import { SigmoidDrawingEngine } from "./sigmoid-drawing-engine.bn";

// ═══════════════════════════════════════════════════════════════════════
// مثال 1: دالة الخطوة (Step Function)
// ═══════════════════════════════════════════════════════════════════════

function example1_StepFunction(): SigmoidShapeEquation {
    /**
     * دالة خطوة بسيطة: من 0 إلى 1 عند x=0
     * Simple step function: from 0 to 1 at x=0
     */
    console.log("\n📊 مثال 1: دالة الخطوة");
    
    let equation = new SigmoidShapeEquation("Step Function", 200);
    
    // انتقال حاد من 0 إلى 1
    equation.addSigmoidTerm(
        1.0,      // scale: الارتفاع
        10.0,     // sharpness: حدة عالية
        0.0,      // center: عند x=0
        1,        // cutoff: n=1 (ناعم نسبياً)
        1.0,      // weight
        "خطوة من 0 إلى 1"
    );
    
    console.log(`   المعادلة: ${equation.toEquationString()}`);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 2: دالة البوابة (Gate Function)
// ═══════════════════════════════════════════════════════════════════════

function example2_GateFunction(): SigmoidShapeEquation {
    /**
     * دالة بوابة: مستطيل من -2 إلى 2 بارتفاع 1
     * Gate function: rectangle from -2 to 2 with height 1
     */
    console.log("\n📊 مثال 2: دالة البوابة");
    
    let equation = new SigmoidShapeEquation("Gate Function", 200);
    
    // بوابة من -2 إلى 2
    equation.addGateTerm(
        1.0,      // height: الارتفاع
        -2.0,     // start: البداية
        2.0,      // end: النهاية
        15.0      // sharpness: الحدة
    );
    
    console.log(`   المعادلة: ${equation.toEquationString()}`);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 3: موجة سلسة (Smooth Wave)
// ═══════════════════════════════════════════════════════════════════════

function example3_SmoothWave(): SigmoidShapeEquation {
    /**
     * موجة سلسة من عدة انتقالات
     * Smooth wave from multiple transitions
     */
    console.log("\n📊 مثال 3: موجة سلسة");
    
    let equation = new SigmoidShapeEquation("Smooth Wave", 300);
    
    // مكون خطي أساسي
    equation.setLinearComponent(0.0, 0.5);
    
    // انتقالات متعددة
    equation.addSmoothTransition(0.0, 1.0, -3.0, 0.5);
    equation.addSmoothTransition(1.0, 0.0, -1.0, 0.5);
    equation.addSmoothTransition(0.0, 1.0, 1.0, 0.5);
    equation.addSmoothTransition(1.0, 0.0, 3.0, 0.5);
    
    console.log(`   عدد الحدود: ${equation.sigmoidTerms.length}`);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 4: منحنى S (S-Curve)
// ═══════════════════════════════════════════════════════════════════════

function example4_SCurve(): SigmoidShapeEquation {
    /**
     * منحنى S كلاسيكي
     * Classic S-curve
     */
    console.log("\n📊 مثال 4: منحنى S");
    
    let equation = new SigmoidShapeEquation("S-Curve", 200);
    
    // سيغمويد قياسي
    equation.addSigmoidTerm(
        2.0,      // scale: من -1 إلى 1
        2.0,      // sharpness: متوسط
        0.0,      // center: عند المركز
        1,        // cutoff: n=1 (ناعم)
        1.0,
        "منحنى S كلاسيكي"
    );
    
    // إزاحة لأسفل
    equation.setLinearComponent(0.0, -1.0);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 5: نبضة (Pulse)
// ═══════════════════════════════════════════════════════════════════════

function example5_Pulse(): SigmoidShapeEquation {
    /**
     * نبضة حادة (شكل جرس)
     * Sharp pulse (bell shape)
     */
    console.log("\n📊 مثال 5: نبضة");
    
    let equation = new SigmoidShapeEquation("Pulse", 300);
    
    // نبضة باستخدام n=2 (زوجي)
    equation.addSigmoidTerm(
        1.0,      // scale
        5.0,      // sharpness: عالي
        0.0,      // center
        2,        // cutoff: n=2 (شكل جرس)
        1.0,
        "نبضة مركزية"
    );
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 6: خط مائل مع انتقالات (Sloped Line with Transitions)
// ═══════════════════════════════════════════════════════════════════════

function example6_SlopedWithTransitions(): SigmoidShapeEquation {
    /**
     * خط مائل مع انتقالات حادة
     * Sloped line with sharp transitions
     */
    console.log("\n📊 مثال 6: خط مائل مع انتقالات");
    
    let equation = new SigmoidShapeEquation("Sloped with Transitions", 300);
    
    // مكون خطي: خط مائل
    equation.setLinearComponent(0.3, 0.0);
    
    // انتقال حاد صعوداً
    equation.addSharpTransition(0.0, 1.5, -2.0, 20.0, 3);
    
    // انتقال حاد نزولاً
    equation.addSharpTransition(0.0, -1.5, 2.0, 20.0, 3);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 7: سلم (Staircase)
// ═══════════════════════════════════════════════════════════════════════

function example7_Staircase(): SigmoidShapeEquation {
    /**
     * سلم من عدة خطوات
     * Staircase from multiple steps
     */
    console.log("\n📊 مثال 7: سلم");
    
    let equation = new SigmoidShapeEquation("Staircase", 400);
    
    // خطوات متعددة
    for (let i = 0; i < 5; i++) {
        let center = -4.0 + i * 2.0;
        equation.addSigmoidTerm(
            0.5,      // scale: ارتفاع كل خطوة
            15.0,     // sharpness: حاد
            center,   // center: موقع الخطوة
            1,
            1.0,
            `خطوة ${i + 1}`
        );
    }
    
    console.log(`   عدد الخطوات: 5`);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 8: شكل معقد (Complex Shape)
// ═══════════════════════════════════════════════════════════════════════

function example8_ComplexShape(): SigmoidShapeEquation {
    /**
     * شكل معقد يجمع كل المكونات
     * Complex shape combining all components
     */
    console.log("\n📊 مثال 8: شكل معقد");
    
    let equation = new SigmoidShapeEquation("Complex Shape", 500);
    
    // مكون خطي أساسي
    equation.setLinearComponent(0.1, 0.0);
    
    // بوابة
    equation.addGateTerm(1.0, -5.0, -3.0, 10.0);
    
    // انتقال سلس
    equation.addSmoothTransition(0.0, 1.5, -1.0, 0.3);
    
    // نبضة
    equation.addSigmoidTerm(0.8, 5.0, 1.0, 2, 1.0, "نبضة");
    
    // انتقال حاد
    equation.addSharpTransition(0.0, -1.0, 3.5, 15.0, 3);
    
    console.log(`   عدد الحدود: ${equation.sigmoidTerms.length}`);
    
    return equation;
}

// ═══════════════════════════════════════════════════════════════════════
// مثال 9: منحنى بارامتري - دائرة (Parametric Circle)
// ═══════════════════════════════════════════════════════════════════════

function example9_ParametricCircle(): object {
    /**
     * دائرة باستخدام معادلات بارامترية
     * Circle using parametric equations
     * 
     * x(t) = r * cos(t) ≈ r * (1 - 2*σ(t))
     * y(t) = r * sin(t) ≈ r * (σ(t) - σ(t-π))
     */
    console.log("\n📊 مثال 9: دائرة بارامترية");
    
    // معادلة X (تقريب cos)
    let xEquation = new SigmoidShapeEquation("Circle X", 200);
    xEquation.setLinearComponent(0.0, 2.0);
    xEquation.addSigmoidTerm(-4.0, 2.0, 0.0, 1, 1.0, "cos approximation");
    
    // معادلة Y (تقريب sin)
    let yEquation = new SigmoidShapeEquation("Circle Y", 200);
    yEquation.addSigmoidTerm(2.0, 2.0, -1.57, 1, 1.0, "sin up");
    yEquation.addSigmoidTerm(-2.0, 2.0, 1.57, 1, 1.0, "sin down");
    
    return {
        x: xEquation,
        y: yEquation,
        description: "دائرة بارامترية"
    };
}

// ═══════════════════════════════════════════════════════════════════════
// تشغيل جميع الأمثلة
// ═══════════════════════════════════════════════════════════════════════

function runAllExamples(): void {
    console.log("═══════════════════════════════════════════════════════");
    console.log("🌊 أمثلة محرك الرسم بالسيغمويد");
    console.log("═══════════════════════════════════════════════════════");
    
    let examples = [
        example1_StepFunction(),
        example2_GateFunction(),
        example3_SmoothWave(),
        example4_SCurve(),
        example5_Pulse(),
        example6_SlopedWithTransitions(),
        example7_Staircase(),
        example8_ComplexShape()
    ];
    
    console.log("\n✅ تم إنشاء 8 أمثلة بنجاح");
    console.log("═══════════════════════════════════════════════════════");
}

export {
    example1_StepFunction,
    example2_GateFunction,
    example3_SmoothWave,
    example4_SCurve,
    example5_Pulse,
    example6_SlopedWithTransitions,
    example7_Staircase,
    example8_ComplexShape,
    example9_ParametricCircle,
    runAllExamples
};

