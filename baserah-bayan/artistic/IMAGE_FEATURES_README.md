# نظام استخلاص الخصائص المشتركة من الصور
# Image Common Features Extraction System

## 📋 نظرة عامة | Overview

نظام متقدم لاستخلاص الخصائص المشتركة من مجموعة صور لنفس الكائن، مع القدرة على تجاوز الاختلافات غير الجوهرية.

### الفكرة الأساسية

عند إدخال مجموعة صور لنفس الكائن (مثلاً: قطة)، قد تختلف الصور في:
- 🖼️ **الخلفية**: حديقة، غرفة، شارع
- 📏 **الحجم**: صغير، متوسط، كبير
- 📍 **الموقع**: يمين، يسار، وسط
- 🎨 **اللون**: أبيض، أسود، رمادي
- 🔄 **الزاوية**: أمامي، جانبي، خلفي
- 💡 **الإضاءة**: ساطع، خافت، ظل

**النظام يستخلص العوامل المشتركة = الشكل الحقيقي للكائن**

---

## 🏗️ البنية المعمارية | Architecture

```
مجموعة صور
    ↓
استنباط معادلات لكل صورة (Sigmoid Inference)
    ↓
استخلاص ميزات من كل صورة
    ↓
مقارنة الميزات وإيجاد المشتركات
    ↓
تطبيق خيارات التجاوز
    ↓
تصفية حسب نسبة السماح
    ↓
الشكل النهائي المستخلص
```

---

## 📁 الملفات | Files

### 1. `image-common-features-extractor.bn` (686 سطر)

**الوصف**: النظام الرئيسي لاستخلاص الخصائص المشتركة

**المكونات**:
- **7 تعدادات (Enums)**:
  - `InvarianceOption`: خيارات التجاوز (7 خيارات)
  - `FeatureType`: أنواع الميزات (6 أنواع)
  - `ConfidenceLevel`: مستويات الثقة (5 مستويات)

- **5 فئات (Classes)**:
  - `InputImage`: صورة مدخلة
  - `CommonFeature`: ميزة مشتركة
  - `ExtractionSettings`: إعدادات الاستخلاص
  - `ExtractionResult`: نتيجة الاستخلاص
  - `ImageCommonFeaturesExtractor`: المستخلص الرئيسي

**الوظائف الرئيسية**:
```javascript
addImage(image: InputImage): void
extract(): ExtractionResult
getStatistics(): object
getReport(): string
```

---

### 2. `image-features-examples.bn` (300+ سطر)

**الوصف**: أمثلة عملية شاملة

**الأمثلة**:
1. استخلاص شكل قطة من صور متعددة
2. استخلاص شكل سيارة مع تجاوز اللون
3. استخلاص شكل وجه إنسان
4. استخلاص شكل شجرة مع تجاوز الحجم
5. مقارنة بين إعدادات مختلفة
6. استخدام أنواع ميزات محددة

---

### 3. `image-features-demo.html` (400+ سطر)

**الوصف**: واجهة ويب تفاعلية

**الميزات**:
- ✅ رفع صور متعددة (Drag & Drop)
- ✅ إعدادات قابلة للتخصيص
- ✅ خيارات التجاوز (7 خيارات)
- ✅ اختيار أنواع الميزات
- ✅ عرض النتائج في الوقت الفعلي
- ✅ إحصائيات مباشرة
- ✅ تصميم عصري متجاوب

---

## 🔄 خيارات التجاوز | Invariance Options

### 1. تجاوز الموقع (Position Invariance)
```javascript
settings.enableInvariance(InvarianceOption.POSITION);
```
- يتجاهل موقع الكائن في الصورة
- مفيد عندما يكون الكائن في مواقع مختلفة

### 2. تجاوز الحجم (Scale Invariance)
```javascript
settings.enableInvariance(InvarianceOption.SCALE);
```
- يتجاهل حجم الكائن
- مفيد عندما تكون الصور بأحجام مختلفة

### 3. تجاوز اللون (Color Invariance)
```javascript
settings.enableInvariance(InvarianceOption.COLOR);
```
- يتجاهل لون الكائن
- **تعطيله** عندما تريد لون محدد (مثلاً: قطة بيضاء فقط)

### 4. تجاوز الدوران (Rotation Invariance)
```javascript
settings.enableInvariance(InvarianceOption.ROTATION);
```
- يتجاهل زاوية دوران الكائن
- مفيد للأشكال المصورة من زوايا مختلفة

### 5. تجاوز الإضاءة (Illumination Invariance)
```javascript
settings.enableInvariance(InvarianceOption.ILLUMINATION);
```
- يتجاهل اختلافات الإضاءة
- مفيد للصور المأخوذة في ظروف إضاءة مختلفة

### 6. تجاوز المنظور (Perspective Invariance)
```javascript
settings.enableInvariance(InvarianceOption.PERSPECTIVE);
```
- يتجاهل اختلافات المنظور
- مفيد للصور المأخوذة من مسافات مختلفة

### 7. تجاوز الضوضاء (Noise Invariance)
```javascript
settings.enableInvariance(InvarianceOption.NOISE);
```
- يتجاهل الضوضاء في الصور
- مفيد للصور ذات الجودة المنخفضة

---

## 🎯 أنواع الميزات | Feature Types

### 1. الميزات الهندسية (Geometric)
- الانحناء (Curvature)
- التماثل (Symmetry)
- نسبة الطول/العرض (Aspect Ratio)
- الاستطالة (Compactness)

### 2. الميزات الطوبولوجية (Topological)
- هل الشكل مغلق؟ (Is Closed)
- عدد تقاطعات الصفر (Zero Crossings)

### 3. الميزات النسيجية (Textural)
- التعقيد (Complexity)
- الانتروبيا (Entropy)

### 4. الميزات الطيفية (Spectral)
- الدورية (Periodicity)

### 5. الميزات البنيوية (Structural)
- عدد النقاط القصوى (Extrema Count)
- نقاط الانعطاف (Inflection Points)

### 6. الميزات الإحصائية (Statistical)
- التباين (Variance)
- المساحة (Area)
- المحيط (Perimeter)

---

## 💡 أمثلة الاستخدام | Usage Examples

### مثال 1: استخلاص شكل قطة بيضاء

```javascript
import { ImageCommonFeaturesExtractor, InputImage, InvarianceOption } from "./image-common-features-extractor.bn";

// إنشاء المستخلص
const extractor = new ImageCommonFeaturesExtractor("cat_extractor");

// الإعدادات
extractor.settings.toleranceRate = 0.2;           // 20% سماح
extractor.settings.minOccurrenceRate = 0.7;       // 70% حد أدنى

// تفعيل خيارات التجاوز
extractor.settings.enableInvariance(InvarianceOption.POSITION);
extractor.settings.enableInvariance(InvarianceOption.SCALE);
extractor.settings.enableInvariance(InvarianceOption.ROTATION);
extractor.settings.enableInvariance(InvarianceOption.ILLUMINATION);

// عدم تجاوز اللون (نريد قطة بيضاء فقط)
// extractor.settings.disableInvariance(InvarianceOption.COLOR);

// إضافة صور
const cat1 = new InputImage("cat_001", imageData1, 800, 600);
const cat2 = new InputImage("cat_002", imageData2, 1024, 768);
const cat3 = new InputImage("cat_003", imageData3, 640, 480);

extractor.addImage(cat1);
extractor.addImage(cat2);
extractor.addImage(cat3);

// استخلاص
const result = extractor.extract();

// عرض النتائج
console.log(result.report);
console.log(`الثقة: ${(result.overallConfidence * 100).toFixed(1)}%`);
console.log(`الميزات المشتركة: ${result.commonFeatures.length}`);
```

### مثال 2: استخلاص شكل سيارة (أي لون)

```javascript
const extractor = new ImageCommonFeaturesExtractor("car_extractor");

// تفعيل تجاوز اللون (نريد أي لون)
extractor.settings.enableInvariance(InvarianceOption.COLOR);
extractor.settings.enableInvariance(InvarianceOption.POSITION);
extractor.settings.enableInvariance(InvarianceOption.SCALE);

// إضافة صور سيارات بألوان مختلفة
extractor.addImage(new InputImage("car_red", redCarData, 1920, 1080));
extractor.addImage(new InputImage("car_blue", blueCarData, 1920, 1080));
extractor.addImage(new InputImage("car_black", blackCarData, 1600, 900));

const result = extractor.extract();
```

### مثال 3: التركيز على ميزات محددة

```javascript
const extractor = new ImageCommonFeaturesExtractor("face_extractor");

// تفعيل أنواع ميزات محددة فقط
extractor.settings.featureTypes.clear();
extractor.settings.enableFeatureType(FeatureType.GEOMETRIC);
extractor.settings.enableFeatureType(FeatureType.STRUCTURAL);
extractor.settings.enableFeatureType(FeatureType.TOPOLOGICAL);

// إضافة صور وجوه
for (let i = 1; i <= 10; i++) {
    extractor.addImage(new InputImage(`face_${i}`, faceData[i], 512, 512));
}

const result = extractor.extract();
```

---

## 📊 الإعدادات | Settings

### نسبة السماح (Tolerance Rate)
```javascript
settings.toleranceRate = 0.15;  // 15% سماح
```
- **القيمة**: 0.0 - 1.0
- **الوصف**: نسبة الفرق المقبولة بين الميزات
- **منخفض (0.05)**: صارم جداً، ميزات متطابقة تقريباً
- **متوسط (0.15)**: توازن جيد
- **عالي (0.3)**: متساهل، يقبل اختلافات كبيرة

### الحد الأدنى لنسبة الظهور (Min Occurrence Rate)
```javascript
settings.minOccurrenceRate = 0.7;  // 70%
```
- **القيمة**: 0.0 - 1.0
- **الوصف**: الحد الأدنى لنسبة ظهور الميزة في الصور
- **عالي (0.9)**: الميزة يجب أن تظهر في 90% من الصور
- **متوسط (0.6)**: الميزة يجب أن تظهر في 60% من الصور
- **منخفض (0.4)**: الميزة يجب أن تظهر في 40% من الصور

---

## 🎯 مستويات الثقة | Confidence Levels

| المستوى | النسبة | الوصف |
|---------|--------|-------|
| **VERY_HIGH** | > 90% | ثقة عالية جداً - الميزة موجودة في معظم الصور |
| **HIGH** | 70-90% | ثقة عالية - الميزة موجودة في أغلب الصور |
| **MEDIUM** | 50-70% | ثقة متوسطة - الميزة موجودة في نصف الصور |
| **LOW** | 30-50% | ثقة منخفضة - الميزة موجودة في بعض الصور |
| **VERY_LOW** | < 30% | ثقة منخفضة جداً - الميزة نادرة |

---

## 🔍 آلية العمل | How It Works

### الخطوة 1: استنباط المعادلات
```
لكل صورة:
  - استخدام SigmoidInferenceEngine
  - استنباط معادلة الشكل
  - حساب الميزات
```

### الخطوة 2: استخلاص الميزات
```
لكل صورة:
  - استخلاص الميزات الهندسية
  - استخلاص الميزات الطوبولوجية
  - استخلاص الميزات النسيجية
  - استخلاص الميزات الطيفية
  - استخلاص الميزات البنيوية
  - استخلاص الميزات الإحصائية
```

### الخطوة 3: إيجاد المشتركات
```
لكل ميزة:
  - حساب القيمة المتوسطة
  - حساب التباين
  - تطبيق خيارات التجاوز
  - حساب نسبة الظهور
```

### الخطوة 4: التصفية
```
تصفية الميزات حسب:
  - نسبة الظهور >= minOccurrenceRate
  - التباين <= toleranceRate
```

### الخطوة 5: الاستخلاص النهائي
```
- بناء معادلة الشكل النهائي
- حساب الثقة الإجمالية
- توليد التقرير
```

---

## 📈 الإحصائيات | Statistics

```javascript
const stats = extractor.getStatistics();

console.log(stats);
// {
//   totalExtractions: 5,
//   totalImages: 25,
//   successfulExtractions: 4,
//   averageConfidence: 0.78,
//   successRate: 0.8
// }
```

---

## 🎨 التكامل مع الأنظمة الأخرى

### مع محرك الاستنباط بالسيغمويد
```javascript
import { SigmoidInferenceEngine } from "./sigmoid-inference-engine.bn";

const extractor = new ImageCommonFeaturesExtractor("extractor");
// المستخلص يستخدم SigmoidInferenceEngine داخلياً
```

### مع معادلة الشكل بالسيغمويد
```javascript
const result = extractor.extract();

if (result.extractedShape) {
    // استخدام الشكل المستخلص
    const shape = result.extractedShape;
    // رسم الشكل، تحليله، إلخ
}
```

---

## 🚀 حالات الاستخدام | Use Cases

### 1. التعرف على الأشكال
- استخلاص شكل كائن من صور متعددة
- بناء قاعدة بيانات أشكال

### 2. تصنيف الصور
- تحديد الخصائص المميزة لفئة معينة
- التمييز بين الفئات المختلفة

### 3. البحث بالصور
- إيجاد صور مشابهة
- البحث عن كائن معين

### 4. التعلم الآلي
- استخلاص ميزات للتدريب
- تحسين دقة النماذج

### 5. معالجة الصور
- إزالة الخلفيات
- تحسين جودة الصور

---

## 🎯 أفضل الممارسات | Best Practices

### 1. عدد الصور
- **الحد الأدنى**: 3-5 صور
- **الموصى به**: 10-20 صورة
- **الأمثل**: 20+ صورة

### 2. جودة الصور
- استخدم صور واضحة
- تجنب الصور المشوشة جداً
- تأكد من وضوح الكائن

### 3. تنوع الصور
- صور من زوايا مختلفة
- إضاءات مختلفة
- خلفيات متنوعة

### 4. الإعدادات
- ابدأ بإعدادات متوسطة
- اضبط حسب النتائج
- جرب خيارات مختلفة

---

## 📝 ملاحظات | Notes

### الأداء
- الاستخلاص: < 2 ثانية لـ 10 صور
- الاستنباط: < 100ms لكل صورة
- التصفية: < 50ms

### القيود
- يعمل بشكل أفضل مع أشكال واضحة
- قد يحتاج ضبط للأشكال المعقدة جداً
- الدقة تعتمد على جودة الصور

---

## 🎉 الخلاصة | Conclusion

نظام **استخلاص الخصائص المشتركة من الصور** يوفر:

✅ **استخلاص ذكي** للخصائص المشتركة
✅ **خيارات تجاوز** مرنة وقابلة للتخصيص
✅ **أنواع ميزات** متعددة ومتنوعة
✅ **إعدادات دقيقة** للتحكم الكامل
✅ **نتائج موثوقة** مع مستويات ثقة
✅ **واجهة تفاعلية** سهلة الاستخدام

**"استخلاص الشكل الحقيقي من وسط الاختلافات!"** 🔍✨

