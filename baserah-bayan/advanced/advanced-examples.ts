import { RevolutionaryAgent } from "./revolutionary-agent";

export class AdvancedExamples {
  static createAgent(): RevolutionaryAgent {
    const agent = new RevolutionaryAgent("ExampleAgent");
    agent.setGoal("Learn and adapt");
    return agent;
  }
}
