/**
 * أمثلة الأنظمة المتقدمة - الجزء 2
 * نظام بصيرة AI
 * 
 * 📚 8 أمثلة عملية شاملة
 * 🎯 تغطي جميع الأنظمة المتقدمة الإضافية
 * 
 * المطور: باسل يحيى عبدالله
 */

import { 
    SpecializedKnowledgeIntegrationSystem,
    KnowledgeDomain,
    MathematicalKnowledgeSystem,
    PhysicalKnowledgeSystem
} from './specialized-knowledge-systems.bn';

import {
    EmotionalIntelligenceSystem,
    EmotionType,
    EmpathyLevel
} from './emotional-intelligence.bn';

import {
    NLUNLGIntegrationSystem,
    ResponseStyle,
    IntentType
} from './nlu-nlg-system.bn';

// ==================== الأمثلة ====================

/**
 * مثال 1: استخدام نظام المعرفة الرياضية
 */
function example1_MathematicalKnowledge(): void {
    console.log("=== مثال 1: نظام المعرفة الرياضية ===\n");
    
    let mathSystem = new MathematicalKnowledgeSystem();
    
    // حل معادلة تربيعية: x² - 5x + 6 = 0
    let solution = mathSystem.solveEquation("quadratic", { a: 1, b: -5, c: 6 });
    
    console.log("حل المعادلة التربيعية x² - 5x + 6 = 0:");
    console.log(JSON.stringify(solution, null, 2));
    
    // إضافة معادلة جديدة
    mathSystem.addEquation(
        "linear",
        "ax + b = 0",
        "المعادلة الخطية البسيطة"
    );
    
    console.log("\nإحصائيات النظام الرياضي:");
    console.log(`- إجمالي المعرفة: ${mathSystem.knowledgeBase.length}`);
    console.log(`- عدد المعادلات: ${Object.keys(mathSystem.equations).length}`);
    console.log(`- عدد النظريات: ${Object.keys(mathSystem.theorems).length}`);
}

/**
 * مثال 2: استخدام نظام المعرفة الفيزيائية
 */
function example2_PhysicalKnowledge(): void {
    console.log("\n=== مثال 2: نظام المعرفة الفيزيائية ===\n");
    
    let physicsSystem = new PhysicalKnowledgeSystem();
    
    // حساب القوة: F = ma
    let force = physicsSystem.calculateForce(10, 5);
    
    console.log("حساب القوة (F = ma):");
    console.log(JSON.stringify(force, null, 2));
    
    console.log("\nإحصائيات النظام الفيزيائي:");
    console.log(`- إجمالي المعرفة: ${physicsSystem.knowledgeBase.length}`);
    console.log(`- عدد القوانين: ${Object.keys(physicsSystem.laws).length}`);
    console.log(`- عدد الثوابت: ${Object.keys(physicsSystem.constants).length}`);
}

/**
 * مثال 3: نظام المعرفة المتخصصة المتكامل
 */
function example3_IntegratedSpecializedKnowledge(): void {
    console.log("\n=== مثال 3: نظام المعرفة المتخصصة المتكامل ===\n");
    
    let system = new SpecializedKnowledgeIntegrationSystem();
    
    // استعلام عن معرفة رياضية
    let mathQuery = system.queryKnowledge(KnowledgeDomain.MATHEMATICAL, "فيثاغورس");
    console.log("استعلام رياضي:");
    console.log(JSON.stringify(mathQuery, null, 2));
    
    // إحصائيات شاملة
    let stats = system.getStatistics();
    console.log("\nإحصائيات شاملة:");
    console.log(JSON.stringify(stats, null, 2));
    
    // مثال سريع
    let quickExample = system.quickExample();
    console.log("\nمثال سريع:");
    console.log(JSON.stringify(quickExample, null, 2));
}

/**
 * مثال 4: التعرف على المشاعر
 */
function example4_EmotionRecognition(): void {
    console.log("\n=== مثال 4: التعرف على المشاعر ===\n");
    
    let eiSystem = new EmotionalIntelligenceSystem();
    
    // نص حزين
    let sadText = "أنا حزين جداً اليوم، أشعر بالاكتئاب";
    let sadResult = eiSystem.processEmotionalInput(sadText);
    
    console.log("تحليل نص حزين:");
    console.log(`النص: "${sadText}"`);
    console.log(`المشاعر المكتشفة: ${sadResult.recognizedEmotion.type}`);
    console.log(`الشدة: ${sadResult.recognizedEmotion.intensity.toFixed(2)}`);
    console.log(`رد التعاطف: ${sadResult.empathyResponse.message}`);
    
    // نص سعيد
    let joyText = "أنا سعيد جداً ومبسوط! يوم رائع";
    let joyResult = eiSystem.processEmotionalInput(joyText);
    
    console.log("\nتحليل نص سعيد:");
    console.log(`النص: "${joyText}"`);
    console.log(`المشاعر المكتشفة: ${joyResult.recognizedEmotion.type}`);
    console.log(`الشدة: ${joyResult.recognizedEmotion.intensity.toFixed(2)}`);
    console.log(`رد التعاطف: ${joyResult.empathyResponse.message}`);
}

/**
 * مثال 5: إدارة المشاعر
 */
function example5_EmotionManagement(): void {
    console.log("\n=== مثال 5: إدارة المشاعر ===\n");
    
    let eiSystem = new EmotionalIntelligenceSystem();
    
    // معالجة مشاعر غضب قوية
    let angerText = "أنا غاضب جداً ومنزعج للغاية!";
    let result = eiSystem.processEmotionalInput(angerText);
    
    console.log("معالجة مشاعر الغضب:");
    console.log(`النص: "${angerText}"`);
    console.log(`المشاعر: ${result.recognizedEmotion.type}`);
    console.log(`الشدة الأصلية: ${(result.recognizedEmotion.intensity + (result.management?.reduction || 0)).toFixed(2)}`);
    
    if (result.management) {
        console.log(`\nالإدارة:`);
        console.log(`- الاستراتيجية: ${result.management.strategy}`);
        console.log(`- الشدة الجديدة: ${result.management.newIntensity.toFixed(2)}`);
        console.log(`- التخفيض: ${result.management.reduction.toFixed(2)}`);
        console.log(`- الفعالية: ${(result.management.effectiveness * 100).toFixed(0)}%`);
    }
    
    console.log(`\nالتوصية: ${result.understanding.recommendation}`);
}

/**
 * مثال 6: فهم اللغة الطبيعية (NLU)
 */
function example6_NaturalLanguageUnderstanding(): void {
    console.log("\n=== مثال 6: فهم اللغة الطبيعية ===\n");
    
    let nluNlgSystem = new NLUNLGIntegrationSystem();
    
    // تحليل سؤال
    let question = "ما هو الطقس اليوم في القاهرة؟";
    let analysis = nluNlgSystem.nluSystem.analyzeText(question);
    
    console.log(`النص: "${question}"`);
    console.log(`النية: ${analysis.intent.type}`);
    console.log(`الثقة: ${(analysis.intent.confidence * 100).toFixed(0)}%`);
    console.log(`عدد الكلمات: ${analysis.wordCount}`);
    console.log(`التعقيد: ${analysis.complexity}`);
    console.log(`المشاعر: ${analysis.sentiment.polarity} (${analysis.sentiment.score.toFixed(2)})`);
    
    if (analysis.entities.length > 0) {
        console.log(`\nالكيانات المستخرجة:`);
        for (let entity of analysis.entities) {
            console.log(`- ${entity.text} (${entity.type})`);
        }
    }
}

/**
 * مثال 7: توليد اللغة الطبيعية (NLG)
 */
function example7_NaturalLanguageGeneration(): void {
    console.log("\n=== مثال 7: توليد اللغة الطبيعية ===\n");
    
    let nluNlgSystem = new NLUNLGIntegrationSystem();
    
    // توليد ردود بأساليب مختلفة
    console.log("توليد ردود تحية بأساليب مختلفة:\n");
    
    let formal = nluNlgSystem.nlgSystem.generateResponse(IntentType.GREETING, ResponseStyle.FORMAL);
    console.log(`رسمي: ${formal.response}`);
    
    let informal = nluNlgSystem.nlgSystem.generateResponse(IntentType.GREETING, ResponseStyle.INFORMAL);
    console.log(`غير رسمي: ${informal.response}`);
    
    let friendly = nluNlgSystem.nlgSystem.generateResponse(IntentType.GREETING, ResponseStyle.FRIENDLY);
    console.log(`ودي: ${friendly.response}`);
    
    // توليد من بيانات
    console.log("\nتوليد نص من بيانات:");
    let data = {
        name: "أحمد",
        age: 25,
        city: "القاهرة",
        hobbies: ["القراءة", "البرمجة", "الرياضة"]
    };
    let generated = nluNlgSystem.nlgSystem.generateFromData(data);
    console.log(generated);
}

/**
 * مثال 8: نظام متكامل شامل
 */
function example8_ComprehensiveIntegration(): void {
    console.log("\n=== مثال 8: نظام متكامل شامل ===\n");
    
    // 1. نظام المعرفة المتخصصة
    let knowledgeSystem = new SpecializedKnowledgeIntegrationSystem();
    let knowledgeStats = knowledgeSystem.getStatistics();
    
    // 2. نظام الذكاء العاطفي
    let eiSystem = new EmotionalIntelligenceSystem();
    eiSystem.empathySystem.adjustEmpathyLevel(EmpathyLevel.VERY_HIGH);
    let emotionResult = eiSystem.processEmotionalInput("أنا سعيد جداً بهذا الإنجاز!");
    
    // 3. نظام NLU/NLG
    let nluNlgSystem = new NLUNLGIntegrationSystem();
    let conversation = nluNlgSystem.processConversation("شكراً على مساعدتك", ResponseStyle.FRIENDLY);
    
    console.log("=== إحصائيات شاملة ===\n");
    
    console.log("1. نظام المعرفة المتخصصة:");
    console.log(`   - إجمالي المعرفة: ${knowledgeStats.totalKnowledge}`);
    console.log(`   - المجالات: 8 مجالات`);
    
    console.log("\n2. نظام الذكاء العاطفي:");
    let eiStats = eiSystem.getStatistics();
    console.log(`   - إجمالي التفاعلات: ${eiStats.totalInteractions}`);
    console.log(`   - الدرجة الإجمالية: ${(eiStats.overallScore * 100).toFixed(0)}%`);
    console.log(`   - مستوى التعاطف: ${eiStats.empathy.empathyLevel}`);
    
    console.log("\n3. نظام NLU/NLG:");
    let nluNlgStats = nluNlgSystem.getStatistics();
    console.log(`   - إجمالي المحادثات: ${nluNlgStats.totalConversations}`);
    console.log(`   - التحليلات: ${nluNlgStats.nlu.totalAnalyses}`);
    console.log(`   - التوليدات: ${nluNlgStats.nlg.totalGenerations}`);
    
    console.log("\n=== مثال تفاعل متكامل ===");
    console.log(`المستخدم: "شكراً على مساعدتك"`);
    console.log(`النظام: "${conversation.response}"`);
    console.log(`المشاعر المكتشفة: ${emotionResult.recognizedEmotion.type}`);
    console.log(`رد التعاطف: ${emotionResult.empathyResponse.message}`);
}

/**
 * تشغيل جميع الأمثلة
 */
function runAllExamples(): void {
    console.log("╔════════════════════════════════════════════════════════════╗");
    console.log("║     أمثلة الأنظمة المتقدمة - الجزء 2 - نظام بصيرة AI     ║");
    console.log("║              المطور: باسل يحيى عبدالله                    ║");
    console.log("╚════════════════════════════════════════════════════════════╝\n");
    
    example1_MathematicalKnowledge();
    example2_PhysicalKnowledge();
    example3_IntegratedSpecializedKnowledge();
    example4_EmotionRecognition();
    example5_EmotionManagement();
    example6_NaturalLanguageUnderstanding();
    example7_NaturalLanguageGeneration();
    example8_ComprehensiveIntegration();
    
    console.log("\n╔════════════════════════════════════════════════════════════╗");
    console.log("║                  تم تشغيل جميع الأمثلة بنجاح!             ║");
    console.log("╚════════════════════════════════════════════════════════════╝");
}

// ==================== التصدير ====================

export {
    example1_MathematicalKnowledge,
    example2_PhysicalKnowledge,
    example3_IntegratedSpecializedKnowledge,
    example4_EmotionRecognition,
    example5_EmotionManagement,
    example6_NaturalLanguageUnderstanding,
    example7_NaturalLanguageGeneration,
    example8_ComprehensiveIntegration,
    runAllExamples
};
