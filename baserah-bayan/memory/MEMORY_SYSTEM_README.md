# 🧠 نظام الذاكرة المتكامل - Integrated Memory System

## 📋 نظرة عامة

نظام الذاكرة المتكامل هو أحد المكونات الأساسية في **بصيرة AI**، وهو مصمم لمحاكاة الذاكرة البشرية بثلاثة أنواع رئيسية:

1. **الذاكرة الحدثية (Episodic Memory)** - تخزين الأحداث والتجارب
2. **الذاكرة الدلالية (Semantic Memory)** - تخزين المعرفة والحقائق
3. **الذاكرة العاملة (Working Memory)** - تخزين المعلومات المؤقتة

بالإضافة إلى **نظام تتبع السياق (Context Tracker)** الذي يتتبع المواضيع والكيانات والأهداف في الحوار.

---

## 🏗️ البنية المعمارية

```
memory/
├── episodic-memory.bn          # الذاكرة الحدثية (512 سطر)
├── semantic-memory.bn          # الذاكرة الدلالية (510 سطر)
├── working-memory.bn           # الذاكرة العاملة (418 سطر)
├── context-tracker.bn          # متتبع السياق (520 سطر)
├── memory-integration.bn       # نظام الدمج (300 سطر)
├── memory-examples.bn          # أمثلة عملية (300 سطر)
├── memory-demo.html            # واجهة ويب تفاعلية (400 سطر)
└── MEMORY_SYSTEM_README.md     # هذا الملف
```

**الحجم الإجمالي:** ~2,960 سطر

---

## 📚 المكونات الرئيسية

### 1. الذاكرة الحدثية (Episodic Memory)

**الملف:** `episodic-memory.bn`

**الوظيفة:** تخزين واسترجاع الأحداث والتجارب (الحوارات، التفاعلات، التجارب)

**الفئات الرئيسية:**
- `Episode` - حدث عام
- `DialogueEpisode` - حوار محدد
- `EpisodicMemory` - مدير الذاكرة الحدثية

**الميزات:**
- تخزين الحوارات مع السياق الكامل
- استرجاع الأحداث الأخيرة
- البحث حسب الموضوع
- البحث عن أحداث مشابهة
- دمج الذكريات القديمة

**مثال:**
```javascript
let episodicMemory = new EpisodicMemory(1000);

let episodeId = episodicMemory.storeDialogue(
    "ما هو نظام بصيرة؟",
    "بصيرة هو نظام ذكاء اصطناعي ثوري",
    "question",
    ["بصيرة", "AI"],
    ["بصيرة", "ذكاء اصطناعي"],
    {location: "web"},
    true
);

let recent = episodicMemory.retrieveRecent(5);
```

---

### 2. الذاكرة الدلالية (Semantic Memory)

**الملف:** `semantic-memory.bn`

**الوظيفة:** تخزين المعرفة العامة والحقائق (Subject-Predicate-Object)

**الفئات الرئيسية:**
- `Fact` - حقيقة (ثلاثية SPO)
- `Concept` - مفهوم
- `Rule` - قاعدة
- `SemanticMemory` - مدير الذاكرة الدلالية

**الميزات:**
- تخزين الحقائق بصيغة ثلاثية
- تخزين المفاهيم مع التعريفات
- تخزين القواعد المنطقية
- الاستدلال على حقائق جديدة
- البحث والاستعلام

**مثال:**
```javascript
let semanticMemory = new SemanticMemory(10000);

semanticMemory.storeFact(
    "بصيرة",
    "يستخدم",
    "معادلات رياضية متكيفة",
    1.0,
    "learned"
);

semanticMemory.storeConcept(
    "بصيرة",
    "نظام ذكاء اصطناعي ثوري",
    {type: "AI system", approach: "mathematical"}
);

let facts = semanticMemory.retrieveFactsAbout("بصيرة");
```

---

### 3. الذاكرة العاملة (Working Memory)

**الملف:** `working-memory.bn`

**الوظيفة:** تخزين المعلومات المؤقتة أثناء المعالجة (سعة 7±2 عنصر)

**الفئات الرئيسية:**
- `WorkingMemoryItem` - عنصر في الذاكرة العاملة
- `WorkingMemory` - مدير الذاكرة العاملة

**الميزات:**
- سعة محدودة (قانون ميلر: 7±2)
- انتهاء صلاحية تلقائي (TTL)
- إخلاء ذكي (LRU مع الأولوية)
- أنواع متعددة (بيانات، أهداف، نتائج، متغيرات)

**مثال:**
```javascript
let workingMemory = new WorkingMemory(7);

workingMemory.store("current_user", "أحمد", ItemType.DATA, 1);
workingMemory.store("current_task", "تعلم البرمجة", ItemType.GOAL, 2);

let user = workingMemory.retrieve("current_user");
let goals = workingMemory.getActiveGoals();
```

---

### 4. متتبع السياق (Context Tracker)

**الملف:** `context-tracker.bn`

**الوظيفة:** تتبع السياق الحالي للحوار (المواضيع، الكيانات، الأهداف)

**الفئات الرئيسية:**
- `Topic` - موضوع
- `ContextualEntity` - كيان سياقي
- `Goal` - هدف
- `TopicTransition` - انتقال بين المواضيع
- `AdvancedContextTracker` - متتبع السياق المتقدم

**الميزات:**
- تتبع المواضيع النشطة
- تتبع الكيانات المذكورة
- تتبع الأهداف
- تسجيل الانتقالات بين المواضيع
- تنظيف تلقائي للسياق القديم

**مثال:**
```javascript
let contextTracker = new AdvancedContextTracker();

contextTracker.updateContext(
    1,
    "أخبرني عن الذكاء الاصطناعي",
    [{text: "الذكاء الاصطناعي", entityType: "concept"}]
);

let context = contextTracker.getCurrentContext();
let history = contextTracker.getTopicHistory();
```

---

### 5. نظام الدمج (Integration System)

**الملف:** `memory-integration.bn`

**الوظيفة:** دمج جميع أنواع الذاكرة في واجهة موحدة

**الفئة الرئيسية:**
- `IntegratedMemorySystem` - النظام المتكامل

**الميزات:**
- واجهة موحدة لجميع أنواع الذاكرة
- دمج تلقائي من الذاكرة العاملة إلى طويلة المدى
- بحث شامل في جميع الأنواع
- تعلم من التجارب
- إحصائيات شاملة

**مثال:**
```javascript
let memory = new IntegratedMemorySystem();

// حفظ
memory.remember({
    type: "dialogue",
    userInput: "ما هي بصيرة؟",
    botResponse: "بصيرة نظام ذكاء اصطناعي",
    topics: ["بصيرة"]
});

// استرجاع
let recent = memory.recall({type: "recent", count: 5});

// بحث شامل
let results = memory.search("بصيرة", 10);

// تعلم
memory.learn({
    userInput: "علمني عن البرمجة",
    botResponse: "البرمجة هي فن كتابة التعليمات",
    facts: [{subject: "البرمجة", predicate: "هي", object: "فن"}]
});
```

---

## 🎯 حالات الاستخدام

### 1. حوار مع المستخدم
```javascript
let memory = new IntegratedMemorySystem();

// حفظ الحوار
memory.remember({
    type: "dialogue",
    userInput: "ما هو نظام بصيرة؟",
    botResponse: "بصيرة هو نظام ذكاء اصطناعي ثوري",
    topics: ["بصيرة", "AI"]
});

// استرجاع الحوارات السابقة
let history = memory.recall({type: "recent", count: 10});
```

### 2. التعلم من التجارب
```javascript
memory.learn({
    userInput: "علمني عن Python",
    botResponse: "Python لغة برمجة عالية المستوى",
    facts: [
        {subject: "Python", predicate: "هي", object: "لغة برمجة"}
    ]
});
```

### 3. تتبع السياق
```javascript
let contextTracker = memory.getContextTracker();

contextTracker.updateContext(1, "أخبرني عن AI", [
    {text: "AI", entityType: "concept"}
]);

let context = contextTracker.getCurrentContext();
console.log("المواضيع النشطة:", context.activeTopics);
```

---

## 📊 الإحصائيات

```javascript
let stats = memory.getStatistics();

console.log("الذاكرة الحدثية:", stats.episodic.totalEpisodes);
console.log("الذاكرة الدلالية:", stats.semantic.totalFacts);
console.log("الذاكرة العاملة:", stats.working.currentSize);
console.log("السياق:", stats.context.totalTopics);
```

---

## 🌐 الواجهة التفاعلية

افتح `memory-demo.html` في المتصفح لتجربة النظام بشكل تفاعلي:

**الميزات:**
- إضافة حوارات جديدة
- عرض الذاكرة الحدثية
- عرض الذاكرة الدلالية
- عرض السياق الحالي
- إحصائيات مباشرة
- واجهة عربية جميلة

---

## 🔗 التكامل مع بصيرة AI

نظام الذاكرة يتكامل مع:
- **نظام التفكير** - استخدام الذاكرة في التفكير
- **نظام التعلم** - التعلم من التجارب المحفوظة
- **نظام الحوار** - تتبع سياق الحوار
- **نظام المعرفة** - تخزين واسترجاع المعرفة

---

## 📝 ملاحظات تقنية

1. **السعة:** يمكن تخصيص السعة لكل نوع ذاكرة
2. **الأداء:** استخدام Map للبحث السريع O(1)
3. **الدمج:** دمج تلقائي كل ساعة (قابل للتخصيص)
4. **التنظيف:** تنظيف تلقائي للذكريات القديمة
5. **الأولوية:** نظام أولويات للذاكرة العاملة

---

## 🎓 المراجع

- **قانون ميلر (Miller's Law):** سعة الذاكرة العاملة 7±2
- **نظرية الذاكرة البشرية:** Episodic vs Semantic Memory
- **RDF Triples:** Subject-Predicate-Object للحقائق

---

## ✅ الحالة

- [x] الذاكرة الحدثية (512 سطر)
- [x] الذاكرة الدلالية (510 سطر)
- [x] الذاكرة العاملة (418 سطر)
- [x] متتبع السياق (520 سطر)
- [x] نظام الدمج (300 سطر)
- [x] الأمثلة (300 سطر)
- [x] الواجهة التفاعلية (400 سطر)
- [x] التوثيق (هذا الملف)

**الحجم الإجمالي:** ~2,960 سطر

---

**المؤلف:** باسل يحيى عبدالله  
**التاريخ:** 2025-10-27  
**الإصدار:** 1.0.0  
**المشروع:** بصيرة AI - Baserah AI
