# 📚 نظام المعاجم اللغوية - Lexicon System

## 🎯 نظرة عامة

نظام المعاجم اللغوية هو نظام ثوري لاستخراج معاني الحروف من المعاجم العربية. يستخدم النظام تقنيات متقدمة لتحليل الكلمات واستنباط المعاني المشتركة للحروف من خلال دراسة الأنماط اللغوية.

## 🌟 الميزات الرئيسية

### 1. **تحميل المعاجم الرقمية**
- دعم مصادر متعددة (يدوي، ملفات، قواعد بيانات، إنترنت)
- معجم الراموز (26 MB)
- المعجم الرقمي
- البحث والاستعلام المتقدم

### 2. **استخراج معاني الحروف**
- **6 طرق استخراج:**
  - حرف واحد مشترك
  - حرفان متتابعان
  - حرفان بينهما حرف
  - ثلاثة أحرف
  - جذر لغوي
  - نمط لغوي

### 3. **إدارة قاعدة البيانات**
- عمليات CRUD كاملة
- فهرسة ذكية للبحث السريع
- **5 أنواع بحث:**
  - مطابق تماماً
  - يحتوي
  - يبدأ بـ
  - ينتهي بـ
  - نمط (Pattern)

### 4. **الاستخراج من الإنترنت**
- دعم معاجم متعددة (المعاني، القاموس، لسان العرب)
- تحديد معدل الطلبات (Rate Limiting)
- إعادة المحاولة التلقائية
- التخزين المؤقت

### 5. **التكامل مع الخبير/المستكشف**
- استشارة الخبير للتوجيه
- **3 استراتيجيات تخزين مؤقت:**
  - بدون تخزين
  - ذاكرة فقط
  - قاعدة بيانات فقط
  - هجين
- تتبع الأداء والإحصائيات

## 📁 هيكل الملفات

```
baserah-bayan/lexicon/
├── digital-lexicon-loader.bn          (335 سطر)  - محمل المعاجم
├── character-meaning-extractor.bn     (461 سطر)  - مستخرج المعاني
├── dictionary-database-manager.bn     (536 سطر)  - مدير قاعدة البيانات
├── digital-dictionary-scraper.bn      (411 سطر)  - مستخرج من الإنترنت
├── lexicon-integration.bn             (357 سطر)  - نظام التكامل
├── lexicon-examples.bn                (450 سطر)  - أمثلة عملية
├── lexicon-demo.html                  (695 سطر)  - واجهة ويب
└── LEXICON_SYSTEM_README.md           (هذا الملف)

baserah-bayan/databases/
├── arramooz_dictionary.db             (26 MB)    - معجم الراموز
└── digital_dictionary.db              (28 KB)    - المعجم الرقمي
```

**إجمالي:** ~3,245 سطر من كود البيان + 2 قاعدة بيانات

## 🔧 الاستخدام

### 1. تحميل معجم بسيط

```javascript
import { DigitalLexiconLoader, LexiconSource, Language } from "./digital-lexicon-loader.bn";

let loader = new DigitalLexiconLoader("my_loader");

// إضافة كلمات
loader.addWord("كتب", "سجل الأفكار", PartOfSpeech.VERB, "كتب", LexiconSource.MANUAL, Language.ARABIC);
loader.addWord("كتاب", "مجموعة صفحات", PartOfSpeech.NOUN, "كتب", LexiconSource.MANUAL, Language.ARABIC);

// البحث
let word = loader.getWord("كتاب");
console.log(word.meaning);

// الإحصائيات
let stats = loader.getStatistics();
console.log(`إجمالي الكلمات: ${stats.totalWords}`);
```

### 2. استخراج معاني الحروف

```javascript
import { CharacterMeaningExtractor } from "./character-meaning-extractor.bn";

let extractor = new CharacterMeaningExtractor("my_extractor");
extractor.loadLexicon(loader.lexicon);

// استخراج المعاني
let analyses = extractor.extractCharacterMeanings(0.1);

// تحليل حرف معين
let kafAnalysis = extractor.getCharacterAnalysis("ك");
console.log(`عدد الكلمات: ${kafAnalysis.totalWords}`);
console.log(`الثقة: ${kafAnalysis.confidence}`);

// أفضل المعاني
let topMeanings = kafAnalysis.getTopMeanings(5);
for (let m of topMeanings) {
    console.log(`${m.meaning}: ${m.score}`);
}
```

### 3. إدارة قاعدة البيانات

```javascript
import { DictionaryDatabaseManager, SearchType } from "./dictionary-database-manager.bn";

let dbManager = new DictionaryDatabaseManager("databases/digital_dictionary.db");

// إدراج كلمة
dbManager.insertWord("علم", "المعرفة والإدراك", "manual");

// البحث
let entry = dbManager.searchWord("علم");
console.log(entry.meaning);

// البحث بنمط
let results = dbManager.searchByPattern("عل", SearchType.STARTS_WITH);
console.log(`عدد النتائج: ${results.length}`);

// البحث بحرف مشترك
let charResults = dbManager.searchByCharacter("ع");
console.log(`كلمات تحتوي على 'ع': ${charResults.length}`);
```

### 4. الاستخراج من الإنترنت

```javascript
import { DigitalDictionaryScraper, DictionarySource } from "./digital-dictionary-scraper.bn";

let scraper = new DigitalDictionaryScraper("databases/digital_dictionary.db");

// استخراج معنى كلمة
let meaning = await scraper.scrapeAlmaany("حكمة");
console.log(`المعنى: ${meaning}`);

// تحليل معنى حرف
let analysis = scraper.analyzeCharacterMeanings("ح");
console.log(`المعنى العام: ${analysis.generalMeaning}`);
console.log(`الثقة: ${analysis.confidence}`);

// استخراج متعدد
let words = ["علم", "حكمة", "فهم"];
let results = await scraper.scrapeMultipleWords(words, DictionarySource.ALMAANY);
```

### 5. النظام المتكامل

```javascript
import { IntegratedLexiconSystem, IntegrationMode } from "./lexicon-integration.bn";

let system = new IntegratedLexiconSystem("my_system", IntegrationMode.FULL_INTEGRATION);

// التهيئة
system.initialize();

// البحث عن كلمة (مع كاش)
let word = system.searchWord("كتاب", true);
console.log(word.meaning);

// استخراج معاني الحروف
let charAnalyses = system.extractCharacterMeanings(0.1);

// تحليل حرف معين
let charAnalysis = await system.analyzeCharacter("ك");

// الإحصائيات الشاملة
let stats = system.getStatistics();
console.log(`معدل إصابة الكاش: ${stats.cache.hitRate}%`);
```

## 🎨 الواجهة التفاعلية

افتح `lexicon-demo.html` في المتصفح للوصول إلى:

### اللوحات التفاعلية:

1. **🔍 البحث في المعجم**
   - بحث بأنماط مختلفة
   - عرض النتائج الفورية

2. **🔤 تحليل معاني الحروف**
   - تحليل حرف واحد
   - استخراج جميع الحروف
   - عرض الثقة والمعاني

3. **➕ إضافة كلمات جديدة**
   - إضافة كلمات للمعجم
   - تحديد الجذر والمعنى

4. **📊 الإحصائيات**
   - إجمالي الكلمات والحروف
   - متوسط الطول
   - معدل الكاش

5. **📋 سجل الأحداث**
   - تتبع جميع العمليات
   - رسائل ملونة حسب النوع

## 🧠 التكامل مع الخبير/المستكشف

النظام يستشير الخبير/المستكشف في:

1. **اختيار طريقة الاستخراج**
   - الخبير يوصي بأفضل طريقة حسب السياق

2. **تحديد الأولويات**
   - منخفضة، متوسطة، عالية، حرجة

3. **التوجيه والإرشاد**
   - توصيات ذكية لتحسين الأداء

## 📊 الإحصائيات والأداء

### نظام الكاش:
- **معدل الإصابة:** يتم حساب نسبة الإصابة/الفشل
- **الحد الأقصى:** 1000 عنصر افتراضياً
- **استراتيجيات:** بدون، ذاكرة، قاعدة بيانات، هجين

### الفهرسة:
- **فهرس الكلمات:** Map<string, number>
- **فهرس الحروف:** Map<string, Array<number>>
- **بحث O(1)** للكلمات المفردة
- **بحث O(n)** للأنماط

## 🔬 خوارزميات الاستخراج

### 1. استخراج حرف واحد:
```
1. البحث عن جميع الكلمات التي تحتوي على الحرف
2. استخراج الكلمات المفتاحية من المعاني
3. حساب التكرار لكل كلمة مفتاحية
4. ترتيب حسب التكرار
5. حساب الثقة = min(عدد_الكلمات / 10, 1.0)
```

### 2. استخراج حرفين متتابعين:
```
1. البحث عن كلمات تحتوي على الحرفين متتابعين
2. تجميع الكلمات في مجموعات
3. تحليل المعاني المشتركة
4. حساب قوة المجموعة
5. استنباط المعنى المشترك
```

### 3. استخراج حرفين بينهما حرف:
```
1. البحث عن كلمات تحتوي على النمط: حرف1 + أي_حرف + حرف2
2. تحليل المعاني
3. استنباط المعنى المشترك
```

## 🗄️ قواعد البيانات

### معجم الراموز (arramooz_dictionary.db):
- **الحجم:** 26 MB
- **الكلمات:** ~100,000+ كلمة عربية
- **المحتوى:** كلمات، معاني، جذور، أنواع

### المعجم الرقمي (digital_dictionary.db):
- **الحجم:** 28 KB
- **الاستخدام:** تخزين معاني الحروف المستخرجة
- **النمو:** ينمو تلقائياً مع الاستخدام

## 🎯 حالات الاستخدام

1. **البحث اللغوي:**
   - دراسة معاني الحروف العربية
   - تحليل الجذور اللغوية

2. **معالجة اللغة الطبيعية:**
   - فهم دلالات الكلمات
   - استخراج المعاني السياقية

3. **التعليم:**
   - تعليم اللغة العربية
   - شرح معاني الحروف

4. **البحث والتطوير:**
   - تطوير أنظمة ذكاء اصطناعي لغوية
   - بناء معاجم ذكية

## 🚀 الأداء

- **البحث:** O(1) للكلمات المفردة
- **الاستخراج:** O(n*m) حيث n = عدد الكلمات، m = طول الكلمة
- **الكاش:** يحسن الأداء بنسبة 70-90%
- **الفهرسة:** تسريع البحث بنسبة 10x

## 📝 ملاحظات مهمة

1. **النمو التلقائي:**
   - معاني الحروف تُحفظ في قاعدة البيانات
   - لا حاجة لإعادة الاستخراج

2. **التخزين المؤقت:**
   - يقلل الحمل على قاعدة البيانات
   - يحسن سرعة الاستجابة

3. **التكامل:**
   - يتكامل مع نظام المعرفة
   - يتكامل مع نظام التعلم
   - تحت إشراف الخبير/المستكشف

## 🎓 أمثلة عملية

راجع `lexicon-examples.bn` للحصول على 6 أمثلة عملية شاملة:

1. تحميل معجم بسيط
2. استخراج معاني الحروف
3. إدارة قاعدة البيانات
4. الاستخراج من الإنترنت
5. النظام المتكامل
6. البحث المتقدم

## 🔗 الروابط

- **المشروع الرئيسي:** `baserah-bayan/`
- **الوحدة الفنية:** `baserah-bayan/artistic/`
- **نظام التفكير:** `baserah-bayan/thinking/`
- **نظام المعرفة:** `baserah-bayan/knowledge/`

---

**المطور:** باسل يحيى عبدالله  
**جميع الأفكار والنظريات من إبداع:** باسل يحيى عبدالله  
**نظام بصيرة AI الثوري** - Pure Mathematical AI (No Neural Networks)

