# 🧮 النواة الرياضية - Mathematical Core

## المعادلة الأم - Mother Equation

### 📐 التعريف الرياضي

```
O = (id, Φ, Ψ(t), Γ)
```

حيث:
- **id**: الهوية الفريدة للكائن (Unique Identity)
- **Φ (Phi)**: الخصائص الثابتة (Static Properties)
- **Ψ(t) (Psi)**: الخصائص الديناميكية المتغيرة مع الزمن (Dynamic Properties)
- **Γ (Gamma)**: دالة الشكل (Shape Function)

---

## 🎯 الفلسفة

المعادلة الأم هي **الأساس التجريدي** الذي يرث منه كل شيء في نظام بصيرة AI.

### لماذا "الأم"؟

- ✅ **شاملة** - تحتوي على كل ما يحتاجه أي كائن
- ✅ **تجريدية** - لا يتم إنشاء كائنات منها مباشرة
- ✅ **أساسية** - كل الكائنات ترث منها
- ✅ **رياضية** - تمثيل رياضي دقيق للواقع

---

## 📦 المكونات الأربعة

### 1. الهوية (id)

```javascript
id: string  // معرف فريد UUID
```

**الغرض:**
- تمييز كل كائن عن غيره
- تتبع الكائنات في النظام
- الربط بين الكائنات

**مثال:**
```
"a1b2c3d4-e5f6-4789-a012-b3c4d5e6f7g8"
```

---

### 2. الخصائص الثابتة - Φ (Phi)

```javascript
class StaticProperties {
    mass: number;           // الكتلة
    density: number;        // الكثافة
    material: string;       // المادة
    color: string;          // اللون
    texture: string;        // النسيج
    customProperties: object;
}
```

**الغرض:**
- تمثيل الخصائص التي لا تتغير مع الزمن
- الخصائص الجوهرية للكائن
- المادة المصنوع منها الكائن

**أمثلة:**
- كتلة الكرة = 0.5 كجم (ثابتة)
- مادة الجدار = طوب (ثابتة)
- لون التفاحة = أحمر (ثابت نسبياً)

---

### 3. الخصائص الديناميكية - Ψ(t) (Psi)

```javascript
class DynamicProperties {
    position: number[];      // الموقع [x, y, z]
    velocity: number[];      // السرعة [vx, vy, vz]
    acceleration: number[];  // التسارع [ax, ay, az]
    rotation: number[];      // الدوران [rx, ry, rz]
    energy: number;          // الطاقة
    temperature: number;     // درجة الحرارة
    timestamp: Date;         // الطابع الزمني
    customDynamic: object;
}
```

**الغرض:**
- تمثيل الخصائص المتغيرة مع الزمن
- حالة الكائن في لحظة معينة
- تتبع التغيرات

**أمثلة:**
- موقع الكرة = [10, 5, 0] (يتغير)
- سرعة السيارة = [20, 0, 0] (تتغير)
- درجة حرارة الماء = 80°C (تتغير)

---

### 4. دالة الشكل - Γ (Gamma)

```javascript
class ShapeFunction {
    shapeType: string;       // نوع الشكل
    dimensions: object;      // الأبعاد
    volume: number;          // الحجم
    surfaceArea: number;     // مساحة السطح
    sigmoidTerms: any[];     // حدود سيغمويد معممة
    linearTerms: any[];      // حدود خطية
}
```

**الغرض:**
- تمثيل الشكل الهندسي للكائن
- حساب الحجم ومساحة السطح
- استخدام دوال سيغمويد المعممة + خطوط مستقيمة

**لماذا سيغمويد معممة؟**
- ✅ **مرونة** - يمكن رسم أي شكل
- ✅ **سلاسة** - التغيرات ناعمة وليست مفاجئة
- ✅ **تكيف** - تتغير المعاملات بدل تغيير الدالة كلها

**دالة سيغمويد المعممة:**
```
σ(x) = 1 / (1 + e^(-a·x^(α+iβ)))
```
حيث:
- a: المعامل
- α: الجزء الحقيقي من الأس
- β: الجزء التخيلي من الأس

---

## 🔄 دورة حياة الكائن

```
1. الإنشاء (Creation)
   ↓
2. التهيئة (Initialization)
   - توليد id
   - تهيئة Φ, Ψ(t), Γ
   ↓
3. التشغيل (Operation)
   - تحديث Ψ(t)
   - تسجيل الحالات
   ↓
4. التطور (Evolution)
   - تكيف المعادلات
   - تعلم من التجربة
```

---

## 💡 أمثلة الاستخدام

### مثال 1: كرة بسيطة

```javascript
// لا نستخدم MotherEquation مباشرة!
// بل نرث منها في الأصناف الأخرى

// سيتم إنشاء PhysicalObject يرث من MotherEquation
class PhysicalObject extends MotherEquation {
    constructor(name: string) {
        super(ObjectType.PHYSICAL, name);
    }
}

// إنشاء كرة
let ball = new PhysicalObject("كرة");
ball.phi.mass = 0.5;
ball.phi.color = "أحمر";
ball.gamma.shapeType = "sphere";
ball.gamma.dimensions["radius"] = 0.1;
ball.gamma.calculateVolume();
```

### مثال 2: تحديث الموقع

```javascript
// تحديث موقع الكرة
ball.psi.updatePosition([10.0, 5.0, 0.0]);

// تحديث السرعة
ball.psi.updateVelocity([2.0, 0.0, 0.0]);

// تسجيل الحالة
ball.recordState();
```

### مثال 3: الحصول على الحالة

```javascript
// الحصول على الحالة الكاملة
let state = ball.getState();
console.log(state);

// الحصول على السجل
let history = ball.getHistory(10);  // آخر 10 حالات
```

---

## 🏗️ الوراثة

```
MotherEquation (تجريدية)
    ↓
    ├── PhysicalObject (كائن فيزيائي)
    ├── AbstractObject (كائن مجرد)
    ├── CompositeObject (كائن مركب)
    ├── LinguisticObject (كائن لغوي)
    └── ShapeEquation (معادلة الشكل العام)
```

---

## 📊 الخصائص الناشئة

عندما نجمع عدة كائنات، تظهر **خصائص ناشئة** جديدة:

```
جدار = لبنة₁ + لبنة₂ + ... + لبنةₙ

متانة_الجدار = ƒ(متانة_اللبنات, قوة_الأسمنت, نمط_البناء)
```

هذه الخصائص **ليست مجرد مجموع** خصائص الأجزاء، بل هي **نتيجة التفاعل** بينها.

---

## 🎯 الفوائد

### 1. توحيد التمثيل
- كل شيء له نفس البنية الأساسية
- سهولة التعامل مع أنواع مختلفة

### 2. المرونة
- إضافة خصائص مخصصة
- التكيف مع احتياجات مختلفة

### 3. التتبع
- سجل كامل للتغيرات
- إمكانية الرجوع للحالات السابقة

### 4. الرياضية
- تمثيل دقيق
- إمكانية الحسابات الرياضية

---

## 🔮 المستقبل

المعادلة الأم ستتطور لتشمل:

- ✅ **العلاقات** - روابط بين الكائنات
- ✅ **السببية** - تأثير كائن على آخر
- ✅ **التعلم** - تكيف تلقائي
- ✅ **الذاكرة** - تذكر الحالات المهمة

---

## 📚 الملفات ذات الصلة

- `mother-equation.bn` - التطبيق الكامل
- `adaptive-equations.bn` - المعادلات المتكيفة
- `mathematical-operators.bn` - المشغلات الرياضية

---

<div align="center">

**المعادلة الأم: أساس كل شيء في بصيرة AI**

**O = (id, Φ, Ψ(t), Γ)**

</div>

