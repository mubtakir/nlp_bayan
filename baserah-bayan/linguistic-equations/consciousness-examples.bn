/**
 * أمثلة الوعي الآلي
 * ==================
 * 
 * أمثلة تطبيقية على نظام الوعي الآلي
 * 
 * @version 1.0
 * @author باسل يحيى عبدالله
 */

import { MachineConsciousness } from './machine-consciousness.bn';

/**
 * مثال 1: جملة بسيطة
 */
function example1_SimpleSentence(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 1: جملة بسيطة - أحمد ضرب الكرة");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    consciousness.processSentence("أحمد ضرب الكرة");
    
    // الاستعلام عن حالة أحمد
    console.log("\n🔍 الاستعلام عن حالة أحمد:");
    let ahmedState = consciousness.queryThing("أحمد");
    console.log(JSON.stringify(ahmedState, null, 2));
    
    // الاستعلام عن حالة الكرة
    console.log("\n🔍 الاستعلام عن حالة الكرة:");
    let ballState = consciousness.queryThing("الكرة");
    console.log(JSON.stringify(ballState, null, 2));
}

/**
 * مثال 2: جملة مع تأثيرات متعددة
 */
function example2_ComplexSentence(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 2: جملة معقدة - زيد أكل تفاحة");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    consciousness.processSentence("زيد أكل تفاحة");
    
    // الاستعلام عن حالة زيد
    console.log("\n🔍 الاستعلام عن حالة زيد:");
    let zaidState = consciousness.queryThing("زيد");
    console.log(JSON.stringify(zaidState, null, 2));
}

/**
 * مثال 3: قصة متسلسلة
 */
function example3_Story(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 3: قصة متسلسلة - يوم في حياة محمد");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    let story = [
        "محمد استيقظ",
        "محمد أكل خبز",
        "محمد شرب ماء",
        "محمد قرأ كتاب",
        "محمد كتب رسالة",
        "محمد نام"
    ];
    
    consciousness.processStory(story);
    
    // عرض التاريخ
    console.log("\n📜 تاريخ الأحداث:");
    let history = consciousness.getHistory();
    history.forEach((entry, index) => {
        console.log(`\n${index + 1}. ${entry.sentence}`);
        console.log(`   التغييرات: ${entry.changes.length}`);
    });
    
    // الحالة النهائية لمحمد
    console.log("\n🔍 الحالة النهائية لمحمد:");
    let mohammedState = consciousness.queryThing("محمد");
    console.log(JSON.stringify(mohammedState, null, 2));
}

/**
 * مثال 4: تفاعلات متعددة
 */
function example4_MultipleInteractions(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 4: تفاعلات متعددة");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    // سلسلة من الأحداث
    consciousness.processSentence("علي ضرب الكرة");
    consciousness.processSentence("سارة قرأ كتاب");
    consciousness.processSentence("خالد أكل تفاحة");
    consciousness.processSentence("فاطمة كتب رسالة");
    
    // عرض حالة العالم الكاملة
    console.log("\n🌍 حالة العالم الكاملة:");
    let worldState = consciousness.memory.worldState;
    worldState.forEach((thing, name) => {
        console.log(`\n📦 ${name}:`);
        thing.properties.forEach((property, propName) => {
            console.log(`   ${propName}: ${property.value.toFixed(1)}${property.unit}`);
        });
    });
}

/**
 * مثال 5: تتبع التغييرات عبر الزمن
 */
function example5_TrackingChanges(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 5: تتبع التغييرات عبر الزمن");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    // نفس الشخص يقوم بأفعال متعددة
    console.log("📊 تتبع تغيرات حالة يوسف:\n");
    
    consciousness.processSentence("يوسف أكل خبز");
    let state1 = consciousness.queryThing("يوسف");
    console.log(`بعد الأكل - جوع: ${state1.properties.جوع.value.toFixed(1)}%, طاقة: ${state1.properties.طاقة.value.toFixed(1)}%`);
    
    consciousness.processSentence("يوسف قرأ كتاب");
    let state2 = consciousness.queryThing("يوسف");
    console.log(`بعد القراءة - معرفة: ${state2.properties.معرفة.value.toFixed(1)}%`);
    
    consciousness.processSentence("يوسف نام");
    let state3 = consciousness.queryThing("يوسف");
    console.log(`بعد النوم - تعب: ${state3.properties.تعب?.value.toFixed(1)}%, طاقة: ${state3.properties.طاقة.value.toFixed(1)}%`);
}

/**
 * مثال 6: اختبار الذاكرة
 */
function example6_MemoryTest(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 6: اختبار الذاكرة");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    // معالجة عدة جمل
    for (let i = 1; i <= 15; i++) {
        consciousness.processSentence(`شخص${i} أكل تفاحة`);
    }
    
    console.log(`\n📚 عدد المعادلات في الذاكرة: ${consciousness.memory.equations.length}`);
    console.log(`📚 الحد الأقصى للذاكرة قصيرة المدى: ${consciousness.memory.maxShortTermSize}`);
    console.log(`🌐 عدد الأشياء في حالة العالم: ${consciousness.memory.worldState.size}`);
    
    // عرض المعادلات الأخيرة
    console.log("\n📖 آخر 5 معادلات في الذاكرة:");
    let recent = consciousness.memory.getRecentEquations(5);
    recent.forEach((eq, index) => {
        console.log(`   ${index + 1}. ${eq.sentence}`);
    });
}

/**
 * مثال 7: قصة معقدة
 */
function example7_ComplexStory(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 7: قصة معقدة - مغامرة أحمد");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    let adventure = [
        "أحمد استيقظ",
        "أحمد أكل خبز",
        "أحمد شرب ماء",
        "أحمد ذهب",
        "أحمد قرأ كتاب",
        "أحمد كتب رسالة",
        "أحمد ضرب الكرة",
        "أحمد جلس",
        "أحمد نام"
    ];
    
    consciousness.processStory(adventure);
    
    // تحليل الرحلة
    console.log("\n📊 تحليل رحلة أحمد:");
    let finalState = consciousness.queryThing("أحمد");
    
    console.log("\n🎯 الحالة النهائية:");
    for (let prop in finalState.properties) {
        let p = finalState.properties[prop];
        let percentage = (p.normalized * 100).toFixed(0);
        let bar = "█".repeat(Math.floor(p.normalized * 20));
        console.log(`   ${prop}: ${p.value.toFixed(1)}${p.unit} [${bar}] ${percentage}%`);
    }
    
    // إحصائيات
    console.log("\n📈 إحصائيات:");
    console.log(`   عدد الأحداث: ${adventure.length}`);
    console.log(`   مستوى الوعي: ${(consciousness.awarenessLevel * 100).toFixed(0)}%`);
    console.log(`   مستوى الفهم: ${(consciousness.understandingLevel * 100).toFixed(0)}%`);
}

/**
 * مثال 8: مقارنة حالات
 */
function example8_CompareStates(): void {
    console.log("\n" + "█".repeat(70));
    console.log("█  مثال 8: مقارنة حالات شخصين");
    console.log("█".repeat(70) + "\n");
    
    let consciousness = new MachineConsciousness();
    
    // شخصان يقومان بأفعال مختلفة
    consciousness.processSentence("أحمد أكل تفاحة");
    consciousness.processSentence("محمد قرأ كتاب");
    consciousness.processSentence("أحمد قرأ كتاب");
    consciousness.processSentence("محمد أكل تفاحة");
    
    // مقارنة الحالات
    let ahmed = consciousness.queryThing("أحمد");
    let mohammed = consciousness.queryThing("محمد");
    
    console.log("\n📊 مقارنة الحالات:\n");
    console.log("الخاصية          أحمد        محمد");
    console.log("─".repeat(50));
    
    let allProps = new Set([
        ...Object.keys(ahmed.properties),
        ...Object.keys(mohammed.properties)
    ]);
    
    allProps.forEach(prop => {
        let ahmedVal = ahmed.properties[prop]?.value.toFixed(1) || "N/A";
        let mohammedVal = mohammed.properties[prop]?.value.toFixed(1) || "N/A";
        console.log(`${prop.padEnd(15)} ${String(ahmedVal).padEnd(10)} ${mohammedVal}`);
    });
}

/**
 * تشغيل جميع الأمثلة
 */
function runAllExamples(): void {
    example1_SimpleSentence();
    example2_ComplexSentence();
    example3_Story();
    example4_MultipleInteractions();
    example5_TrackingChanges();
    example6_MemoryTest();
    example7_ComplexStory();
    example8_CompareStates();
    
    console.log("\n" + "█".repeat(70));
    console.log("█  ✅ اكتملت جميع الأمثلة");
    console.log("█".repeat(70) + "\n");
}

export {
    example1_SimpleSentence,
    example2_ComplexSentence,
    example3_Story,
    example4_MultipleInteractions,
    example5_TrackingChanges,
    example6_MemoryTest,
    example7_ComplexStory,
    example8_CompareStates,
    runAllExamples
};
