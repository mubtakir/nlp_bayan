# 🌐 Web IDE Guide

دليل استخدام بيئة التطوير المتكاملة على الويب.

---

## 🚀 البداية السريعة

### تشغيل Web IDE

```bash
cd web_ide
python app.py
```

ثم افتح المتصفح على: **http://localhost:5000**

---

## 🎨 مكونات الواجهة

### 1. محرر الكود (Editor)
- **Syntax highlighting** للكلمات المفتاحية
- **تكبير/تصغير** النص
- **حفظ تلقائي** للكود

### 2. زر التنفيذ (Run)
- اضغط "Run" أو `Ctrl+Enter`
- شاهد النتائج فوراً

### 3. منطقة النتائج (Output)
- **Output**: نتائج print
- **Logic Results**: نتائج الاستعلامات المنطقية
- **Errors**: رسائل الأخطاء

### 4. الرسم البياني المنطقي (Logic Graph)
- تصور Facts و Rules
- عرض العلاقات
- تفاعلي مع zoom

---

## 💡 أمثلة سريعة

### مثال 1: Hello World

```bayan
print("مرحباً من Web IDE!")
```

اضغط **Run** ← شاهد النتيجة

### مثال 2: برمجة منطقية

```bayan
logic {
    fact parent("أحمد", "محمد").
    fact parent("محمد", "سارة").
    
    rule grandparent(?x, ?z) :- 
        parent(?x, ?y), parent(?y, ?z).
}

?- grandparent(?gp, "سارة")
```

اضغط **Run** ← شاهد النتائج + الرسم البياني

---

## 🎯 مميزات مفيدة

### تحميل الأمثلة
- قائمة "Examples" منسدلة
- اختر مثال ← يُحمل في المحرر تلقائياً

### تصدير الكود
- زر "Export" ← حفظ كملف `.bayan`

### مشاركة الكود
- انسخ الرابط من المتصفح
- شارك مع الآخرين

---

## ⚠️ ملاحظات مهمة

⚠️ **Web IDE للتجربة فقط** - ليس للإنتاج

⚠️ **لا يُحفظ تلقائياً** - احفظ عملك خارجياً

⚠️ **الأمان**: لا تدخل معلومات حساسة

---

## 🔧 حل المشاكل

### المشكلة: لا يعمل Run

**الحل:**
- تحقق من الأخطاء في الكود
- راجع منطقة Errors

### المشكلة: Logic Graph فارغ

**الحل:**
- تأكد من وجود `logic { }` block
- أضف facts أو rules

---

## 📱 للمطورين

### تخصيص Web IDE

الملفات:
- `web_ide/app.py` - Backend
- `web_ide/templates/index.html` - Frontend
- `web_ide/static/` - CSS/JS

---

[← البداية](README.md)
