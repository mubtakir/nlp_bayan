# 📊 تحليل بيانات عربية

مثال عملي لمعالجة وتحليل بيانات CSV بالعربية باستخدام Bayan.

---

## 📋 نظرة عامة

هذا المثال يوضح:
- قراءة ملفات CSV بمحتوى عربي
- معالجة البيانات النصية
- إحصائيات وصفية
- تصور بسيط للنتائج

---

## 💻 الكود الكامل

```bayan
# arabic_data_analysis.bayan
# تحليل بيانات المبيعات بالعربية

import ai.data as data

hybrid {
    # بيانات المبيعات (CSV)
    sales_data = """
الاسم,المدينة,المبلغ,التقييم
أحمد,الرياض,15000,5
فاطمة,جدة,22000,4
محمد,الدمام,18000,5
نورة,الرياض,25000,5
خالد,جدة,12000,3
سارة,الرياض,30000,5
عبدالله,الدمام,16000,4
مريم,جدة,28000,5
"""
    
    def analyze_sales() {
        """تحليل شامل لبيانات المبيعات"""
        
        print("📊 تحليل بيانات المبيعات")
        print("="*60)
        
        # 1. قراءة البيانات
        rows = data.read_csv_string(sales_data, ",")
        headers = rows[0]
        data_rows = rows[1:]
        
        print(f"\n✅ تم قراءة {len(data_rows)} سجل")
        print(f"📋 الأعمدة: {', '.join(headers)}\n")
        
        # 2. استخراج البيانات
        names = []
        cities = []
        amounts = []
        ratings = []
        
        for row in (data_rows) {
            if (len(row) >= 4) {
                names.append(row[0].strip())
                cities.append(row[1].strip())
                amounts.append(float(row[2].strip()))
                ratings.append(int(row[3].strip()))
            }
        }
        
        # 3. الإحصائيات
        print("📈 الإحصائيات العامة:")
        print("-" * 60)
        
        # المبالغ
        total_sales = sum(amounts)
        avg_sales = data.mean(amounts)
        min_sales = data.min_value(amounts)
        max_sales = data.max_value(amounts)
        
        print(f"💰 إجمالي المبيعات: {total_sales:,.0f} ريال")
        print(f"📊 متوسط المبيعات: {avg_sales:,.0f} ريال")
        print(f"⬇️  أقل مبيعات: {min_sales:,.0f} ريال")
        print(f"⬆️  أعلى مبيعات: {max_sales:,.0f} ريال")
        
        # التقييمات
        avg_rating = data.mean(ratings)
        print(f"⭐ متوسط التقييم: {avg_rating:.2f}/5")
        
        # 4. تحليل حسب المدينة
        print("\n🏙️  التحليل حسب المدينة:")
        print("-" * 60)
        
        unique_cities = []
        for city in (cities) {
            if (city not in unique_cities) {
                unique_cities.append(city)
            }
        }
        
        for city in (unique_cities) {
            city_amounts = []
            city_count = 0
            
            for i in (range(len(cities))) {
                if (cities[i] == city) {
                    city_amounts.append(amounts[i])
                    city_count = city_count + 1
                }
            }
            
            city_total = sum(city_amounts)
            city_avg = data.mean(city_amounts)
            
            print(f"\n{city}:")
            print(f"  عدد المبيعات: {city_count}")
            print(f"  إجمالي: {city_total:,.0f} ريال")
            print(f"  متوسط: {city_avg:,.0f} ريال")
        }
        
        # 5. الموظفون الأفضل
        print("\n🏆 الموظفون الأفضل أداءً:")
        print("-" * 60)
        
        # إنشاء قائمة بالبيانات المدمجة
        employee_data = []
        for i in (range(len(names))) {
            employee_data.append({
                "name": names[i],
                "amount": amounts[i],
                "rating": ratings[i]
            })
        }
        
        # ترتيب حسب المبلغ
        sorted_by_amount = sorted(
            employee_data,
            key=lambda x: x["amount"],
            reverse=True
        )
        
        print("\nأعلى 3 مبيعات:")
        for i in (range(min(3, len(sorted_by_amount)))) {
            emp = sorted_by_amount[i]
            print(f"{i+1}. {emp['name']}: {emp['amount']:,.0f} ريال (⭐{emp['rating']})")
        }
        
        # الموظفون ذوو التقييم الممتاز
        print("\nموظفون بتقييم ممتاز (5 نجوم):")
        excellent_count = 0
        for emp in (employee_data) {
            if (emp["rating"] == 5) {
                print(f"  ✨ {emp['name']}: {emp['amount']:,.0f} ريال")
                excellent_count = excellent_count + 1
            }
        }
        
        print(f"\nالإجمالي: {excellent_count} موظف")
        
        # 6. رسم بياني نصي بسيط
        print("\n📊 رسم بياني للمبيعات:")
        print("-" * 60)
        
        max_bar_length = 40
        max_amount_val = max(amounts)
        
        for i in (range(len(names))) {
            bar_length = int((amounts[i] / max_amount_val) * max_bar_length)
            bar = "█" * bar_length
            print(f"{names[i]:12} | {bar} {amounts[i]:,.0f}")
        }
        
        print("\n" + "="*60)
        print("✅ اكتمل التحليل!")
    }
    
    # تشغيل التحليل
    analyze_sales()
}
```

---

## 🎮 كيفية التشغيل

```bash
python -m bayan.bayan.cli arabic_data_analysis.bayan
```

---

## 📖 مثال على الن تيجة

```
📊 تحليل بيانات المبيعات
============================================================

✅ تم قراءة 8 سجل
📋 الأعمدة: الاسم, المدينة, المبلغ, التقييم

📈 الإحصائيات العامة:
------------------------------------------------------------
💰 إجمالي المبيعات: 166,000 ريال
📊 متوسط المبيعات: 20,750 ريال
⬇️  أقل مبيعات: 12,000 ريال
⬆️  أعلى مبيعات: 30,000 ريال
⭐ متوسط التقييم: 4.50/5

🏙️  التحليل حسب المدينة:
------------------------------------------------------------

الرياض:
  عدد المبيعات: 3
  إجمالي: 70,000 ريال
  متوسط: 23,333 ريال

جدة:
  عدد المبيعات: 3
  إجمالي: 62,000 ريال
  متوسط: 20,667 ريال

الدمام:
  عدد المبيعات: 2
  إجمالي: 34,000 ريال
  متوسط: 17,000 ريال

🏆 الموظفون الأفضل أداءً:
------------------------------------------------------------

أعلى 3 مبيعات:
1. سارة: 30,000 ريال (⭐5)
2. مريم: 28,000 ريال (⭐5)
3. نورة: 25,000 ريال (⭐5)

موظفون بتقييم ممتاز (5 نجوم):
  ✨ أحمد: 15,000 ريال
  ✨ محمد: 18,000 ريال
  ✨ نورة: 25,000 ريال
  ✨ سارة: 30,000 ريال
  ✨ مريم: 28,000 ريال

الإجمالي: 5 موظف

📊 رسم بياني للمبيعات:
------------------------------------------------------------
أحمد        | ████████████████████ 15,000
فاطمة       | █████████████████████████████ 22,000
محمد        | ████████████████████████ 18,000
نورة        | █████████████████████████████████ 25,000
خالد        | ████████████████ 12,000
سارة        | ████████████████████████████████████████ 30,000
عبدالله     | █████████████████████ 16,000
مريم        | █████████████████████████████████████ 28,000

============================================================
✅ اكتمل التحليل!
```

---

## 🎯 التوسعات الممكنة

### 1. قراءة من ملف CSV حقيقي
```bayan
# قراءة من ملف
with open("sales.csv", "r", encoding="utf-8") as f:
    content = f.read()

rows = data.read_csv_string(content, ",")
```

### 2. تصدير النتائج
```bayan
# كتابة النتائج لملف
output = data.write_csv_string(results, ",")
with open("results.csv", "w", encoding="utf-8") as f:
    f.write(output)
```

### 3. رسوم بيانية متقدمة
```python
# استخدام matplotlib
import matplotlib.pyplot as plt
plt.bar(names, amounts)
plt.show()
```

### 4. تحليل زمني
```bayan
# إضافة عمود التاريخ
fact sale("أحمد", "2024-01", 15000).
# تحليل الاتجاهات
```

---

## 🎓 ما تعلمناه

✅ **معالجة CSV بالعربية**: Unicode handling  
✅ **الإحصائيات الوصفية**: mean, min, max  
✅ **تجميع البيانات**: group by city  
✅ **الفرز والترتيب**: sorted()  
✅ **visualization بسيط**: نصي  

---

[← العودة لـ Cookbook](../README.md)
