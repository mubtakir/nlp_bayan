# 📚 الدليل الشامل لواجهة المنطق الرسومية
# Complete Guide to Logic Graph Interface

---

## 📋 جدول المحتويات

1. [التثبيت والإعداد الأولي](#التثبيت-والإعداد-الأولي)
2. [طرق التشغيل المختلفة](#طرق-التشغيل-المختلفة)
3. [استخدام الواجهة](#استخدام-الواجهة)
4. [أمثلة عملية](#أمثلة-عملية)
5. [فهم الرسم البياني](#فهم-الرسم-البياني)
6. [استكشاف الأخطاء](#استكشاف-الأخطاء)
7. [نصائح متقدمة](#نصائح-متقدمة)

---

## 1️⃣ التثبيت والإعداد الأولي

### المتطلبات الأساسية
- Python 3.8 أو أحدث
- متصفح حديث (Chrome, Firefox, Edge)
- نظام Linux/Ubuntu

### خطوات التثبيت (مرة واحدة فقط)

#### الخطوة 1: إنشاء البيئة الافتراضية
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide14
python3 -m venv venv
```

#### الخطوة 2: تفعيل البيئة الافتراضية
```bash
source venv/bin/activate
```
ستلاحظ ظهور `(venv)` في بداية سطر الأوامر.

#### الخطوة 3: تثبيت المكتبات المطلوبة
```bash
pip install flask
```

---

## 2️⃣ طرق التشغيل المختلفة

### الطريقة 1: التشغيل المباشر (الأسهل) ⭐

```bash
# من مجلد المشروع
cd /home/al-mubtakir/Documents/bayan_python_ide14

# تفعيل البيئة الافتراضية
source venv/bin/activate

# تشغيل الخادم
python3 web_ide/app.py
```

**ثم افتح المتصفح:**
```
http://127.0.0.1:5001/logic_graph
```

---

### الطريقة 2: التشغيل بدون تفعيل البيئة

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide14
./venv/bin/python web_ide/app.py
```

---

### الطريقة 3: التشغيل في الخلفية

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide14
nohup ./venv/bin/python web_ide/app.py > server.log 2>&1 &
```

**لإيقافه:**
```bash
pkill -f "web_ide/app.py"
```

**لرؤية السجلات:**
```bash
tail -f server.log
```

---

### الطريقة 4: استخدام سكريبت تشغيل

أنشئ ملف `start_logic_graph.sh`:

```bash
#!/bin/bash
cd /home/al-mubtakir/Documents/bayan_python_ide14
source venv/bin/activate
python3 web_ide/app.py
```

**اجعله قابل للتنفيذ:**
```bash
chmod +x start_logic_graph.sh
```

**شغّله:**
```bash
./start_logic_graph.sh
```

---

### الطريقة 5: فتح المتصفح تلقائياً

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide14
./venv/bin/python web_ide/app.py &
sleep 2
xdg-open http://127.0.0.1:5001/logic_graph
```

---

## 3️⃣ استخدام الواجهة

### تخطيط الواجهة

```
┌─────────────────────────────────────────────────────────┐
│  بيان المنطق الرسومي    [أزرار الأمثلة] [تحقق وتنفيذ ▶] │
├──────────────────┬──────────────────────────────────────┤
│                  │                                      │
│   المحرر         │        الرسم البياني                │
│   (كود بيان)     │        (تفاعلي)                     │
│                  │                                      │
├──────────────────┤                                      │
│  مسار التحقق     │                                      │
│  المخرجات        │                                      │
└──────────────────┴──────────────────────────────────────┘
```

### الأزرار الرئيسية

1. **مثال بسيط** (رمادي): يحمل مثالاً بسيطاً عن الحقائق والقواعد
2. **القياس المنطقي** (رمادي): مثال عن الاستنتاج المنطقي الكلاسيكي
3. **اختبار التناقض** (أحمر): مثال يكتشف التناقضات المنطقية
4. **تحقق وتنفيذ ▶** (أخضر): ينفذ الكود ويرسم الرسم البياني

### التبويبات السفلية

- **مسار التحقق**: يعرض خطوات الاستنتاج المنطقي بالتفصيل
- **المخرجات**: يعرض نتائج التنفيذ والاستعلامات

---

## 4️⃣ أمثلة عملية

### مثال 1: حقيقة بسيطة
```
مبرمج(أحمد).
```
**النتيجة:** عقدة واحدة تمثل أحمد كمبرمج.

---

### مثال 2: قاعدة منطقية (شبكة سببية)
```
مبرمج(أحمد).
يكتب_كود(X) :- مبرمج(X).
```
**النتيجة:** 
- 🟢 عقدة خضراء: أحمد
- 🟣 عقدة بنفسجية: IMPLIES (القاعدة)
- 🟠 عقد برتقالية: مبرمج، يكتب_كود
- أسهم تربط بينهم

---

### مثال 3: قياس منطقي
```
إنسان(سقراط).
فان(X) :- إنسان(X).
```
**النتيجة:** يظهر أن سقراط فانٍ لأنه إنسان.

---

### مثال 4: احتمالات (Probabilities)
```
مبرمج(أحمد).
ماهر(أحمد). [0.7]
```
**النتيجة:** 
- الخط الأول سميك ومتصل (100%)
- الخط الثاني متقطع وشفاف (70%)

---

### مثال 5: اكتشاف التناقض
```
لون(السيارة, أحمر).
لون(السيارة, أزرق).
```
**النتيجة:** 
- 🚨 تنبيه أحمر في أعلى الصفحة
- رسالة: "تناقض منطقي مكتشف!"

---

### مثال 6: علاقات متعددة
```
أب(أحمد, محمد).
أب(محمد, علي).
جد(X, Z) :- أب(X, Y), أب(Y, Z).
```
**النتيجة:** يستنتج أن أحمد جد علي.

---

### مثال 7: حقائق ثلاثية (N-ary)
```
يعيش_في(أحمد, الرياض, السعودية).
```
**النتيجة:** 
- 🔵 عقدة زرقاء مركزية للحدث
- 🟢 ثلاث عقد خضراء للمعاملات

---

## 5️⃣ فهم الرسم البياني

### الألوان والمعاني

| اللون | النوع | المعنى | مثال |
|------|------|--------|------|
| 🟢 أخضر | كيان (Entity) | كائنات ومفاهيم أساسية | أحمد، سقراط، السيارة |
| 🟣 بنفسجي | قاعدة (Rule) | قواعد منطقية سببية | IMPLIES |
| 🟠 برتقالي | قيمة (Value) | خصائص وصفات | مبرمج، لون، عمر |
| 🔵 أزرق | حدث (Event) | علاقات معقدة (3+ معاملات) | يعيش_في |

### أنواع الخطوط

| النوع | المعنى | الشكل |
|------|--------|-------|
| **متصل سميك** | حقيقة مؤكدة (100%) | ━━━━━━ |
| **متقطع شفاف** | حقيقة احتمالية (<100%) | ╌╌╌╌╌╌ |

### العلاقات (الأسهم)

- **is**: علاقة "هو" (كيان → خاصية)
- **causes**: علاقة سببية (قاعدة → نتيجة)
- **condition**: شرط (شرط → قاعدة)
- **arg1, arg2, ...**: معاملات الأحداث

### التفاعل مع الرسم

1. **السحب**: اسحب أي عقدة لتحريكها
2. **التكبير/التصغير**: استخدم عجلة الماوس
3. **التحريك**: اسحب الخلفية الفارغة

---

## 6️⃣ استكشاف الأخطاء

### المشكلة: "لا يمكن الوصول للموقع"

**الأسباب المحتملة:**
1. الخادم لم يبدأ بعد
2. الخادم توقف
3. المنفذ 5001 مستخدم

**الحلول:**
```bash
# تحقق من تشغيل الخادم
ps aux | grep "web_ide/app.py"

# إذا لم يكن يعمل، شغّله
cd /home/al-mubtakir/Documents/bayan_python_ide14
./venv/bin/python web_ide/app.py

# إذا كان المنفذ مستخدماً، أوقف العملية القديمة
pkill -f "web_ide/app.py"
```

---

### المشكلة: الصفحة فارغة أو بيضاء

**الحلول:**
1. حدّث الصفحة (F5 أو Ctrl+R)
2. امسح الذاكرة المؤقتة (Ctrl+Shift+Delete)
3. افتح أدوات المطور (F12) وتحقق من Console
4. جرب متصفحاً آخر

---

### المشكلة: خطأ في التحليل (Parsing Error)

**الأسباب:**
- استخدام `//` للتعليقات (غير مدعوم)
- بناء جملة خاطئ

**الحل:**
```
# ❌ خطأ
// هذا تعليق
أحمد هو مبرمج.

# ✅ صحيح
مبرمج(أحمد).
```

---

### المشكلة: "NameError: undefined variable"

**السبب:** استخدام متغير غير معرّف

**الحل:**
```
# ❌ خطأ
أحمد مبرمج.

# ✅ صحيح
مبرمج(أحمد).
```

---

### المشكلة: الرسم البياني لا يظهر

**الحلول:**
1. تأكد من الضغط على "تحقق وتنفيذ ▶"
2. تحقق من تبويب "مسار التحقق" لرؤية الأخطاء
3. جرب مثالاً بسيطاً أولاً
4. تحقق من Console في أدوات المطور (F12)

---

### المشكلة: "ModuleNotFoundError: No module named 'flask'"

**الحل:**
```bash
# تأكد من تفعيل البيئة الافتراضية
source venv/bin/activate

# ثبّت Flask
pip install flask
```

---

## 7️⃣ نصائح متقدمة

### نصيحة 1: استخدام ملفات منفصلة

يمكنك كتابة الكود في ملف `.bayan` ثم نسخه للواجهة:

```bash
# أنشئ ملف
nano my_logic.bayan

# اكتب الكود
مبرمج(أحمد).
يكتب_كود(X) :- مبرمج(X).

# احفظ (Ctrl+O, Enter, Ctrl+X)
# انسخ المحتوى للواجهة
```

---

### نصيحة 2: حفظ الرسوم البيانية

لحفظ الرسم البياني كصورة:
1. انقر بزر الماوس الأيمن على الرسم
2. اختر "Save image as..."
3. أو استخدم أدوات لقطة الشاشة

---

### نصيحة 3: استخدام المنافذ المختلفة

إذا أردت تشغيل الخادم على منفذ مختلف:

```python
# عدّل السطر الأخير في web_ide/app.py
app.run(host='127.0.0.1', port=8080, debug=True)
```

ثم افتح: `http://127.0.0.1:8080/logic_graph`

---

### نصيحة 4: الوصول من أجهزة أخرى

لفتح الواجهة من أجهزة أخرى على نفس الشبكة:

```python
# عدّل السطر الأخير في web_ide/app.py
app.run(host='0.0.0.0', port=5001, debug=True)
```

ثم من الجهاز الآخر:
```
http://[عنوان_IP_للجهاز]:5001/logic_graph
```

---

### نصيحة 5: تصدير البيانات

البيانات متاحة عبر API:

```bash
# احصل على الرسم البياني كـ JSON
curl -X POST http://127.0.0.1:5001/api/ide/run_logic \
  -H "Content-Type: application/json" \
  -d '{"code": "مبرمج(أحمد)."}'
```

---

### نصيحة 6: اختصارات لوحة المفاتيح

في المحرر:
- `Ctrl+A`: تحديد الكل
- `Ctrl+C`: نسخ
- `Ctrl+V`: لصق
- `Ctrl+Z`: تراجع

في المتصفح:
- `F5`: تحديث الصفحة
- `F12`: فتح أدوات المطور
- `Ctrl+Shift+I`: فتح أدوات المطور

---

## 📌 ملخص الأوامر السريعة

```bash
# التثبيت (مرة واحدة)
cd /home/al-mubtakir/Documents/bayan_python_ide14
python3 -m venv venv
source venv/bin/activate
pip install flask

# التشغيل (كل مرة)
cd /home/al-mubtakir/Documents/bayan_python_ide14
source venv/bin/activate
python3 web_ide/app.py

# ثم افتح المتصفح
# http://127.0.0.1:5001/logic_graph

# الإيقاف
# اضغط Ctrl+C في الطرفية
```

---

## 🎓 أمثلة إضافية للتجربة

### مثال: عائلة
```
أب(أحمد, محمد).
أب(أحمد, فاطمة).
أب(محمد, علي).
أخ(X, Y) :- أب(Z, X), أب(Z, Y).
جد(X, Z) :- أب(X, Y), أب(Y, Z).
```

### مثال: حيوانات
```
حيوان(قطة).
حيوان(كلب).
ثديي(X) :- حيوان(X).
يتنفس(X) :- ثديي(X).
```

### مثال: رياضيات
```
زوجي(2).
زوجي(4).
فردي(1).
فردي(3).
رقم(X) :- زوجي(X).
رقم(X) :- فردي(X).
```

---

## 🆘 الدعم والمساعدة

إذا واجهت مشكلة:
1. راجع قسم [استكشاف الأخطاء](#استكشاف-الأخطاء)
2. افتح أدوات المطور (F12) وتحقق من Console
3. راجع سجلات الخادم في الطرفية
4. جرب الأمثلة البسيطة أولاً

---

## 📚 مصادر إضافية

- **ملف البدء السريع**: `HOW_TO_USE_LOGIC_GRAPH.md`
- **ملف التشغيل**: `START_LOGIC_GRAPH.md`
- **أمثلة بيان**: مجلد `examples/`
- **الوثائق الكاملة**: مجلد `docs/`

---

**استمتع باستكشاف المنطق الرسومي! 🚀**

*آخر تحديث: 2025-11-22*
