# 🏆 تحدي بيان - Bayan Language Challenge
# هل تستطيع لغتك فعل هذا بنفس عدد السطور؟

**الإصدار**: 0.6.0 | **التاريخ**: 2025-12-06

---

## 🎯 فكرة التحدي

اكتب برنامجاً يحقق **كل** الميزات التالية:
- ✅ كيانات مع حالات ديناميكية
- ✅ أفعال تؤثر على الحالات
- ✅ علاقات بين الكيانات
- ✅ شبكة سببية متسلسلة
- ✅ استعلامات منطقية

**الشرط**: بدون مكتبات خارجية متخصصة!

---

## 🔥 التحدي الأول: كيانات + حالات + أفعال + علاقات + سببية
### (12 سطر فقط!)

```bayan
hybrid {
    # كيان مع حالات ضبابية وأفعال
    entity إنسان {
        states: {"صحة": 0.8, "طاقة": 0.7, "جوع": 0.3},
        actions: {
            "يأكل": {"effects": [{"on": "جوع", "formula": "max(value - 0.4, 0)"}, {"on": "طاقة", "formula": "min(value + 0.2, 1)"}]},
            "يعمل": {"effects": [{"on": "طاقة", "formula": "max(value - 0.3, 0)"}, {"on": "جوع", "formula": "min(value + 0.2, 1)"}]}
        }
    }
    # علاقات وسببية
    fact صداقة("أحمد", "خالد").
    fact يسبب("عمل_طويل", "تعب").
    fact يسبب("تعب", "جوع").
    rule يؤدي_إلى(?س, ?ص) :- يسبب(?س, ?ص).
    rule يؤدي_إلى(?س, ?ز) :- يسبب(?س, ?و), يؤدي_إلى(?و, ?ز).
}
query صداقة("أحمد", ?صديق).
query يؤدي_إلى("عمل_طويل", ?نتيجة).
```

### ❓ التحدي للغات الأخرى:
**اكتب نفس البرنامج بـ Python أو Java أو C++ في 12 سطر بدون مكتبات!**

ستحتاج على الأقل:
- `class Entity` + `__init__` + attributes
- `dict` للحالات
- `methods` للأفعال
- تحديث الحالات يدوياً
- `list/set` للعلاقات
- محرك استدلال للسببية المتسلسلة

**النتيجة**: 50-80 سطر على الأقل!

---

## 🔥 التحدي الثاني: شبكة سببية متسلسلة مع احتمالات
### (15 سطر فقط!)

```bayan
hybrid {
    # شبكة سببية للحالات النفسية
    fact يسبب("ضغط_نفسي", "قلق", 0.9).
    fact يسبب("قلق", "أرق", 0.85).
    fact يسبب("أرق", "تعب", 0.95).
    fact يسبب("تعب", "ضعف_تركيز", 0.8).

    # علاجات
    fact يخفف("استرخاء", "قلق", 0.7).
    fact يخفف("نوم_كافي", "تعب", 0.9).

    # قواعد الاستدلال
    rule سلسلة_سببية(?س, ?ص, ?ق) :- يسبب(?س, ?ص, ?ق).
    rule سلسلة_سببية(?س, ?ز, ?ق) :- يسبب(?س, ?و, ?ق1), سلسلة_سببية(?و, ?ز, ?ق2).

    print("ما الذي يسببه الضغط النفسي؟")
}
query سلسلة_سببية("ضغط_نفسي", ?نتيجة, ?احتمال).
```

### ❓ التحدي:
**اكتب شبكة سببية مع استدلال متسلسل واحتمالات في 15 سطر!**

ستحتاج:
- Graph structure
- Probability propagation
- Recursive inference
- Path finding

**النتيجة**: 100-150 سطر + مكتبات!

---

## 🔥 التحدي الثالث: شبكة دلالية هرمية
### (14 سطر فقط!)

```bayan
hybrid {
    # هرمية المفاهيم (Taxonomy)
    fact نوع_من("كائن_حي", "شيء").
    fact نوع_من("إنسان", "كائن_حي").
    fact نوع_من("حيوان", "كائن_حي").
    fact نوع_من("قط", "حيوان").
    fact نوع_من("طالب", "إنسان").

    # خصائص تنتقل بالوراثة
    fact خاصية("كائن_حي", "يتنفس").
    fact خاصية("إنسان", "يفكر").
    fact خاصية("قط", "يموء").

    # وراثة الخصائص
    rule يملك(?س, ?خ) :- خاصية(?س, ?خ).
    rule يملك(?س, ?خ) :- نوع_من(?س, ?أب), يملك(?أب, ?خ).
}
query يملك("طالب", ?خاصية).
query يملك("قط", ?خاصية).
```

### ❓ التحدي:
**اكتب شبكة دلالية مع وراثة خصائص في 14 سطر!**

ستحتاج:
- Class hierarchy
- Property inheritance
- Polymorphism
- Reflection/introspection

**النتيجة**: 60-100 سطر!

---

## 🔥 التحدي النهائي: دمج الكل معاً!
### (25 سطر فقط!)

```bayan
hybrid {
    # === الكيانات ===
    entity شخص {
        states: {"صحة": 0.9, "مزاج": 0.7},
        actions: {"يمرض": {"effects": [{"on": "صحة", "formula": "max(value-0.3,0)"}]}}
    }

    # === الشبكة الدلالية ===
    fact نوع_من("مرض", "حالة").
    fact نوع_من("زكام", "مرض").
    fact نوع_من("عدوى", "مرض").

    # === الشبكة السببية ===
    fact يسبب("برد", "زكام").
    fact يسبب("زكام", "عدوى").
    fact يسبب("عدوى", "تعب").

    # === العلاقات ===
    fact صداقة("أحمد", "خالد").
    fact ينتقل("زكام", "مصافحة").

    # === قواعد الاستدلال ===
    rule سلسلة(?س, ?ص) :- يسبب(?س, ?ص).
    rule سلسلة(?س, ?ز) :- يسبب(?س, ?و), سلسلة(?و, ?ز).
    rule يصيب(?م, ?ش2) :- صداقة(?ش1, ?ش2), ينتقل(?م, "مصافحة").
    rule هو_نوع(?س, ?ص) :- نوع_من(?س, ?ص).
    rule هو_نوع(?س, ?ز) :- نوع_من(?س, ?و), هو_نوع(?و, ?ز).
}
query سلسلة("برد", ?نتيجة).
query يصيب("زكام", ?شخص).
query هو_نوع("زكام", ?نوع).
```

### ❓ التحدي الأعظم:
**اكتب برنامجاً يجمع:**
1. كيانات مع حالات ديناميكية
2. شبكة دلالية هرمية
3. شبكة سببية متسلسلة
4. علاقات بين كيانات
5. استعلامات منطقية متعددة

**في 25 سطر فقط وبدون أي مكتبات!**

**النتيجة المتوقعة بلغات أخرى**: 200-400 سطر + مكتبات متخصصة!

---

## 📊 جدول المقارنة

| الميزة | بيان | Python | Java | C++ |
|--------|------|--------|------|-----|
| كيان مع حالات | 5 سطور | 20+ سطر | 30+ سطر | 40+ سطر |
| أفعال تؤثر على حالات | سطر واحد | 10+ سطر | 15+ سطر | 20+ سطر |
| شبكة سببية | 5 سطور | 50+ سطر | 60+ سطر | 80+ سطر |
| شبكة دلالية | 6 سطور | 40+ سطر | 50+ سطر | 70+ سطر |
| استدلال متسلسل | سطرين | 30+ سطر | 40+ سطر | 50+ سطر |
| علاقات | سطر واحد | 10+ سطر | 15+ سطر | 20+ سطر |
| **المجموع** | **20-25 سطر** | **160+ سطر** | **210+ سطر** | **280+ سطر** |

---

## 🎯 لماذا بيان مختلفة؟

### 1. 🔥 لا تعتمد على المسافات البادئة
```bayan
# بيان تستخدم الأقواس { } مثل C/Java
if (x > 5) { print("كبير") }
```

### 2. 🌍 ثنائية اللغة الكاملة
```bayan
# يمكنك الكتابة بالعربية أو الإنجليزية
def دالة(): { return 5 }
دالة greet(): { أرجع "مرحباً" }
```

### 3. 🧠 ثلاثية النموذج (Hybrid)
```bayan
hybrid {
    x = 10              # إجرائي
    class A: { }        # كائني
    fact loves(a,b).    # منطقي
}
```

### 4. 📝 استعلامات منطقية مدمجة
```bayan
query parent(?X, "أحمد").  # من هو والد أحمد؟
```

---

## 🚀 جرّب بنفسك!

### التثبيت:
```bash
git clone https://github.com/mubtakir/nlp_bayan.git
cd nlp_bayan
python run_bayan.py examples/entity_new_syntax_demo.by
```

### أو جرّب Web IDE:
```bash
cd web_ide
python app.py
# ثم افتح http://127.0.0.1:5000
```

---

## 📚 موارد إضافية

- **[BAYAN_FOR_AI_MODELS.md](BAYAN_FOR_AI_MODELS.md)** - دليل شامل للغة
- **[docs/ENTITY_SYSTEM_GUIDE.md](docs/ENTITY_SYSTEM_GUIDE.md)** - نظام الكيانات
- **[docs/CAUSAL_NETWORK_ENGINE_GUIDE.md](docs/CAUSAL_NETWORK_ENGINE_GUIDE.md)** - الشبكات السببية
- **[examples/](examples/)** - 74 مثال عملي

---

## 🏆 شارك التحدي!

انسخ هذا المنشور وشاركه:

> **🔥 تحدي بيان!**
>
> هل تستطيع كتابة برنامج يجمع:
> - كيانات مع حالات ديناميكية
> - شبكة سببية متسلسلة
> - شبكة دلالية هرمية
> - علاقات وعلمنطق
>
> في **25 سطر فقط** وبدون مكتبات خارجية؟
>
> لغة بيان تفعلها! جرّب:
> https://github.com/mubtakir/nlp_bayan

---

**#بيان #BayanLang #ProgrammingChallenge #ArabicProgramming**
