# 🤖 دليل تشغيل نماذج الذكاء الاصطناعي في بيان
# AI Models Setup Guide for Bayan

**التاريخ**: 2025-12-05  
**الحالة**: دليل شامل لجميع الأوضاع

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الوضع 1: المستقل (Standalone)](#الوضع-1-المستقل-standalone)
3. [الوضع 2: المحلي (Local - Ollama)](#الوضع-2-المحلي-local---ollama)
4. [الوضع 3: السحابي (Cloud - Gemini)](#الوضع-3-السحابي-cloud---gemini)
5. [أمثلة عملية](#أمثلة-عملية)
6. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

لغة بيان تدعم **3 أوضاع** لتشغيل نماذج الذكاء الاصطناعي:

| الوضع | الوصف | المتطلبات | الاستخدام |
|------|-------|-----------|----------|
| **Standalone** | بيان فقط، بدون نماذج خارجية | لا شيء ✅ | الخصوصية القصوى |
| **Local** | Ollama مع نماذج مفتوحة المصدر | Ollama + نموذج | خصوصية + قوة |
| **Cloud** | Gemini 1.5 Pro عبر API | مفتاح API | القوة القصوى |

---

## 🔧 الوضع 1: المستقل (Standalone)

### ✅ المميزات:
- **لا يحتاج أي إعداد** - يعمل مباشرة
- **خصوصية كاملة** - لا يرسل بيانات للخارج
- **سريع** - لا انتظار للشبكة
- **يثبت قدرة بيان المستقلة**

### 🚀 كيفية التشغيل:

#### الطريقة 1: عبر Python مباشرة
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide1
python3 examples/neuro_symbolic_demo.py
```

#### الطريقة 2: عبر كود Python
```python
import sys
sys.path.append('/home/al-mubtakir/Documents/bayan_python_ide1')

from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# إنشاء النظام في الوضع المستقل
loop = NeuroSymbolicLoop(mode="standalone")

# معالجة نص عربي
result = loop.process("أحمد ضرب الكرة", language="arabic")

# عرض النتائج
print("النتيجة:", result['realization'])
print("التفاصيل:", result['dream'])
```

#### الطريقة 3: عبر مفسر بيان
```bayan
hybrid {
    # سيتم إضافة دعم مباشر قريباً
    print("الوضع المستقل يعمل!")
}
```

### 📊 ما يمكنه فعله:
- ✅ تحليل الجمل إلى معادلات لغوية
- ✅ استخراج الكيانات والأفعال
- ✅ استنتاج النتائج المنطقية
- ✅ بناء دوائر منطقية

### ⚠️ القيود:
- محدود بقاعدة المعرفة الحالية
- يحتاج قواعد سببية أكثر للمجالات الجديدة

---

## 🏠 الوضع 2: المحلي (Local - Ollama)

### ✅ المميزات:
- **خصوصية عالية** - كل شيء على جهازك
- **نماذج مفتوحة المصدر** - مجاني تماماً
- **لا يحتاج إنترنت** بعد التحميل
- **قوة معقولة** - نماذج 7B-70B

### 📦 التثبيت:

#### الخطوة 1: تثبيت Ollama
```bash
# على Linux
curl -fsSL https://ollama.com/install.sh | sh

# أو قم بتحميله من:
# https://ollama.com/download
```

#### الخطوة 2: تثبيت مكتبة Python
```bash
pip install ollama
```

#### الخطوة 3: تشغيل خادم Ollama
```bash
# في نافذة طرفية منفصلة
ollama serve
```

#### الخطوة 4: تحميل نموذج
```bash
# النموذج الموصى به (7B - سريع)
ollama pull qwen2.5-coder:7b

# أو نموذج أكبر (14B - أقوى)
ollama pull qwen2.5-coder:14b

# أو نموذج عربي
ollama pull llama3.2:latest
```

### 🚀 كيفية التشغيل:

#### عبر Python:
```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# إنشاء النظام في الوضع المحلي
loop = NeuroSymbolicLoop(mode="local")

# التحقق من الحالة
info = loop.get_info()
print("الحالة:", info['gateway']['available'])

# معالجة نص
result = loop.process("محمد يدرس البرمجة", language="arabic")
print("النتيجة:", result['realization'])
```

#### عبر Demo:
```bash
# عدّل ملف examples/neuro_symbolic_demo.py
# أزل التعليق عن السطر:
# demo_mode("Local (Ollama)", "local", test_input)

python3 examples/neuro_symbolic_demo.py
```

### 🔍 التحقق من التثبيت:
```bash
# تحقق من أن Ollama يعمل
ollama list

# يجب أن ترى النماذج المحملة
# NAME                    ID              SIZE
# qwen2.5-coder:7b        abc123...       4.7GB
```

---

## ☁️ الوضع 3: السحابي (Cloud - Gemini)

### ✅ المميزات:
- **القوة القصوى** - Gemini 1.5 Pro
- **سياق طويل** - حتى 2M رمز
- **فهم متعدد اللغات** - عربي ممتاز
- **تحديثات مستمرة** - أحدث التقنيات

### 📦 التثبيت:

#### الخطوة 1: تثبيت المكتبة
```bash
pip install google-generativeai
```

#### الخطوة 2: الحصول على مفتاح API

1. **اذهب إلى**: https://makersuite.google.com/app/apikey
2. **سجّل دخول** بحساب Google
3. **اضغط "Create API Key"**
4. **انسخ المفتاح** (يبدأ بـ `AIza...`)

#### الخطوة 3: إعداد المفتاح

##### الطريقة 1: متغير بيئة مؤقت
```bash
export GEMINI_API_KEY="AIzaSy..."
```

##### الطريقة 2: ملف .env (موصى به)
```bash
# أنشئ ملف .env في مجلد المشروع
cd /home/al-mubtakir/Documents/bayan_python_ide1
echo 'GEMINI_API_KEY="AIzaSy..."' > .env
```

##### الطريقة 3: في ملف .bashrc (دائم)
```bash
echo 'export GEMINI_API_KEY="AIzaSy..."' >> ~/.bashrc
source ~/.bashrc
```

### 🚀 كيفية التشغيل:

#### عبر Python:
```python
import os
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

# تأكد من وجود المفتاح
print("API Key:", os.getenv("GEMINI_API_KEY")[:10] + "...")

# إنشاء النظام في الوضع السحابي
loop = NeuroSymbolicLoop(mode="cloud")

# التحقق من الحالة
info = loop.get_info()
print("متصل:", info['gateway']['available'])

# معالجة نص
result = loop.process("الطالب المجتهد ينجح في الامتحان", language="arabic")
print("النتيجة:", result['realization'])
```

#### عبر Demo:
```bash
# عدّل ملف examples/neuro_symbolic_demo.py
# أزل التعليق عن السطر:
# demo_mode("Cloud (Gemini)", "cloud", test_input)

python3 examples/neuro_symbolic_demo.py
```

### 💰 التكلفة:
- **مجاني**: 15 طلب/دقيقة، 1500 طلب/يوم
- **مدفوع**: أسعار تنافسية للاستخدام الكثيف

---

## 🎯 أمثلة عملية

### مثال 1: مقارنة الأوضاع الثلاثة

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

test_input = "أحمد ضرب الكرة"

# الوضع المستقل
standalone = NeuroSymbolicLoop(mode="standalone")
result1 = standalone.process(test_input, language="arabic")
print("مستقل:", result1['realization'])

# الوضع المحلي (إذا كان متاحاً)
try:
    local = NeuroSymbolicLoop(mode="local")
    result2 = local.process(test_input, language="arabic")
    print("محلي:", result2['realization'])
except:
    print("محلي: غير متاح")

# الوضع السحابي (إذا كان متاحاً)
try:
    cloud = NeuroSymbolicLoop(mode="cloud")
    result3 = cloud.process(test_input, language="arabic")
    print("سحابي:", result3['realization'])
except:
    print("سحابي: غير متاح")
```

### مثال 2: معالجة نصوص متعددة

```python
from bayan.ai.neuro_symbolic_loop import NeuroSymbolicLoop

loop = NeuroSymbolicLoop(mode="standalone")  # أو "local" أو "cloud"

texts = [
    "محمد يأكل تفاحة",
    "الطالب يدرس الرياضيات",
    "السيارة تسير بسرعة"
]

for text in texts:
    result = loop.process(text, language="arabic")
    print(f"\nالنص: {text}")
    print(f"الذرات: {result['dream']}")
    print(f"النتيجة: {result['realization']}")
```

### مثال 3: استخدام LLM Gateway مباشرة

```python
from bayan.ai.llm_gateway import LLMGateway

# إنشاء Gateway
gateway = LLMGateway(mode="cloud")  # أو "local" أو "standalone"

# توليد نص
response = gateway.generate("اشرح لي البرمجة الكائنية بالعربية")
print(response)

# توليد JSON منظم
schema = {
    "title": "string",
    "concepts": ["string"],
    "difficulty": "easy | medium | hard"
}

structured = gateway.generate_structured(
    "حلل هذا الموضوع: البرمجة الكائنية",
    schema
)
print(structured)
```

---

## 🔧 استكشاف الأخطاء

### المشكلة 1: "Ollama not running"

**الحل:**
```bash
# تأكد من تشغيل Ollama
ollama serve

# في نافذة أخرى، تحقق
ollama list
```

### المشكلة 2: "GEMINI_API_KEY not found"

**الحل:**
```bash
# تحقق من المتغير
echo $GEMINI_API_KEY

# إذا كان فارغاً، أعد تعيينه
export GEMINI_API_KEY="AIzaSy..."

# أو أضفه لـ .bashrc
echo 'export GEMINI_API_KEY="AIzaSy..."' >> ~/.bashrc
source ~/.bashrc
```

### المشكلة 3: "google-generativeai not installed"

**الحل:**
```bash
pip install google-generativeai
```

### المشكلة 4: "ollama not installed"

**الحل:**
```bash
pip install ollama
```

### المشكلة 5: النموذج بطيء جداً

**الحل:**
- **للوضع المحلي**: استخدم نموذج أصغر
  ```bash
  ollama pull qwen2.5-coder:3b  # أصغر وأسرع
  ```
- **للوضع السحابي**: تحقق من اتصال الإنترنت

---

## 📊 مقارنة الأوضاع

| الميزة | Standalone | Local | Cloud |
|--------|-----------|-------|-------|
| **السرعة** | ⚡⚡⚡ سريع جداً | ⚡⚡ سريع | ⚡ متوسط |
| **الخصوصية** | 🔒🔒🔒 كاملة | 🔒🔒 عالية | 🔒 متوسطة |
| **القوة** | 💪 محدودة | 💪💪 جيدة | 💪💪💪 ممتازة |
| **التكلفة** | 💰 مجاني | 💰 مجاني | 💰 مجاني/مدفوع |
| **الإنترنت** | ❌ لا يحتاج | ❌ لا يحتاج | ✅ يحتاج |
| **الإعداد** | ✅ جاهز | ⚙️ متوسط | ⚙️ سهل |

---

## 🎯 متى تستخدم كل وضع؟

### استخدم **Standalone** إذا:
- ✅ تريد خصوصية كاملة
- ✅ لا تحتاج إنترنت
- ✅ تعمل على بيانات حساسة
- ✅ تريد سرعة قصوى

### استخدم **Local** إذا:
- ✅ تريد خصوصية + قوة
- ✅ لديك GPU قوي
- ✅ تعمل على مشاريع كبيرة
- ✅ تريد نماذج مفتوحة المصدر

### استخدم **Cloud** إذا:
- ✅ تريد أفضل أداء
- ✅ تعمل على نصوص معقدة
- ✅ لديك اتصال إنترنت جيد
- ✅ لا مشكلة في إرسال البيانات للسحابة

---

## 🚀 البدء السريع (خطوة واحدة)

### للوضع المستقل (لا يحتاج إعداد):
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide1
python3 examples/neuro_symbolic_demo.py
```

### للوضع المحلي:
```bash
# 1. ثبّت Ollama
curl -fsSL https://ollama.com/install.sh | sh

# 2. شغّل الخادم
ollama serve &

# 3. حمّل نموذج
ollama pull qwen2.5-coder:7b

# 4. ثبّت المكتبة
pip install ollama

# 5. جرّب
python3 examples/neuro_symbolic_demo.py
```

### للوضع السحابي:
```bash
# 1. ثبّت المكتبة
pip install google-generativeai

# 2. احصل على مفتاح من:
# https://makersuite.google.com/app/apikey

# 3. عيّن المفتاح
export GEMINI_API_KEY="AIzaSy..."

# 4. جرّب
python3 examples/neuro_symbolic_demo.py
```

---

## 📚 موارد إضافية

### الوثائق:
- [Ollama Documentation](https://github.com/ollama/ollama)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [Bayan Project Architecture](PROJECT_ARCHITECTURE_GUIDE.md)

### أمثلة:
- `examples/neuro_symbolic_demo.py` - عرض الأوضاع الثلاثة
- `examples/dual_brain_demo.py` - نظام الدماغ المزدوج
- `examples/istinbat_demo.py` - محرك الاستنباط

### الملفات المصدرية:
- `bayan/ai/llm_gateway.py` - واجهة النماذج اللغوية
- `bayan/ai/neuro_symbolic_loop.py` - حلقة الدمج العصبي-الرمزي
- `bayan/bayan/istinbat_engine.py` - محرك الاستنباط
- `bayan/bayan/dynamic_builder.py` - باني الدوائر الديناميكي

---

## ✅ قائمة التحقق

قبل البدء:
- [ ] قرأت هذا الدليل
- [ ] اخترت الوضع المناسب لاحتياجاتك
- [ ] ثبّت المتطلبات اللازمة

للوضع المحلي:
- [ ] ثبّت Ollama
- [ ] شغّلت `ollama serve`
- [ ] حمّلت نموذج (`ollama pull qwen2.5-coder:7b`)
- [ ] ثبّت `pip install ollama`

للوضع السحابي:
- [ ] ثبّت `pip install google-generativeai`
- [ ] حصلت على مفتاح API من Google
- [ ] عيّنت `GEMINI_API_KEY`

---

## 🎉 جاهز للبدء!

الآن لديك كل ما تحتاجه لتشغيل نماذج الذكاء الاصطناعي في بيان!

**اختر الوضع المناسب وابدأ التجربة! 🚀**

---

**آخر تحديث**: 2025-12-05  
**الإصدار**: 1.0  
**المؤلف**: مشروع لغة بيان
