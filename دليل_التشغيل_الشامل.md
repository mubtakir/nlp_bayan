# 📖 دليل التشغيل الشامل لنظام بيان وبصيرة

## 🎯 نظرة عامة

هذا الدليل يغطي جميع طرق التفاعل مع نظامي **بيان** (اللغوي) و**بصيرة** (البصري).

---

## 📋 فهرس الواجهات

| الواجهة | النظام | الملف | الوظيفة |
|---------|--------|-------|---------|
| 🖥️ CLI بيان | بيان | `bayan_cli.py` | تحليل لغوي، لهجات، تعليم |
| 🖥️ CLI بصيرة | بصيرة | `basera_main.py` | استنباط بصري، وكيل ذكي |
| 🌐 Web IDE | بيان | `web_ide/app.py` | محرر كود مرئي |
| 🎨 Gradio | بصيرة | `multi_user_interfaces.py` | واجهة تفاعلية |
| 📡 API | كلاهما | `multi_user_interfaces.py` | واجهة برمجية |

---

## ⚡ التشغيل السريع

### 1️⃣ واجهة بيان التفاعلية (CLI)

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2
python3 bayan_cli.py
```

**الأوامر المتاحة:**

| الأمر | الوظيفة | مثال |
|-------|---------|-------|
| `<جملة>` | تحليل جملة عربية | `أحمد ضرب الكرة` |
| `convert <لهجة> <نص>` | تحويل لهجة للفصحى | `convert مصري عايز آكل` |
| `detect <نص>` | اكتشاف اللهجة | `detect عايز أروح` |
| `learn` | 🎓 بدء الدروس التفاعلية | `learn` |
| `visualize <جملة>` | 📊 تصور المعادلة بصرياً | `visualize أحمد يأكل` |
| `chat` | 🤖 وضع المحادثة الذكية | `chat` |
| `baserah <كلمة>` | 👁️ تحليل بصري-دلالي | `baserah عقل` |
| `letter <حرف>` | 📝 تحليل شكل الحرف | `letter ع` |
| `compare <ح1> <ح2>` | 🔍 مقارنة حرفين | `compare ب ت` |
| `stats` | إحصائيات النظام | `stats` |
| `dialects` | اللهجات المدعومة | `dialects` |
| `exit` | الخروج | `exit` |

---

### 2️⃣ واجهة بصيرة التفاعلية (CLI)

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 basera_main.py
```

**الأوامر المتاحة:**

| الأمر | الوظيفة | مثال |
|-------|---------|-------|
| `status` | 📊 حالة النظام | `status` |
| `test` | 🧪 اختبار شامل | `test` |
| `search <استعلام>` | 🔍 البحث في المعرفة | `search الذكاء الاصطناعي` |
| `design <محتوى>` | 🎨 إنشاء تصميم فني | `design شعار شركة` |
| `infer <وصف_صورة>` | 👁️ استنباط معادلة من وصف صورة | `infer دائرة حمراء` |
| `think <فكرة>` | 🧠 معالجة فكرة تفكيرياً | `think كيف يعمل العقل` |
| `solve <مشكلة>` | 🤖 حل مشكلة بالوكيل الذكي | `solve تحليل كلمة عربية` |
| `theories` | 🧬 عرض النظريات الثورية | `theories` |
| `components` | 🔧 المكونات المحملة | `components` |
| `exit` | 🚪 الخروج | `exit` |

---

### 3️⃣ واجهة الويب (Web IDE)

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2
python3 web_ide/app.py
```

ثم افتح المتصفح على: **http://127.0.0.1:5001/ide**

**الصفحات المتاحة:**

| الصفحة | الرابط | الوظيفة |
|--------|--------|---------|
| المحرر الرئيسي | `/ide` | كتابة وتنفيذ كود بيان |
| الرسم البياني المنطقي | `/logic_graph` | تصور العلاقات المنطقية |
| ملعب الذكاء الاصطناعي | `/ai_playground` | تجربة ميزات AI |
| المصحح | `/debugger` | تصحيح الأخطاء |

---

### 4️⃣ واجهة Gradio التفاعلية (بصيرة)

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 knowledge/knowledge_feeding_interface.py --gradio
```

**أو استخدم الواجهات المتعددة:**

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 advanced/multi_user_interfaces.py
```

---

## 🎓 الاستخدامات العملية

### 📝 1. تحليل كلمة ومعناها

```bash
# في واجهة بيان
python3 bayan_cli.py
بيان> baserah عقل
```

**النتيجة:**
```
👁️ تحليل بصيرة للكلمة: عقل
📊 المعاني: العمق, الرؤية, العمل, المعرفة, الإحاطة
🧵 التماسك: 0.446
📍 الموقع: متوازن - إيجابي - عميق
```

---

### 🔤 2. تحليل شكل الحرف

```bash
بيان> letter ع
```

**النتيجة:**
```
📝 تحليل الحرف: ع
🔍 الشكل: مفتوح
📚 المعاني: العمق, الرؤية, العمل, المعرفة
📐 المعادلة: open_curve
⚡ الضد البصري: ص, ض, ط
```

---

### 🖼️ 3. إدخال صورة والتعرف عليها

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 -c "
from advanced.revolutionary_image_inference_system import RevolutionaryImageInferenceSystem
system = RevolutionaryImageInferenceSystem()
result = system.infer_equation_from_image('path/to/image.png')
print(result)
"
```

**أو استخدم الواجهة التفاعلية:**

```bash
python3 basera_main.py
🧬 بصيرة> infer دائرة حمراء كبيرة
```

---

### 🧪 4. اختبار دقة استنباط الأشكال

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 -c "
from advanced.comprehensive_testing_system import ComprehensiveTestingSystem
tester = ComprehensiveTestingSystem()
results = tester.run_all_tests()
print(f'دقة الاستنباط: {results[\"accuracy\"]*100:.1f}%')
"
```

---

### 💡 5. إنشاء كلمة من معنى (اختراع كلمة)

```bash
python3 bayan_cli.py
بيان> baserah قوة+سرعة+علو
```

**أو برمجياً:**

```python
from extensions.bayan_baserah_bridge import BayanBaserahBridge
from extensions.visual_semantic_engine import VisualSemanticEngine

bridge = BayanBaserahBridge()
engine = VisualSemanticEngine()

# اختراع كلمة من معاني
meanings = ["قوة", "سرعة", "علو"]
suggested_letters = bridge.suggest_letters_for_meaning(meanings)
print(f"الحروف المقترحة: {suggested_letters}")
```

---

### 🎓 6. النظام التعليمي التفاعلي

```bash
python3 bayan_cli.py
بيان> learn
```

**الدروس المتاحة:**

| الدرس | الموضوع |
|-------|---------|
| 1 | مقدمة في المعادلات اللغوية |
| 2 | تحليل الجمل العربية |
| 3 | الاستنباط والاستدلال |

---

### 🧠 7. تدريب وتعليم النظام

#### أ. تغذية المعرفة (بصيرة):

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/baserah_ai-main
python3 -c "
from knowledge.knowledge_feeding_system import KnowledgeFeedingSystem
feeder = KnowledgeFeedingSystem()

# تغذية من ملف
feeder.process_file('data.json', category='linguistic')

# تغذية نص مباشر
feeder.feed_text('المعرفة الجديدة هنا', category='general')
"
```

#### ب. إضافة حقائق جديدة (بيان):

```python
from bayan.bayan.linguistic_equation import KnowledgeBase

kb = KnowledgeBase()
kb.add_fact("جديد", {"نوع": "اسم", "معنى": "شيء حديث"})
```

#### ج. تعلم الوكيل الذكي:

```bash
python3 basera_main.py
🧬 بصيرة> solve مشكلة معقدة
# بعد الحل، أعطِ تغذية راجعة
```

```python
from advanced.revolutionary_intelligent_agent import RevolutionaryIntelligentAgent
agent = RevolutionaryIntelligentAgent()
agent.learn_from_feedback("ممتاز، الحل صحيح!", task_id="task_123")
```

---

## 🔌 واجهة API البرمجية

### استخدام Python مباشرة:

```python
# === بيان ===
from extensions.extended_istinbat import ExtendedIstinbatEngine
from extensions.dialect_adapter import DialectAdapter

engine = ExtendedIstinbatEngine()
result = engine.process("أحمد أكل التفاحة")

adapter = DialectAdapter()
formal = adapter.to_formal("عايز آكل", "مصري")

# === بصيرة ===
from extensions.bayan_baserah_bridge import BayanBaserahBridge
from extensions.visual_semantic_engine import VisualSemanticEngine

bridge = BayanBaserahBridge()
analysis = bridge.word_visual_analysis("حكمة")

engine = VisualSemanticEngine()
full = engine.full_analysis("عقل")
```

---

## 📊 جدول مقارنة الواجهات

| الميزة | CLI بيان | CLI بصيرة | Web IDE | Gradio |
|--------|----------|-----------|---------|--------|
| تحليل جمل | ✅ | ❌ | ✅ | ❌ |
| لهجات | ✅ | ❌ | ❌ | ❌ |
| تحليل حروف | ✅ | ❌ | ❌ | ❌ |
| استنباط صور | ❌ | ✅ | ❌ | ✅ |
| وكيل ذكي | ❌ | ✅ | ❌ | ✅ |
| تغذية معرفة | ❌ | ✅ | ❌ | ✅ |
| تنفيذ كود | ❌ | ❌ | ✅ | ❌ |
| رسوم بيانية | ❌ | ❌ | ✅ | ❌ |

---

## 🛠️ متطلبات التشغيل

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# متطلبات بصيرة إضافية
pip install -r baserah_ai-main/requirements.txt

# للواجهات الرسومية
pip install gradio flask
```

---

## 🆘 استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| `ModuleNotFoundError` | تأكد من تشغيل من المجلد الصحيح |
| `Gradio غير متوفر` | `pip install gradio` |
| `Flask غير متوفر` | `pip install flask` |
| واجهة لا تعمل | تحقق من الـ port (5001/8000/7860) |

---

---

## 🌍 إضافة لهجة جديدة

### 📁 أماكن تخزين اللهجات:

```
extensions/dialects/
├── egyptian.json      # المصرية
├── gulf.json         # الخليجية
├── levantine.json    # الشامية (سيتم إنشاؤه)
├── moroccan.json     # المغربية (سيتم إنشاؤه)
└── custom/           # لهجات المستخدم المخصصة
    └── sudanese.json # مثال: السودانية
```

### الطريقة 1: إنشاء ملف JSON

```json
{
  "name": "السودانية",
  "code": "sudanese",
  "region": "السودان",
  "vocabulary": {
    "داير": "يريد",
    "مشيت": "ذهبت",
    "جيت": "جئت",
    "شنو": "ماذا",
    "كيفن": "كيف",
    "وين": "أين",
    "زول": "شخص",
    "هسي": "الآن"
  },
  "markers": ["داير", "زول", "كيفن", "هسي"],
  "examples": [
    {
      "dialect": "داير أمشي السوق",
      "standard": "أريد أن أذهب إلى السوق"
    }
  ]
}
```

### الطريقة 2: إضافة برمجياً

```python
from extensions.dialect_adapter import DialectAdapter

adapter = DialectAdapter()

# إضافة لهجة جديدة
adapter.add_dialect(
    code='sudanese',
    name='السودانية',
    vocabulary={
        'داير': 'يريد',
        'زول': 'شخص',
        'هسي': 'الآن'
    },
    markers=['داير', 'زول', 'هسي']
)

# حفظ اللهجة لملف JSON
adapter.save_dialect_to_file('sudanese')

# اختبار التحويل
result = adapter.convert_to_standard('داير أمشي هسي', 'sudanese')
print(result.converted)  # يريد أمشي الآن
```

### الطريقة 3: إضافة كلمة واحدة

```python
adapter.add_word('egyptian', 'يلا', 'هيا')
```

### 📋 عرض اللهجات المتاحة

```python
from extensions.dialect_adapter import DialectAdapter
adapter = DialectAdapter()
print(adapter.list_dialects())
# {'egyptian': 56, 'gulf': 42, 'levantine': 28, 'moroccan': 29}
```

---

## 📞 للمساعدة

- 📖 وثائق بيان: `docs/` و `docs-new/`
- 📖 وثائق بصيرة: `baserah_ai-main/docs/`
- 🧬 النظريات: `baserah_ai-main/developer_guides/`
- 🌍 نظام اللهجات: `extensions/dialects/README.md`

