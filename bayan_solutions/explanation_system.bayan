# ═══════════════════════════════════════════════════════════════════════════
# Explanation System - نظام التفسير
# ═══════════════════════════════════════════════════════════════════════════
# 
# نظام ذكي لتفسير الاستعلامات والنتائج بطريقة واضحة ومفهومة
# An intelligent system to explain queries and results in a clear way
#
# الدوال المتوفرة:
# 1. اشرح_سبب_نتيجة() / explain_cause_effect() - شرح قانون سببي
# 2. اشرح_علاقة() / explain_relation() - شرح علاقة دلالية
# 3. اشرح_مسار() / explain_path() - شرح مسار بين مفهومين
# 4. اشرح_استعلام() / explain_query() - شرح نتيجة استعلام
# 5. اشرح_تلقائي() / auto_explain() - تفسير تلقائي ذكي
#
# ═══════════════════════════════════════════════════════════════════════════

هجين {
    # ═══════════════════════════════════════════════════════════════════════
    # 1. شرح قانون سببي
    # ═══════════════════════════════════════════════════════════════════════
    
    دالة اشرح_سبب_نتيجة(شرط, نتيجة, سبب, قوة) {
        # تحديد مستوى القوة
        مستوى_القوة = "".
        إذا (قوة >= 0.9) {
            مستوى_القوة = "قوي جداً (شبه مؤكد)".
        } وإلا_إذا (قوة >= 0.7) {
            مستوى_القوة = "قوي (محتمل جداً)".
        } وإلا_إذا (قوة >= 0.5) {
            مستوى_القوة = "متوسط (محتمل)".
        } وإلا {
            مستوى_القوة = "ضعيف (احتمال منخفض)".
        }
        
        # بناء التفسير بالعربية
        تفسير_عربي = "📖 التفسير:\n".
        تفسير_عربي = تفسير_عربي + "   عندما يحدث: " + شرط + "\n".
        تفسير_عربي = تفسير_عربي + "   النتيجة: " + نتيجة + "\n".
        تفسير_عربي = تفسير_عربي + "   السبب الفيزيائي/المنطقي: " + سبب + "\n".
        تفسير_عربي = تفسير_عربي + "   قوة العلاقة: " + قوة + " (" + مستوى_القوة + ")".
        
        # بناء التفسير بالإنجليزية
        strength_level_en = "".
        إذا (قوة >= 0.9) {
            strength_level_en = "very strong (almost certain)".
        } وإلا_إذا (قوة >= 0.7) {
            strength_level_en = "strong (very likely)".
        } وإلا_إذا (قوة >= 0.5) {
            strength_level_en = "medium (likely)".
        } وإلا {
            strength_level_en = "weak (low probability)".
        }
        
        تفسير_إنجليزي = "📖 Explanation:\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   When: " + شرط + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Result: " + نتيجة + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Physical/Logical Cause: " + سبب + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Strength: " + قوة + " (" + strength_level_en + ")".
        
        إرجاع {
            "عربي": تفسير_عربي,
            "english": تفسير_إنجليزي,
            "مستوى_القوة": مستوى_القوة,
            "strength_level": strength_level_en
        }.
    }
    
    دالة explain_cause_effect(condition, result, cause, strength) {
        إرجاع اشرح_سبب_نتيجة(condition, result, cause, strength).
    }
    
    # ═══════════════════════════════════════════════════════════════════════
    # 2. شرح علاقة دلالية
    # ═══════════════════════════════════════════════════════════════════════
    
    دالة اشرح_علاقة(من, نوع_العلاقة, إلى, قوة) {
        # تحديد نوع العلاقة
        وصف_العلاقة = "".
        إذا (نوع_العلاقة == "في" أو نوع_العلاقة == "فيه") {
            وصف_العلاقة = "علاقة مكانية (spatial relation)".
        } وإلا_إذا (نوع_العلاقة == "هو" أو نوع_العلاقة == "هي") {
            وصف_العلاقة = "علاقة هوية (identity relation)".
        } وإلا_إذا (نوع_العلاقة == "له" أو نوع_العلاقة == "لها") {
            وصف_العلاقة = "علاقة ملكية (possession relation)".
        } وإلا_إذا (نوع_العلاقة == "ب" أو نوع_العلاقة == "بواسطة") {
            وصف_العلاقة = "علاقة أداة (instrumental relation)".
        } وإلا_إذا (نوع_العلاقة == "لل" أو نوع_العلاقة == "من_أجل") {
            وصف_العلاقة = "علاقة غاية (purpose relation)".
        } وإلا {
            وصف_العلاقة = "علاقة عامة (general relation)".
        }
        
        # بناء التفسير
        تفسير_عربي = "📖 التفسير:\n".
        تفسير_عربي = تفسير_عربي + "   المفهوم الأول: " + من + "\n".
        تفسير_عربي = تفسير_عربي + "   نوع العلاقة: " + نوع_العلاقة + " (" + وصف_العلاقة + ")\n".
        تفسير_عربي = تفسير_عربي + "   المفهوم الثاني: " + إلى + "\n".
        تفسير_عربي = تفسير_عربي + "   قوة العلاقة: " + قوة.
        
        تفسير_إنجليزي = "📖 Explanation:\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   First Concept: " + من + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Relation Type: " + نوع_العلاقة + " (" + وصف_العلاقة + ")\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Second Concept: " + إلى + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Strength: " + قوة.
        
        إرجاع {
            "عربي": تفسير_عربي,
            "english": تفسير_إنجليزي,
            "نوع": وصف_العلاقة
        }.
    }
    
    دالة explain_relation(from, relation_type, to, strength) {
        إرجاع اشرح_علاقة(from, relation_type, to, strength).
    }
    
    # ═══════════════════════════════════════════════════════════════════════
    # 3. شرح مسار بين مفهومين
    # ═══════════════════════════════════════════════════════════════════════
    
    دالة اشرح_مسار(من, إلى, مسار, طول) {
        تفسير_عربي = "📖 التفسير:\n".
        تفسير_عربي = تفسير_عربي + "   المسار من '" + من + "' إلى '" + إلى + "':\n".
        
        إذا (طول == 0) {
            تفسير_عربي = تفسير_عربي + "   ❌ لا يوجد مسار مباشر أو غير مباشر".
        } وإلا_إذا (طول == 1) {
            تفسير_عربي = تفسير_عربي + "   ✅ مسار مباشر (خطوة واحدة)".
        } وإلا {
            تفسير_عربي = تفسير_عربي + "   ✅ مسار غير مباشر (" + طول + " خطوات)".
        }
        
        تفسير_إنجليزي = "📖 Explanation:\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Path from '" + من + "' to '" + إلى + "':\n".
        
        إذا (طول == 0) {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ❌ No direct or indirect path found".
        } وإلا_إذا (طول == 1) {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ✅ Direct path (one step)".
        } وإلا {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ✅ Indirect path (" + طول + " steps)".
        }
        
        إرجاع {
            "عربي": تفسير_عربي,
            "english": تفسير_إنجليزي,
            "الطول": طول
        }.
    }
    
    دالة explain_path(from, to, path, length) {
        إرجاع اشرح_مسار(from, to, path, length).
    }

    # ═══════════════════════════════════════════════════════════════════════
    # 4. شرح نتيجة استعلام
    # ═══════════════════════════════════════════════════════════════════════

    دالة اشرح_استعلام(نوع_الاستعلام, نتائج, عدد_النتائج) {
        تفسير_عربي = "📖 تفسير الاستعلام:\n".
        تفسير_عربي = تفسير_عربي + "   نوع الاستعلام: " + نوع_الاستعلام + "\n".
        تفسير_عربي = تفسير_عربي + "   عدد النتائج: " + عدد_النتائج + "\n".

        إذا (عدد_النتائج == 0) {
            تفسير_عربي = تفسير_عربي + "   ❌ لم يتم العثور على نتائج مطابقة".
        } وإلا_إذا (عدد_النتائج == 1) {
            تفسير_عربي = تفسير_عربي + "   ✅ تم العثور على نتيجة واحدة".
        } وإلا {
            تفسير_عربي = تفسير_عربي + "   ✅ تم العثور على نتائج متعددة".
        }

        تفسير_إنجليزي = "📖 Query Explanation:\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Query Type: " + نوع_الاستعلام + "\n".
        تفسير_إنجليزي = تفسير_إنجليزي + "   Results Count: " + عدد_النتائج + "\n".

        إذا (عدد_النتائج == 0) {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ❌ No matching results found".
        } وإلا_إذا (عدد_النتائج == 1) {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ✅ One result found".
        } وإلا {
            تفسير_إنجليزي = تفسير_إنجليزي + "   ✅ Multiple results found".
        }

        إرجاع {
            "عربي": تفسير_عربي,
            "english": تفسير_إنجليزي,
            "عدد_النتائج": عدد_النتائج
        }.
    }

    دالة explain_query(query_type, results, results_count) {
        إرجاع اشرح_استعلام(query_type, results, results_count).
    }

    # ═══════════════════════════════════════════════════════════════════════
    # 5. تفسير تلقائي ذكي
    # ═══════════════════════════════════════════════════════════════════════

    دالة اشرح_تلقائي(سؤال) {
        تفسير = "📖 تفسير تلقائي:\n".
        تفسير = تفسير + "   السؤال: " + سؤال + "\n\n".

        # تحليل السؤال
        إذا (سؤال يحتوي "لماذا" أو سؤال يحتوي "why") {
            تفسير = تفسير + "   💡 هذا سؤال سببي - يبحث عن السبب وراء نتيجة\n".
            تفسير = تفسير + "   استخدم: استعلام سبب_نتيجة(?شرط, النتيجة, ?سبب, ?قوة)".
        } وإلا_إذا (سؤال يحتوي "ماذا" أو سؤال يحتوي "what") {
            تفسير = تفسير + "   💡 هذا سؤال تنبؤي - يبحث عن النتيجة المتوقعة\n".
            تفسير = تفسير + "   استخدم: استعلام سبب_نتيجة(الشرط, ?نتيجة, ?سبب, ?قوة)".
        } وإلا_إذا (سؤال يحتوي "كيف" أو سؤال يحتوي "how") {
            تفسير = تفسير + "   💡 هذا سؤال عن المسار - يبحث عن العلاقة بين مفهومين\n".
            تفسير = تفسير + "   استخدم: استعلام علاقة(من, ?نوع, إلى, ?قوة)".
        } وإلا_إذا (سؤال يحتوي "أين" أو سؤال يحتوي "where") {
            تفسير = تفسير + "   💡 هذا سؤال مكاني - يبحث عن موقع\n".
            تفسير = تفسير + "   استخدم: استعلام علاقة(الشيء, 'في', ?مكان, ?قوة)".
        } وإلا {
            تفسير = تفسير + "   💡 سؤال عام - جرب استعلامات مختلفة".
        }

        إرجاع {
            "التفسير": تفسير,
            "السؤال": سؤال
        }.
    }

    دالة auto_explain(question) {
        explanation = "📖 Auto Explanation:\n".
        explanation = explanation + "   Question: " + question + "\n\n".

        # Analyze question
        إذا (question يحتوي "why" أو question يحتوي "لماذا") {
            explanation = explanation + "   💡 This is a causal question - looking for the cause\n".
            explanation = explanation + "   Use: query cause_effect(?condition, result, ?cause, ?strength)".
        } وإلا_إذا (question يحتوي "what" أو question يحتوي "ماذا") {
            explanation = explanation + "   💡 This is a predictive question - looking for result\n".
            explanation = explanation + "   Use: query cause_effect(condition, ?result, ?cause, ?strength)".
        } وإلا_إذا (question يحتوي "how" أو question يحتوي "كيف") {
            explanation = explanation + "   💡 This is a path question - looking for relation\n".
            explanation = explanation + "   Use: query relation(from, ?type, to, ?strength)".
        } وإلا_إذا (question يحتوي "where" أو question يحتوي "أين") {
            explanation = explanation + "   💡 This is a spatial question - looking for location\n".
            explanation = explanation + "   Use: query relation(thing, 'in', ?location, ?strength)".
        } وإلا {
            explanation = explanation + "   💡 General question - try different queries".
        }

        إرجاع {
            "explanation": explanation,
            "question": question
        }.
    }

    # ═══════════════════════════════════════════════════════════════════════
    # 6. دالة مساعدة: طباعة تفسير
    # ═══════════════════════════════════════════════════════════════════════

    دالة اطبع_تفسير(تفسير, لغة) {
        إذا (لغة == "عربي" أو لغة == "ar") {
            طباعة(تفسير["عربي"]).
        } وإلا {
            طباعة(تفسير["english"]).
        }
    }

    دالة print_explanation(explanation, language) {
        إذا (language == "arabic" أو language == "ar") {
            طباعة(explanation["عربي"]).
        } وإلا {
            طباعة(explanation["english"]).
        }
    }
}


