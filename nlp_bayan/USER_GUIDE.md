# 📖 دليل المستخدم - User Guide

## 🚀 كيفية التشغيل - How to Run

### الطريقة 1: تشغيل المثال التوضيحي الكامل

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide4
python3 bayan/main.py nlp_bayan/examples/intelligent_dialogue_demo.bayan
```

هذا سيشغل مثال كامل يوضح:
- ✅ إضافة معرفة أولية
- ✅ حوار تفاعلي مع 5 أسئلة
- ✅ تقييم الإجابات
- ✅ التعلم من الحوارات
- ✅ تحديث النظام
- ✅ عرض الإحصائيات

---

### الطريقة 2: استخدام النظام في كودك الخاص

أنشئ ملف جديد (مثلاً `my_test.bayan`):

```bayan
# استيراد النظام الذكي
# (في لغة البيان، الاستيراد تلقائي من نفس المشروع)

# 1. إضافة معرفة أولية
إضافة_حقيقة("Python", "هي", "لغة برمجة عالية المستوى", 0.95, "معرفة_أولية")
إضافة_حقيقة("لغة_البيان", "هي", "لغة برمجة هجينة", 0.95, "معرفة_أولية")

# 2. طرح سؤال
إجابة = سؤال("ما هي Python؟")
طباعة("الإجابة:", إجابة)

# 3. سؤال مع تفاصيل
نتيجة = سؤال_مع_تفاصيل("ما هي لغة البيان؟")
طباعة("الإجابة:", نتيجة["إجابة"])
طباعة("الثقة:", نتيجة["ثقة"])

# 4. تقييم الإجابة
تقييم_إجابة("ما هي لغة البيان؟", نتيجة["إجابة"], 0.9, "ممتاز")

# 5. حالة النظام
الحالة = حالة_النظام()
طباعة("عدد الحوارات:", الحالة["النظام"]["عدد_الحوارات"])
```

ثم شغّله:
```bash
python3 bayan/main.py my_test.bayan
```

---

### الطريقة 3: استخدام تفاعلي (REPL)

```bash
python3 bayan/main.py
```

ثم اكتب الأوامر مباشرة:
```bayan
>>> إضافة_حقيقة("AI", "هو", "ذكاء اصطناعي", 0.9, "مستخدم")
>>> إجابة = سؤال("ما هو AI؟")
>>> طباعة(إجابة)
```

---

## 🎓 كيفية التدريب - How to Train

### 1. التدريب من الحوارات

```bayan
# إضافة حوارات تدريبية
الحوارات_التدريبية = [
    {
        "سؤال": "ما هو الذكاء الاصطناعي؟",
        "إجابة": "الذكاء الاصطناعي هو محاكاة الذكاء البشري بواسطة الآلات",
        "تقييم": 0.95
    },
    {
        "سؤال": "كيف يعمل التعلم الآلي؟",
        "إجابة": "التعلم الآلي يعمل من خلال تدريب نماذج على البيانات",
        "تقييم": 0.9
    },
    {
        "سؤال": "ما هي الشبكات العصبية؟",
        "إجابة": "الشبكات العصبية هي نماذج حسابية مستوحاة من الدماغ البشري",
        "تقييم": 0.92
    }
]

# تدريب النظام
لكل حوار في الحوارات_التدريبية:
{
    التعلم_من_حوار(
        حوار["سؤال"],
        حوار["إجابة"],
        حوار["تقييم"]
    )
}

# تحديث النظام بعد التدريب
تحديث_النظام_الذكي()

طباعة("✅ تم التدريب على", len(الحوارات_التدريبية), "حوار")
```

---

### 2. التدريب من ملف

أنشئ ملف `training_data.json`:
```json
[
    {
        "question": "ما هو الذكاء الاصطناعي؟",
        "answer": "الذكاء الاصطناعي هو محاكاة الذكاء البشري بواسطة الآلات",
        "rating": 0.95
    },
    {
        "question": "كيف يعمل التعلم الآلي؟",
        "answer": "التعلم الآلي يعمل من خلال تدريب نماذج على البيانات",
        "rating": 0.9
    }
]
```

ثم في كود البيان:
```bayan
# قراءة البيانات التدريبية
البيانات = قراءة_json("training_data.json")

# تدريب النظام
لكل عنصر في البيانات:
{
    التعلم_من_حوار(
        عنصر["question"],
        عنصر["answer"],
        عنصر["rating"]
    )
}

تحديث_النظام_الذكي()
```

---

### 3. التدريب التفاعلي

```bayan
# حلقة تدريب تفاعلية
لكل i في range(10):
{
    # طرح سؤال
    السؤال = إدخال("اكتب سؤالاً: ")
    
    # الحصول على إجابة
    النتيجة = سؤال_مع_تفاصيل(السؤال)
    طباعة("الإجابة:", النتيجة["إجابة"])
    
    # طلب تقييم
    التقييم = float(إدخال("قيّم الإجابة (0-1): "))
    التعليق = إدخال("تعليق (اختياري): ")
    
    # تقييم وتعلم
    تقييم_إجابة(السؤال, النتيجة["إجابة"], التقييم, التعليق)
    
    طباعة("✅ تم التعلم من هذا الحوار")
    طباعة("")
}

# تحديث النظام
تحديث_النظام_الذكي()
طباعة("🎉 انتهى التدريب!")
```

---

## 🔗 التكامل مع أنظمة أخرى - Integration with Other Systems

### 1. التكامل مع REST API

```bayan
# إنشاء خادم API بسيط
دالة معالج_سؤال_api(الطلب):
{
    السؤال = الطلب["question"]
    النتيجة = سؤال_مع_تفاصيل(السؤال)
    
    إرجاع {
        "answer": النتيجة["إجابة"],
        "confidence": النتيجة["ثقة"],
        "sources": النتيجة["مصادر"]
    }
}

# تشغيل الخادم
# (يحتاج مكتبة web في لغة البيان)
```

---

### 2. التكامل مع قاعدة بيانات

```bayan
# حفظ المعرفة في قاعدة بيانات
دالة حفظ_معرفة_في_قاعدة_بيانات():
{
    الإحصائيات = إحصائيات_قاعدة_المعرفة()
    
    # حفظ الحقائق
    لكل حقيقة في الإحصائيات["الحقائق"]:
    {
        # استخدام مكتبة قاعدة البيانات
        # db.insert("facts", حقيقة)
    }
    
    # حفظ القواعد
    لكل قاعدة في الإحصائيات["القواعد"]:
    {
        # db.insert("rules", قاعدة)
    }
}

# تحميل المعرفة من قاعدة بيانات
دالة تحميل_معرفة_من_قاعدة_بيانات():
{
    # الحقائق = db.select("facts")
    # لكل حقيقة في الحقائق:
    # {
    #     إضافة_حقيقة(...)
    # }
}
```

---

### 3. التكامل مع Telegram Bot

```bayan
# معالج رسائل Telegram
دالة معالج_رسالة_telegram(الرسالة):
{
    السؤال = الرسالة["text"]
    معرف_المستخدم = الرسالة["user_id"]
    
    # الحصول على إجابة
    النتيجة = سؤال_مع_تفاصيل(السؤال)
    
    # إرسال الإجابة
    إرسال_رسالة_telegram(
        معرف_المستخدم,
        النتيجة["إجابة"]
    )
    
    # حفظ الحوار للتعلم لاحقاً
    حفظ_حوار_للمراجعة(السؤال, النتيجة["إجابة"])
}
```

---

### 4. التكامل مع نظام الملفات

```bayan
# قراءة معرفة من ملفات نصية
دالة استيراد_معرفة_من_ملفات(المسار):
{
    الملفات = قائمة_ملفات(المسار)
    
    لكل ملف في الملفات:
    {
        المحتوى = قراءة_ملف(ملف)
        
        # استخراج حقائق من المحتوى
        الحقائق = استخراج_حقائق_من_نص(المحتوى)
        
        لكل حقيقة في الحقائق:
        {
            إضافة_حقيقة(
                حقيقة["موضوع"],
                حقيقة["محمول"],
                حقيقة["قيمة"],
                0.7,
                "ملف:" + ملف
            )
        }
    }
    
    تحديث_النظام_الذكي()
}
```

---

## 📊 مراقبة الأداء - Performance Monitoring

```bayan
# دالة: مراقبة الأداء
دالة مراقبة_الأداء():
{
    الحالة = حالة_النظام()
    
    طباعة("=" * 60)
    طباعة("📊 تقرير الأداء")
    طباعة("=" * 60)
    
    طباعة("🤖 النظام:")
    طباعة("   • عدد الحوارات:", الحالة["النظام"]["عدد_الحوارات"])
    طباعة("   • معدل الأداء:", الحالة["النظام"]["معدل_الأداء"])
    
    طباعة("📚 المعرفة:")
    طباعة("   • الحقائق:", الحالة["المعرفة"]["عدد_الحقائق"])
    طباعة("   • القواعد:", الحالة["المعرفة"]["عدد_القواعد"])
    طباعة("   • الأنماط:", الحالة["المعرفة"]["عدد_الأنماط"])
    
    طباعة("🎓 التعلم:")
    طباعة("   • معدل النجاح:", الحالة["التعلم"]["معدل_النجاح"])
    طباعة("   • الأنماط المتعلمة:", الحالة["التعلم"]["عدد_الأنماط"])
}
```

---

## 🎯 نصائح للاستخدام الأمثل

1. **ابدأ بمعرفة أولية جيدة**: أضف حقائق أساسية قبل البدء
2. **قيّم الإجابات دائماً**: هذا يساعد النظام على التعلم
3. **حدّث النظام بانتظام**: استخدم `تحديث_النظام_الذكي()` بعد كل جلسة
4. **راقب الأداء**: استخدم `حالة_النظام()` لمتابعة التحسن
5. **استخدم التغذية الراجعة**: أضف تعليقات مفصلة عند التقييم

---

**جاهز للتشغيل! 🚀**

