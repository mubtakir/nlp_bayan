# ============================================
# اختبار النظام الذكي - Intelligent System Test
# ============================================

طباعة("🚀 بدء اختبار النظام الذكي المتكامل")
طباعة("=" * 60)
طباعة("")

# ============================================
# 1. إضافة معرفة أولية
# ============================================

طباعة("📚 الخطوة 1: إضافة معرفة أولية...")
طباعة("")

# إضافة حقائق عن الذكاء الاصطناعي
إضافة_حقيقة("الذكاء_الاصطناعي", "هو", "محاكاة الذكاء البشري بواسطة الآلات", 0.95, "معرفة_أساسية")
إضافة_حقيقة("التعلم_الآلي", "هو", "فرع من الذكاء الاصطناعي", 0.9, "معرفة_أساسية")
إضافة_حقيقة("الشبكات_العصبية", "هي", "نموذج حسابي مستوحى من الدماغ", 0.9, "معرفة_أساسية")

# إضافة حقائق عن لغة البيان
إضافة_حقيقة("لغة_البيان", "هي", "لغة برمجة هجينة ثنائية اللغة", 0.95, "معرفة_أساسية")
إضافة_حقيقة("لغة_البيان", "تدعم", "البرمجة المنطقية والاحتمالية", 0.9, "معرفة_أساسية")

طباعة("✅ تم إضافة 5 حقائق")
طباعة("")

# ============================================
# 2. اختبار الأسئلة البسيطة
# ============================================

طباعة("=" * 60)
طباعة("💬 الخطوة 2: اختبار الأسئلة البسيطة")
طباعة("=" * 60)
طباعة("")

# سؤال 1
طباعة("👤 السؤال 1: ما هو الذكاء الاصطناعي؟")
إجابة1 = سؤال("ما هو الذكاء الاصطناعي؟")
طباعة("🤖 الإجابة:", إجابة1)
طباعة("")

# سؤال 2
طباعة("👤 السؤال 2: ما هي لغة البيان؟")
إجابة2 = سؤال("ما هي لغة البيان؟")
طباعة("🤖 الإجابة:", إجابة2)
طباعة("")

# ============================================
# 3. اختبار الأسئلة مع التفاصيل
# ============================================

طباعة("=" * 60)
طباعة("🔍 الخطوة 3: اختبار الأسئلة مع التفاصيل")
طباعة("=" * 60)
طباعة("")

طباعة("👤 السؤال: ما هو التعلم الآلي؟")
نتيجة = سؤال_مع_تفاصيل("ما هو التعلم الآلي؟")
طباعة("🤖 الإجابة:", نتيجة["إجابة"])
طباعة("📊 الثقة:", نتيجة["ثقة"])
طباعة("📚 المصادر:", نتيجة["مصادر"])
طباعة("")

# ============================================
# 4. اختبار التقييم والتعلم
# ============================================

طباعة("=" * 60)
طباعة("🎓 الخطوة 4: اختبار التقييم والتعلم")
طباعة("=" * 60)
طباعة("")

طباعة("✅ تقييم الإجابة السابقة...")
تقييم_إجابة("ما هو التعلم الآلي؟", نتيجة["إجابة"], 0.9, "إجابة جيدة")
طباعة("✅ تم التعلم من الحوار")
طباعة("")

# ============================================
# 5. اختبار التحديث
# ============================================

طباعة("=" * 60)
طباعة("🔄 الخطوة 5: تحديث النظام")
طباعة("=" * 60)
طباعة("")

نتيجة_تحديث = تحديث_النظام_الذكي()
طباعة("✅ تم التحديث بنجاح!")
طباعة("📈 معدل الأداء:", نتيجة_تحديث["معدل_الأداء"])
طباعة("🧠 أنماط جديدة:", نتيجة_تحديث["أنماط_جديدة"])
طباعة("")

# ============================================
# 6. عرض الإحصائيات
# ============================================

طباعة("=" * 60)
طباعة("📊 الخطوة 6: إحصائيات النظام")
طباعة("=" * 60)
طباعة("")

الحالة = حالة_النظام()

طباعة("🤖 النظام:")
طباعة("   • الحالة:", الحالة["النظام"]["الحالة"])
طباعة("   • عدد الحوارات:", الحالة["النظام"]["عدد_الحوارات"])
طباعة("   • معدل الأداء:", الحالة["النظام"]["معدل_الأداء"])
طباعة("")

طباعة("📚 قاعدة المعرفة:")
طباعة("   • عدد الحقائق:", الحالة["المعرفة"]["عدد_الحقائق"])
طباعة("   • عدد القواعد:", الحالة["المعرفة"]["عدد_القواعد"])
طباعة("   • عدد الأنماط:", الحالة["المعرفة"]["عدد_الأنماط"])
طباعة("")

طباعة("🎓 نظام التعلم:")
طباعة("   • عدد الحوارات:", الحالة["التعلم"]["عدد_الحوارات"])
طباعة("   • معدل النجاح:", الحالة["التعلم"]["معدل_النجاح"])
طباعة("")

# ============================================
# 7. النتيجة النهائية
# ============================================

طباعة("=" * 60)
طباعة("🎉 اكتمل الاختبار بنجاح!")
طباعة("=" * 60)
طباعة("")
طباعة("✅ النظام يعمل بشكل صحيح")
طباعة("✅ جميع المكونات متكاملة")
طباعة("✅ النظام جاهز للاستخدام")
طباعة("")

