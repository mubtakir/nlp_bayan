# ═══════════════════════════════════════════════════════════════════════════
# 🏆 تحدي بيان #2: شبكة سببية متسلسلة مع احتمالات
# Bayan Challenge #2: Causal Network with Probabilities
# ═══════════════════════════════════════════════════════════════════════════
# 15 سطر فقط! / Only 15 lines!

hybrid {
    # شبكة سببية للحالات النفسية
    fact يسبب("ضغط_نفسي", "قلق", 0.9).
    fact يسبب("قلق", "أرق", 0.85).
    fact يسبب("أرق", "تعب", 0.95).
    fact يسبب("تعب", "ضعف_تركيز", 0.8).
    
    # علاجات
    fact يخفف("استرخاء", "قلق", 0.7).
    fact يخفف("نوم_كافي", "تعب", 0.9).
    
    # قواعد الاستدلال السببي
    rule سلسلة_سببية(?س, ?ص, ?ق) :- يسبب(?س, ?ص, ?ق).
    rule سلسلة_سببية(?س, ?ز, ?ق) :- يسبب(?س, ?و, ?ق1), سلسلة_سببية(?و, ?ز, ?ق2).
    
    print("═══════════════════════════════════════════════════════════════")
    print("🏆 تحدي بيان #2: شبكة سببية متسلسلة")
    print("═══════════════════════════════════════════════════════════════")
    print("")
    print("📊 الاستعلامات:")
}

print("1️⃣ ما الذي يسببه الضغط النفسي؟")
query سلسلة_سببية("ضغط_نفسي", ?نتيجة, ?احتمال).

print("")
print("2️⃣ ما الذي يخفف القلق؟")
query يخفف(?علاج, "قلق", ?فعالية).

print("")
print("✅ التحدي مكتمل!")

