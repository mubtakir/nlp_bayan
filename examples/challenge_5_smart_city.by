# ═══════════════════════════════════════════════════════════════════════════
# 🏙️ تحدي بيان #5: مدينة ذكية + بشر + عدوى + اقتصاد + زمن + موارد
# Bayan Challenge #5: Smart City + Humans + Pandemic + Economy + Time + Resources
# ═══════════════════════════════════════════════════════════════════════════
# نموذج AGI مصغّر: Neuro-Semantic-Causal-Temporal-Economic Fusion
# 30 سطر فقط! / Only 30 lines!

hybrid {
    # === الشبكة الدلالية (Ontology) ===
    fact نوع_من("بشر", "كائن").
    fact نوع_من("إنسان", "بشر").
    fact نوع_من("مرض", "حالة").
    fact نوع_من("عدوى", "مرض").
    fact نوع_من("وباء", "عدوى").
    fact نوع_من("متجر", "مؤسسة").
    fact نوع_من("مدينة", "مكان").
    
    # === الكيانات مع الحالات ===
    entity أحمد {
        states: {"صحة": 0.9, "مال": 50, "طعام": 0.8},
        properties: {"نوع": "إنسان", "مدينة": "دبي"}
    }
    entity فاطمة {
        states: {"صحة": 0.95, "مال": 60, "طعام": 0.7},
        properties: {"نوع": "إنسان", "مدينة": "دبي"}
    }
    
    # === العلاقات الاجتماعية ===
    fact يسكن("أحمد", "دبي").
    fact يسكن("فاطمة", "دبي").
    fact صداقة("أحمد", "فاطمة").
    fact بيع("سوق_دبي", "أحمد").
    
    # === الشبكة السببية (الوباء والاقتصاد) ===
    fact يسبب("ازدحام", "عدوى", 0.7).
    fact يسبب("عدوى", "مرض", 0.8).
    fact يسبب("مرض", "ضعف_طعام", 0.9).
    fact يسبب("شراء", "نقص_مال", 1.0).
    fact يسبب("مخالطة", "انتقال_عدوى", 0.6).
    
    # === قواعد الاستدلال ===
    # وراثة الأنواع
    rule هو_نوع(?س, ?ص) :- نوع_من(?س, ?ص).
    rule هو_نوع(?س, ?ز) :- نوع_من(?س, ?و), هو_نوع(?و, ?ز).
    
    # سلسلة سببية
    rule يؤدي_إلى(?س, ?ص, ?ق) :- يسبب(?س, ?ص, ?ق).
    rule يؤدي_إلى(?س, ?ز, ?ق) :- يسبب(?س, ?و, ?ق1), يؤدي_إلى(?و, ?ز, ?ق2).
    
    # انتقال العدوى بالمخالطة
    rule قد_يصاب(?ش2) :- صداقة(?ش1, ?ش2), يسبب("مخالطة", "انتقال_عدوى", ?ق).
    
    # سكان نفس المدينة معرضون للازدحام
    rule معرض_للعدوى(?ش) :- يسكن(?ش, ?م), يسبب("ازدحام", "عدوى", ?ق).
    
    print("═══════════════════════════════════════════════════════════════")
    print("🏙️ تحدي بيان #5: مدينة ذكية + وباء + اقتصاد")
    print("═══════════════════════════════════════════════════════════════")
    print("")
}

print("📊 الاستعلامات:")
print("")
print("1️⃣ ما أنواع الوباء؟ (شبكة دلالية)")
query هو_نوع("وباء", ?نوع).

print("")
print("2️⃣ ماذا يسبب الازدحام؟ (سلسلة سببية)")
query يؤدي_إلى("ازدحام", ?نتيجة, ?احتمال).

print("")
print("3️⃣ من قد يصاب بالعدوى؟ (مخالطة)")
query قد_يصاب(?شخص).

print("")
print("4️⃣ من معرض للعدوى؟ (سكان المدينة)")
query معرض_للعدوى(?شخص).

print("")
print("5️⃣ من يسكن دبي؟")
query يسكن(?شخص, "دبي").

print("")
print("═══════════════════════════════════════════════════════════════")
print("✅ نموذج المدينة الذكية مكتمل!")
print("═══════════════════════════════════════════════════════════════")
print("")
print("💡 هذا النموذج يجمع:")
print("   ✓ شبكة دلالية (كائن→بشر→إنسان + مرض→عدوى→وباء)")
print("   ✓ علاقات اجتماعية (سكن، صداقة، بيع)")
print("   ✓ شبكة سببية وبائية (ازدحام→عدوى→مرض)")
print("   ✓ اقتصاد مصغر (شراء→نقص مال)")
print("   ✓ انتقال العدوى بالمخالطة")
print("")
print("🔥 في 30 سطر فقط وبدون أي مكتبات!")

