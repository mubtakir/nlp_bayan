# ═══════════════════════════════════════════════════════════════════════════
# 🏆 تحدي بيان النهائي: دمج الكل معاً!
# Bayan Ultimate Challenge: Everything Combined!
# ═══════════════════════════════════════════════════════════════════════════
# 25 سطر فقط! / Only 25 lines!
# كيانات + شبكة دلالية + شبكة سببية + علاقات + استعلامات

hybrid {
    # === الكيانات ===
    entity شخص {
        states: {"صحة": 0.9, "مزاج": 0.7},
        actions: {"يمرض": {"effects": [{"on": "صحة", "formula": "max(value-0.3,0)"}]}}
    }
    
    # === الشبكة الدلالية ===
    fact نوع_من("مرض", "حالة").
    fact نوع_من("زكام", "مرض").
    fact نوع_من("عدوى", "مرض").
    fact نوع_من("حمى", "مرض").
    
    # === الشبكة السببية ===
    fact يسبب("برد", "زكام").
    fact يسبب("زكام", "عدوى").
    fact يسبب("عدوى", "حمى").
    fact يسبب("حمى", "تعب").
    
    # === العلاقات ===
    fact صداقة("أحمد", "خالد").
    fact صداقة("خالد", "سعيد").
    fact ينتقل("زكام", "مصافحة").
    fact ينتقل("عدوى", "مصافحة").
    
    # === قواعد الاستدلال ===
    # سلسلة سببية
    rule سلسلة(?س, ?ص) :- يسبب(?س, ?ص).
    rule سلسلة(?س, ?ز) :- يسبب(?س, ?و), سلسلة(?و, ?ز).
    
    # انتقال المرض
    rule يصيب(?م, ?ش2) :- صداقة(?ش1, ?ش2), ينتقل(?م, "مصافحة").
    
    # هرمية الأنواع
    rule هو_نوع(?س, ?ص) :- نوع_من(?س, ?ص).
    rule هو_نوع(?س, ?ز) :- نوع_من(?س, ?و), هو_نوع(?و, ?ز).
    
    print("═══════════════════════════════════════════════════════════════")
    print("🏆 تحدي بيان النهائي: دمج الكل معاً!")
    print("═══════════════════════════════════════════════════════════════")
    print("")
    print("📊 الاستعلامات:")
}

print("")
print("1️⃣ ما الذي يسببه البرد؟ (سلسلة سببية)")
query سلسلة("برد", ?نتيجة).

print("")
print("2️⃣ من قد يصاب بالزكام؟ (انتقال عبر العلاقات)")
query يصيب("زكام", ?شخص).

print("")
print("3️⃣ ما أنواع الزكام؟ (شبكة دلالية)")
query هو_نوع("زكام", ?نوع).

print("")
print("4️⃣ ما الأمراض التي تنتقل بالمصافحة؟")
query ينتقل(?مرض, "مصافحة").

print("")
print("═══════════════════════════════════════════════════════════════")
print("✅ التحدي النهائي مكتمل!")
print("═══════════════════════════════════════════════════════════════")
print("")
print("💡 هذا البرنامج يجمع:")
print("   ✓ كيانات مع حالات ديناميكية")
print("   ✓ شبكة دلالية هرمية")
print("   ✓ شبكة سببية متسلسلة")
print("   ✓ علاقات بين كيانات")
print("   ✓ استعلامات منطقية متعددة")
print("")
print("🔥 في 25 سطر فقط وبدون أي مكتبات!")

