# ═══════════════════════════════════════════════════════════════════════════
# أمثلة على نظام التفسير
# Explanation System Examples
# ═══════════════════════════════════════════════════════════════════════════

هجين {
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("📖 أمثلة نظام التفسير").
    طباعة("   Explanation System Examples").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # بناء قاعدة المعرفة
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("📚 بناء قاعدة المعرفة...").
    طباعة("").
    
    # قوانين سببية
    سبب_نتيجة("رفع_شيء_لفوق", "يسقط", "جاذبية", 1.0).
    سبب_نتيجة("تسخين_ماء", "يغلي", "طاقة_حرارية", 0.95).
    سبب_نتيجة("دراسة_مجتهدة", "نجاح", "اكتساب_معرفة", 0.9).
    سبب_نتيجة("رياضة_منتظمة", "صحة_جيدة", "تقوية_عضلات", 0.85).
    سبب_نتيجة("قراءة_كثيرة", "ثقافة_واسعة", "تراكم_معرفي", 0.8).
    
    # علاقات دلالية
    علاقة("الإنسان", "له", "بيت", 0.9).
    علاقة("البيت", "فيه", "حمام", 0.95).
    علاقة("البيت", "فيه", "مطبخ", 0.95).
    علاقة("الاستحمام", "في", "حمام", 0.9).
    علاقة("الطعام", "في", "مطبخ", 0.95).
    علاقة("النهر", "هو", "ماء", 1.0).
    علاقة("الكتاب", "لل", "قراءة", 0.95).
    
    طباعة("✅ تم بناء قاعدة المعرفة").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 1️⃣ شرح قانون سببي
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("1️⃣ شرح قانون سببي - Explaining Causal Law").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    # استعلام عن قانون سببي
    استعلام سبب_نتيجة("رفع_شيء_لفوق", ?نتيجة, ?سبب, ?قوة).
    
    طباعة("📊 القانون السببي:").
    طباعة("   الشرط: رفع_شيء_لفوق").
    طباعة("   النتيجة: " + ?نتيجة).
    طباعة("   السبب: " + ?سبب).
    طباعة("   القوة: " + ?قوة).
    طباعة("").
    
    # ملاحظة: في التطبيق الفعلي، يمكن استدعاء دالة اشرح_سبب_نتيجة
    # تفسير = اشرح_سبب_نتيجة("رفع_شيء_لفوق", ?نتيجة, ?سبب, ?قوة).
    # اطبع_تفسير(تفسير, "عربي").
    
    طباعة("💡 التفسير:").
    طباعة("   عندما يحدث: رفع_شيء_لفوق").
    طباعة("   النتيجة: " + ?نتيجة).
    طباعة("   السبب الفيزيائي: " + ?سبب).
    طباعة("   قوة العلاقة: " + ?قوة + " (قوي جداً - شبه مؤكد)").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 2️⃣ شرح علاقة دلالية
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("2️⃣ شرح علاقة دلالية - Explaining Semantic Relation").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    استعلام علاقة("البيت", ?نوع, "حمام", ?قوة2).
    
    طباعة("📊 العلاقة الدلالية:").
    طباعة("   من: البيت").
    طباعة("   نوع العلاقة: " + ?نوع).
    طباعة("   إلى: حمام").
    طباعة("   القوة: " + ?قوة2).
    طباعة("").
    
    طباعة("💡 التفسير:").
    طباعة("   المفهوم الأول: البيت").
    طباعة("   نوع العلاقة: " + ?نوع + " (علاقة مكانية)").
    طباعة("   المفهوم الثاني: حمام").
    طباعة("   قوة العلاقة: " + ?قوة2 + " (قوي جداً)").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 3️⃣ شرح مسار بين مفهومين
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("3️⃣ شرح مسار - Explaining Path").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال: كيف يرتبط الإنسان بالحمام؟").
    طباعة("").
    
    # البحث عن مسار غير مباشر
    استعلام علاقة("الإنسان", ?نوع1, ?وسيط, ?قوة3).
    استعلام علاقة(?وسيط, ?نوع2, "حمام", ?قوة4).
    
    طباعة("📊 المسار:").
    طباعة("   الإنسان " + ?نوع1 + " " + ?وسيط + " " + ?نوع2 + " حمام").
    طباعة("   القوة الكلية: " + (?قوة3 * ?قوة4)).
    طباعة("").
    
    طباعة("💡 التفسير:").
    طباعة("   المسار من 'الإنسان' إلى 'حمام':").
    طباعة("   ✅ مسار غير مباشر (خطوتان)").
    طباعة("   الخطوة 1: الإنسان " + ?نوع1 + " " + ?وسيط).
    طباعة("   الخطوة 2: " + ?وسيط + " " + ?نوع2 + " حمام").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 4️⃣ تفسير تلقائي للأسئلة
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("4️⃣ تفسير تلقائي - Auto Explanation").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال 1: لماذا يسقط الشيء؟").
    طباعة("💡 هذا سؤال سببي - يبحث عن السبب وراء نتيجة").
    طباعة("   استخدم: استعلام سبب_نتيجة(?شرط, 'يسقط', ?سبب, ?قوة)").
    طباعة("").
    
    طباعة("❓ السؤال 2: ماذا يحدث عند التسخين؟").
    طباعة("💡 هذا سؤال تنبؤي - يبحث عن النتيجة المتوقعة").
    طباعة("   استخدم: استعلام سبب_نتيجة('تسخين_ماء', ?نتيجة, ?سبب, ?قوة)").
    طباعة("").
    
    طباعة("❓ السؤال 3: أين يوجد الحمام؟").
    طباعة("💡 هذا سؤال مكاني - يبحث عن موقع").
    طباعة("   استخدم: استعلام علاقة(?مكان, 'فيه', 'حمام', ?قوة)").
    طباعة("").
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("✅ تم اختبار نظام التفسير بنجاح!").
    طباعة("   Explanation system tested successfully!").
    طباعة("═══════════════════════════════════════════════════════════").
}

