# ═══════════════════════════════════════════════════════════════════════════
# أمثلة على استخدام الدوال المساعدة للاستدلال
# Examples of Using Reasoning Helper Functions
# ═══════════════════════════════════════════════════════════════════════════

هجين {
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("🧠 أمثلة الدوال المساعدة للاستدلال").
    طباعة("   Reasoning Helper Functions Examples").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # بناء قاعدة المعرفة
    # Building Knowledge Base
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("📚 بناء قاعدة المعرفة...").
    طباعة("").
    
    # قوانين سببية - Causal laws
    سبب_نتيجة("رفع_شيء_لفوق", "يسقط", "جاذبية", 1.0).
    سبب_نتيجة("تسخين_ماء", "يغلي", "طاقة_حرارية", 0.95).
    سبب_نتيجة("دراسة_مجتهدة", "نجاح", "اكتساب_معرفة", 0.9).
    سبب_نتيجة("رياضة_منتظمة", "صحة_جيدة", "تقوية_عضلات", 0.85).
    سبب_نتيجة("جوع", "بحث_عن_طعام", "حاجة_بيولوجية", 1.0).
    
    # علاقات دلالية - Semantic relations
    علاقة("الإنسان", "له", "بيت", 0.9).
    علاقة("البيت", "فيه", "حمام", 0.95).
    علاقة("البيت", "فيه", "مطبخ", 0.95).
    علاقة("الاستحمام", "في", "حمام", 0.9).
    علاقة("الطعام", "في", "مطبخ", 0.95).
    علاقة("النهر", "هو", "ماء", 1.0).
    علاقة("البحر", "هو", "ماء", 1.0).
    
    طباعة("✅ تم بناء قاعدة المعرفة").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 1️⃣ اختبار دالة "لماذا؟" - Testing "why?" function
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("1️⃣ اختبار دالة 'لماذا؟' - Testing 'why?' function").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال: لماذا يسقط الشيء؟").
    طباعة("   Question: Why does something fall?").
    طباعة("").
    
    # استعلام مباشر
    استعلام سبب_نتيجة(?شرط, "يسقط", ?سبب, ?قوة).
    طباعة("📊 النتيجة: الشرط = " + ?شرط + ", السبب = " + ?سبب + ", القوة = " + ?قوة).
    طباعة("").
    
    طباعة("❓ السؤال: لماذا ينجح الطالب؟").
    طباعة("   Question: Why does a student succeed?").
    طباعة("").
    
    استعلام سبب_نتيجة(?شرط2, "نجاح", ?سبب2, ?قوة2).
    طباعة("📊 النتيجة: الشرط = " + ?شرط2 + ", السبب = " + ?سبب2 + ", القوة = " + ?قوة2).
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 2️⃣ اختبار دالة "ماذا لو؟" - Testing "what if?" function
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("2️⃣ اختبار دالة 'ماذا لو؟' - Testing 'what if?' function").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال: ماذا لو قمنا بتسخين الماء؟").
    طباعة("   Question: What if we heat water?").
    طباعة("").
    
    استعلام سبب_نتيجة("تسخين_ماء", ?نتيجة, ?سبب3, ?قوة3).
    طباعة("📊 النتيجة: " + ?نتيجة + ", السبب = " + ?سبب3 + ", القوة = " + ?قوة3).
    طباعة("").
    
    طباعة("❓ السؤال: ماذا لو مارسنا الرياضة بانتظام؟").
    طباعة("   Question: What if we exercise regularly?").
    طباعة("").
    
    استعلام سبب_نتيجة("رياضة_منتظمة", ?نتيجة2, ?سبب4, ?قوة4).
    طباعة("📊 النتيجة: " + ?نتيجة2 + ", السبب = " + ?سبب4 + ", القوة = " + ?قوة4).
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 3️⃣ اختبار دالة "كيف؟" - Testing "how?" function
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("3️⃣ اختبار دالة 'كيف؟' - Testing 'how?' function").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال: كيف يرتبط الإنسان بالحمام؟").
    طباعة("   Question: How is human related to bathroom?").
    طباعة("").
    
    # مسار مباشر
    استعلام علاقة("البيت", ?نوع1, "حمام", ?قوة5).
    طباعة("📊 علاقة مباشرة: البيت " + ?نوع1 + " حمام (قوة: " + ?قوة5 + ")").
    طباعة("").
    
    # مسار غير مباشر
    طباعة("❓ السؤال: كيف يرتبط الإنسان بالمطبخ؟").
    طباعة("   Question: How is human related to kitchen?").
    طباعة("").
    
    استعلام علاقة("الإنسان", ?نوع2, ?وسيط, ?قوة6).
    استعلام علاقة(?وسيط, ?نوع3, "مطبخ", ?قوة7).
    طباعة("📊 مسار غير مباشر: الإنسان " + ?نوع2 + " " + ?وسيط + " " + ?نوع3 + " مطبخ").
    طباعة("   القوة الكلية: " + (?قوة6 * ?قوة7)).
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # 4️⃣ اختبار دالة "ما العلاقة؟" - Testing "what relation?" function
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("4️⃣ اختبار دالة 'ما العلاقة؟' - Testing 'what relation?'").
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("").
    
    طباعة("❓ السؤال: ما علاقة النهر بالماء؟").
    طباعة("   Question: What is the relation between river and water?").
    طباعة("").
    
    استعلام علاقة("النهر", ?نوع4, "ماء", ?قوة8).
    طباعة("📊 العلاقة: النهر " + ?نوع4 + " ماء (قوة: " + ?قوة8 + ")").
    طباعة("").
    
    طباعة("❓ السؤال: ما علاقة الاستحمام بالحمام؟").
    طباعة("   Question: What is the relation between bathing and bathroom?").
    طباعة("").
    
    استعلام علاقة("الاستحمام", ?نوع5, "حمام", ?قوة9).
    طباعة("📊 العلاقة: الاستحمام " + ?نوع5 + " حمام (قوة: " + ?قوة9 + ")").
    طباعة("").
    
    # ═══════════════════════════════════════════════════════════════════════
    # الخلاصة
    # ═══════════════════════════════════════════════════════════════════════
    
    طباعة("═══════════════════════════════════════════════════════════").
    طباعة("✅ تم اختبار جميع الدوال المساعدة بنجاح!").
    طباعة("   All helper functions tested successfully!").
    طباعة("═══════════════════════════════════════════════════════════").
}

