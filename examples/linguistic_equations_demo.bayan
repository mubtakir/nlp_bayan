# أمثلة على المعادلات اللغوية
# ====================================

print("═══════════════════════════════════════════════════════")
print("     المعادلات اللغوية - تحويل الأفكار إلى معادلات")
print("═══════════════════════════════════════════════════════\n")

# إنشاء قاعدة المعرفة
kb = KnowledgeBase()

# 📌 مثال 1: محمد أكل تفاحة
print("📌 مثال 1: محمد أكل تفاحة\n")

eq1 = create_simple_equation("محمد", "أكل", "تفاحة", kb)

print("الجملة الطبيعية:")
print(f"  {eq1.to_natural_language()}")

print("\nالصيغة الرياضية:")
print(f"  {eq1.to_formal_notation()}")

print("\nالنتائج المستنتجة:")
for result in eq1.results:
    print(f"  • {result}")

# 📌 مثال 2: أحمد ضرب الكرة
print("\n" + "─" * 60)
print("\n📌 مثال 2: أحمد ضرب الكرة\n")

eq2 = LinguisticEquation(
    entities={"أحمد": Role.SUBJECT, "الكرة": Role.OBJECT},
    event="ضرب",
    event_type=EventType.PHYSICAL_ACTION
)

# حساب النتائج
eq2.results = eq2.compute_results(kb)

print("الجملة الطبيعية:")
print(f"  {eq2.to_natural_language()}")

print("\nالصيغة الرياضية:")
print(f"  {eq2.to_formal_notation()}")

print("\nالنتائج:")
for result in eq2.results:
    print(f"  • {result}")

# 📌 مثال 3: تحليل تلقائي للجمل
print("\n" + "─" * 60)
print("\n📌 مثال 3: تحليل تلقائي للجمل\n")

parser = LinguisticEquationParser(kb)

sentences = [
    "زيد أكل خبز",
    "سارة شربت ماء",
    "علي كتب رسالة",
    "فاطمة ذهبت"
]

for sentence in sentences:
    print(f"📝 تحليل: \"{sentence}\"")
    
    eq = parser.parse(sentence)
    if eq:
        print(f"   المعادلة: {eq.to_formal_notation()}")
        if eq.results:
            print("   النتائج:")
            for r in eq.results:
                print(f"     - {r}")
    else:
        print("   ❌ فشل التحليل")
    print()

# 📌 مثال 4: إضافة حدث مخصص
print("─" * 60)
print("\n📌 مثال 4: إضافة حدث مخصص\n")

# إضافة فعل "درس" إلى قاعدة المعرفة
kb.add_custom_event(
    event="درس",
    subject_changes={"تعب": +0.3, "معرفة": +0.6, "تركيز": -0.2},
    object_changes={"مستوى_الفهم": +0.5}
)

# استخدام الفعل الجديد
eq4 = create_simple_equation("خالد", "درس", "رياضيات", kb)

print(f"المعادلة: {eq4.to_formal_notation()}")
print("\nالنتائج:")
for result in eq4.results:
    print(f"  • {result}")

print("\n✅ اكتملت جميع الأمثلة بنجاح!")
