# ═══════════════════════════════════════════════════════════════════════════
# 🏆 تحدي بيان #3: شبكة دلالية هرمية
# Bayan Challenge #3: Semantic Network with Inheritance
# ═══════════════════════════════════════════════════════════════════════════
# 14 سطر فقط! / Only 14 lines!

hybrid {
    # هرمية المفاهيم (Taxonomy)
    fact نوع_من("كائن_حي", "شيء").
    fact نوع_من("إنسان", "كائن_حي").
    fact نوع_من("حيوان", "كائن_حي").
    fact نوع_من("قط", "حيوان").
    fact نوع_من("طالب", "إنسان").
    fact نوع_من("مبرمج", "إنسان").
    
    # خصائص تنتقل بالوراثة
    fact خاصية("كائن_حي", "يتنفس").
    fact خاصية("كائن_حي", "يتغذى").
    fact خاصية("إنسان", "يفكر").
    fact خاصية("إنسان", "يتكلم").
    fact خاصية("قط", "يموء").
    fact خاصية("مبرمج", "يكتب_كود").
    
    # وراثة الخصائص
    rule يملك(?س, ?خ) :- خاصية(?س, ?خ).
    rule يملك(?س, ?خ) :- نوع_من(?س, ?أب), يملك(?أب, ?خ).
    
    # التحقق من النوع
    rule هو_نوع(?س, ?ص) :- نوع_من(?س, ?ص).
    rule هو_نوع(?س, ?ز) :- نوع_من(?س, ?و), هو_نوع(?و, ?ز).
    
    print("═══════════════════════════════════════════════════════════════")
    print("🏆 تحدي بيان #3: شبكة دلالية هرمية")
    print("═══════════════════════════════════════════════════════════════")
    print("")
    print("📊 الاستعلامات:")
}

print("1️⃣ ما خصائص الطالب؟ (يرث من إنسان وكائن_حي)")
query يملك("طالب", ?خاصية).

print("")
print("2️⃣ ما خصائص القط؟")
query يملك("قط", ?خاصية).

print("")
print("3️⃣ ما أنواع المبرمج؟")
query هو_نوع("مبرمج", ?نوع).

print("")
print("✅ التحدي مكتمل!")

