# تصنيف k-أقرب جيران (عربي)

```bayan
include "ai/ml.bayan"

بيانات = [ [0,0], [0,1], [1,0], [1,1] ]
تسميات = [0, 0, 0, 1]
عينات = [ [1,1], [0,0] ]

توقع = توقع_k_متجاور_أقرب(بيانات, تسميات, عينات, k=3)

print("التوقع:")
print(توقع)
```
