# 📘 دليل كتابة كود بيان في VS Code

## 🎯 الخطوات السريعة

```
1. تثبيت إضافة البيان ← 2. إنشاء ملف .by ← 3. كتابة الكود ← 4. تشغيله
```

---

## 📋 المتطلبات

| المتطلب | الإصدار | التحقق |
|---------|---------|--------|
| Python | 3.10+ | `python --version` |
| VS Code | أي إصدار | افتحه |
| مجلد البيان | موجود | `/home/al-mubtakir/Documents/bayan_python_ide2` |

---

## 🔧 الخطوة 1: تثبيت إضافة البيان في VS Code

### الطريقة السريعة (symbolic link):

```bash
# افتح Terminal واكتب:
ln -s /home/al-mubtakir/Documents/bayan_python_ide2/vscode-bayan ~/.vscode/extensions/bayan-0.1.0
```

### ثم أعد تحميل VS Code:
1. اضغط `Ctrl+Shift+P`
2. اكتب `Developer: Reload Window`
3. اضغط Enter

### للتحقق من التثبيت:
1. اضغط `Ctrl+Shift+X` (الإضافات)
2. ابحث عن "Bayan"
3. يجب أن تظهر الإضافة ✅

---

## 📁 الخطوة 2: إنشاء ملف بيان جديد

### في VS Code:
1. اضغط `Ctrl+N` (ملف جديد)
2. اضغط `Ctrl+S` لحفظه
3. سمّه بامتداد `.by` مثل: `برنامجي.by`

### أو من Terminal:
```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2/examples
touch برنامجي.by
code برنامجي.by
```

---

## ✍️ الخطوة 3: كتابة أول برنامج

### برنامج بسيط:
```bayan
# أول برنامج بيان
print("مرحباً بالعالم!")

x = 10
y = 20
print("المجموع: " + str(x + y))
```

### برنامج مع منطق (hybrid):
```bayan
hybrid {
    # تعريف حقائق
    fact صداقة("أحمد", "خالد").
    fact صداقة("خالد", "سعيد").
    
    # تعريف قاعدة
    rule صديق_صديقي(?أ, ?ج) :- صداقة(?أ, ?ب), صداقة(?ب, ?ج).
}

# استعلام
print("أصدقاء أصدقاء أحمد:")
query صديق_صديقي("أحمد", ?من).
```

### برنامج مع كيانات:
```bayan
hybrid {
    entity شخص {
        states: {"صحة": 0.9, "طاقة": 0.8},
        properties: {"اسم": "أحمد", "عمر": 25},
        actions: {
            "يأكل": {"effects": [{"on": "طاقة", "formula": "min(value + 0.2, 1.0)"}]}
        }
    }
}

print("حالة الشخص:")
query state("شخص", "صحة", ?ص).
query state("شخص", "طاقة", ?ط).
```

---

## ▶️ الخطوة 4: تشغيل البرنامج

### الطريقة 1: من Terminal داخل VS Code

1. افتح Terminal: اضغط `` Ctrl+` ``
2. شغّل:

```bash
cd /home/al-mubtakir/Documents/bayan_python_ide2
python -c "from bayan.main import run_file; run_file('examples/برنامجي.by')"
```

### الطريقة 2: إنشاء اختصار (task)

1. اضغط `Ctrl+Shift+P`
2. اكتب `Tasks: Configure Task`
3. اختر `Create tasks.json file from template`
4. اختر `Others`
5. استبدل المحتوى بـ:

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Bayan",
            "type": "shell",
            "command": "python",
            "args": [
                "-c",
                "from bayan.main import run_file; run_file('${file}')"
            ],
            "options": {
                "cwd": "/home/al-mubtakir/Documents/bayan_python_ide2"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
```

6. الآن اضغط `Ctrl+Shift+B` لتشغيل أي ملف بيان مفتوح!

---

## 💡 اختصارات مفيدة (Snippets)

بعد تثبيت الإضافة، اكتب واضغط Tab:

| الاختصار | النتيجة |
|----------|---------|
| `hybrid` + Tab | بلوك hybrid كامل |
| `entity` + Tab | تعريف كيان |
| `fact` + Tab | تعريف حقيقة |
| `rule` + Tab | تعريف قاعدة |
| `query` + Tab | استعلام |
| `def` + Tab | تعريف دالة |
| `class` + Tab | تعريف صنف |
| `for` + Tab | حلقة for |
| `if` + Tab | شرط if |

---

## 🎨 ميزات الإضافة

| الميزة | الحالة |
|--------|--------|
| ✅ تلوين الكود | 200+ كلمة مفتاحية |
| ✅ دعم العربية | كيان، قاعدة، حقيقة... |
| ✅ تلوين المتغيرات المنطقية | `?متغير` |
| ✅ Snippets | 20+ قالب جاهز |
| ✅ أقواس تلقائية | `{}`, `[]`, `()` |
| ✅ تعليقات | `#` |

---

## 📂 أمثلة جاهزة للتجربة

```bash
# تشغيل أمثلة التحديات:
python -c "from bayan.main import run_file; run_file('examples/challenge_1_entity_causal.by')"
python -c "from bayan.main import run_file; run_file('examples/challenge_7_weather.by')"
python -c "from bayan.main import run_file; run_file('examples/challenge_8_rpg_game.by')"
```

---

## 🔍 حل المشاكل الشائعة

### ❌ الإضافة لا تظهر؟
```bash
# تأكد من الرابط:
ls -la ~/.vscode/extensions/ | grep bayan
# إذا لم يظهر، أعد إنشاء الرابط
```

### ❌ خطأ عند التشغيل؟
```bash
# تأكد من المسار الصحيح:
cd /home/al-mubtakir/Documents/bayan_python_ide2
python -c "from bayan.main import run_file; print('OK')"
```

### ❌ لا يوجد تلوين؟
1. تأكد أن الملف بامتداد `.by` أو `.bayan`
2. انظر أسفل يمين VS Code - يجب أن يظهر "Bayan"
3. إذا لا، اضغط عليه واختر "Bayan"

---

## 📖 مراجع إضافية

| الملف | الوصف |
|-------|-------|
| `BAYAN_FOR_AI_MODELS.md` | دليل شامل للغة |
| `BAYAN_CHALLENGE.md` | تحديات وأمثلة |
| `examples/` | مجلد الأمثلة |
| `docs/LANGUAGE_GUIDE.md` | دليل اللغة المفصّل |

---

## 🚀 خلاصة

```
✅ ثبّت الإضافة (ln -s ...)
✅ أعد تحميل VS Code
✅ أنشئ ملف .by
✅ اكتب الكود
✅ شغّل بـ python -c "..."
```

**مبروك! أنت الآن جاهز لكتابة برامج بيان!** 🎉

---

*آخر تحديث: 2025-12-06*
